Finish building **aibrush-media** — a SOTA, capability-routed, in-browser media engine — to 100% of its Definition of Done, in /Users/tarekbadr/Home/software/projects/aibrush/aibrush.lib/media. **main is GREEN**; we currently rank **#2 of 8 (behind mediabunny, 0 wins / 13 ties)** — the job is to flip that to **#1 cross-browser**. DO NOT STOP until the DoD is green.

FIRST read in full (binding): @BUILD_INSTRUCTIONS_SESSION4.md (remaining-work plan + exact mechanics + verified state), BUILD_INSTRUCTIONS.md §2 (the DoD), CLAUDE.md, docs/architecture/. Restate the plan concretely, then build.

STATE (verified five-axis audit 2026-06-26 — build on it, do NOT redo): main GREEN — typecheck 0, `biome ci` 0, full Vitest suite passes, coverage ≥90% all metrics, build+budgets ok. DONE: 12 containers (demux/probe everywhere; mux for mp4+faststart+**fragmented/CMAF**, webm, ogg, mpegts, pcm-out wav/aiff/caf); all 9 ops incl. accurate trim (ADR-082), decrypt CENC/HLS, **preload** (ADR-083), **fluent chain** (ADR-010); filters resize/crop/rotate/flip + **full colorspace matrices** + **HDR→SDR tonemap** + audio resample/remix/gain/fade(PCM); decode via WebCodecs for all codecs + below-WebCodecs AAC/MP3/Vorbis(Symphonia wasm)/FLAC(pure-TS)/PCM; harness adapter registered + runs chromium. NOT DONE = the gaps in §3.

HOW TO WORK — MANDATORY: **ULTRATHINK at MAX reasoning effort before EVERY step** (restate goal; enumerate edge cases — B-frames, VFR, open-GOP, seek, cancel, frame lifetime, backpressure, truncated input, gapless/pre-skip, 10-bit/HDR, multi-channel; design note; THEN code). **Drive this with the Workflow tool — fan out parallel opus agents (MAX effort) on STRICTLY DISJOINT files (one per codec/driver/feature), as much as possible.** Pattern: understand → fan-out implement → adversarially verify (skeptic agents: frame leak? wrong on B-frames/VFR/10-bit? mutated golden? round-trip drift?) → LEADER integrates (full gate + harness serially, re-vendors). Per agent: ULTRATHINK → FAILING strict can-fail oracle first on ≥5 real DOWNLOADED files (ffprobe/ffmpeg/sips/afinfo/reference decoder) → SOTA implement → pass → fresh benchmark → self-verify (typecheck filtered to its files + `vitest run <its files>`) → structured summary. Keep agents WARM. **NEVER run an agent workflow concurrently with a browser harness run** — the browser starves and the run hangs (Session-2/3 lesson); serialize them.

PRIME DIRECTIVES (non-negotiable): ULTRATHINK max effort before every step. NEVER STOP until DoD §2 100% green; blocked → choose SOTA, record an ADR (**ADR-084+**), continue. EVERY LINE SOTA (strict TS, zero `any`/`!`, typed errors, exhaustive, no dead code; every VideoFrame/AudioData closed once). NO FEATURE WITHOUT TESTS (real downloaded corpus + can-fail oracle + fresh benchmark; one-file green is a fail). DOCS ARE LAW (change → doc + ADR same commit). NEVER FAKE (honest NA, no oracle that can't fail, no input→output passthrough). NEVER run git (the user commits). Keep main green.

MECHANICS (full detail in SESSION4 §4): bun 1.3.14; gate = typecheck + `biome ci` + `vitest run --coverage` + build + test:dist + check-budgets. WASM cores: small, permissive-licensed, per-codec (Rust+wasm-bindgen or Emscripten), self-hosted via import.meta.url, lazy + miss-only, no CDN, no COOP/COEP on the common path. Re-vendor (build+vendor-wasm, WIPE old vendor, copy dist, verify imports resolve) before each harness run; clear the `<browser>` cache; `cd ../media-test/media-browser-test && bash scripts/run.sh --engine aibrush-media --browser <chromium|webkit|firefox> [--feature <family>]`; score with `bun scripts/aggregate.mjs`; the adapter is the harness's live file (not vendored); on a hang, pkill the run tree and take the partial. Begin now: read @BUILD_INSTRUCTIONS_SESSION4.md, create the tasks, run §3.A (measure the baseline), then launch the §3.B/C fan-out workflows. Build until the WIN holds on all three browsers.
