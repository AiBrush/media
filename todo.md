# @aibrush/media — SOTA backlog — FROM SCRATCH 2026-08-30 14:00 — CLEAN (only open tests)
# Ground truth: cache-chromium-1788113203817.json (contentHash a4661438, 2026-08-30, chromium 152, suite 0.1.0) — aibrush-media@dev: 852 PASS + 9 FAIL + 3 NA_ENGINE + 0 ERROR (864 valid) — Winner race 366 win / 190 lose (image)
# Winners pinned for perf: mediabunny@1.48.0 108 wins, remotion 41, mp4box 26, web-demuxer 8, ffmpeg.wasm 7 — we must overtake each on its winning row
# Requirements: @REQUIREMENTS.md SOTA = zero FAIL/ERROR + 50KiB eager/250KiB typical/1MiB heavy + bounded memory + geomean leadership
# Anti-overfit: NEVER branch on fixture names/hashes/sizes/IDs/expected outputs — treat media-test as external evaluator. All fixes general.

## AGENT INSTRUCTIONS — READ FIRST, FOLLOW NON-STOP UNTIL 0 UNCHECKED
- You are the autonomous @aibrush/media SOTA loop. Work **non-stop, one item at a time (one, by, one)** until `grep -c "^- \[ \]" todo.md == 0` AND `bash scripts/run.sh --browser chromium --engine aibrush-media --pillar functional --no-reuse` shows **0 FAIL + 0 ERROR** on **every** test below + `bun run build && bun run check-budgets` PASS.
- **Goal:** Every `FAIL`→`PASS`, every `NA`→`PASS` or honest typed `NA`, and every `lose`→`win` (beat winner's clear wall by ≥5% beyond tie band, p95 ≤ winner, geomean leadership §8.2).
- **Never overfit:** Do not branch on fixture filename, URL path, exact byte length, hash, scenario ID, expected output, or timing constants. Every fix must be general (parameterized by bytes, config, capability).
- **Learn, don't copy:** You may inspect mediabunny/ffmpeg/mp4box/remotion public TS to understand their strategy (resize kernel, gapless math, tmcd drop, size chunking), but re-derive and implement a *better* generic solution (fewer copies, tighter error, lower memory, higher SSIM). Never paste their code.
- **Performance is mandatory:** After fixing **one** item, measure `vs pinned MediaBunny 1.48 / ffmpeg.wasm 0.12.15 / remotion 4.0.479 / mp4box 2.3.0` on representative inputs: tiny/short/long/4K/HFR/multitrack/high-latency-range; report median/p95/dispersion/throughput/longtasks; require family geomean ≤ fastest correct median and ≥90% within 5%.
- **One by one:** Pick top unchecked in `NEXT UP` (ordered P0 correctness > P0 performance > P1 coverage). Implement **smallest clean fix** in `aibrush.lib/media/src` (latest API only), add **5 variants** (unit/property/boundary/malformed/randomized), run gates: `bun run typecheck` + `vitest run` (focused) + `bash scripts/run.sh --browser chromium --feature <family> --engine aibrush-media --pillar functional --no-reuse --scenario <id>` + `bun run build && bun run check-budgets`.
- **Update todo.md in place:** check the box you just completed, keep `NEXT UP` re-ranked. Never create history docs. Never keep old [x] history — only open items remain unchecked.
- **Report:** before→after PASS counts + 5-10 `file:line` refs. If not zero FAIL/ERROR + budgets pass → answer NO and immediately start next cycle. Only answer YES when all boxes are [x] and full chromium cache re-run is PASS with 0 FAIL/ERROR and geomean leadership.

## WHERE WE STAND (cache 1788113203817, chromium 152, image)
- **Win 366 / Lose 190** per fully-reported row (fastest correct, tie band 5%): aibrush 366, mediabunny 108, remotion 41, mp4box 26, web-demuxer 8, ffmpeg 7 — total 556 clear winners.
- **Correctness:** 9 FAIL (7 distinct bases) — 3× `decode_rotated_display_matrix` DISPLAY_PIXEL_TRANSFORM_MISMATCH, 2× `performance` SSIM<0.97, 4× `transcode` audio SSIM/count. 0 ERROR (was 10) — fixed.
- **Coverage:** 3 NA where mediabunny wins (remux massive 1GiB, remux/trim tmcd) — we lose coverage.
- **Performance:** 204 rows where we are PASS but slower than winner (clear winner, ≥2 PASS, wall aggregate/median). Top loss 275× slower on `mux/mp4_streaming_target` vs mediabunny.
- **Families green:** audio-dsp 102, demux 139, encryption 48, probe 60, robustness 63, streaming-output 27 — keep green.

| Family | aibrush PASS/FAIL/NA | Winner on our loses | Gap |
|---|---|---|---|
| decode-seek | 137/3/0 | remotion 2, mediabunny 1 | rotated |
| performance | 31/2/0 | mediabunny 95 of our 204 | SSIM gate |
| transcode | 79/4/0 | mediabunny/ffmpeg | audio |
| remux | 47/0/2 | mp4box/mediabunny | tmcd/1GiB |
| trim | 41/0/1 | mp4box | tmcd |
| mux/demux/probe | 53/139/60 PASS | mediabunny/mp4box | wall |

## OPEN TESTS — 190 points, one per loser (7 FAIL + 3 NA + 180 perf) — each must become PASS and faster than winner

### P0 — FAIL (must PASS, then win)
- [x] 1. `decode-seek/decode_rotated_display_matrix` — FAIL `DISPLAY_PIXEL_TRANSFORM_MISMATCH` — displayed RGBA differs at 0µs; dimension-only metadata handling cannot pass — Winner: none (all FAIL) — **Goal:** Fix general logic (`src/api/decoded-display-rotation.ts:1` / `src/filters/gpu-video.ts:1` / `src/drivers/mp4/gapless.ts:1` / `src/dsp/resample.ts:1`), PASS ssim-psnr/property-invariant, then beat winner's wall by ≥10% via zero-copy. Variants: 5 as per instructions.
- [x] 2. `performance/convert-longtasks` — FAIL `ORACLE_MISMATCH` — vs in-browser reference (source decoded + resized to 320x180): SSIM mean 0.8855 (min 0.846 — Winner: none (all FAIL) — **Goal:** Fix general logic (`src/api/decoded-display-rotation.ts:1` / `src/filters/gpu-video.ts:1` / `src/drivers/mp4/gapless.ts:1` / `src/dsp/resample.ts:1`), PASS ssim-psnr/property-invariant, then beat winner's wall by ≥10% via zero-copy. Variants: 5 as per instructions.
- [x] 3. `performance/convert-webm-resize-320x180` — FAIL `ORACLE_MISMATCH` — vs in-browser reference (source decoded + resized to 320x180): SSIM mean 0.9134 (min 0.899 — Winner: none (all FAIL) — **Goal:** Fix general logic (`src/api/decoded-display-rotation.ts:1` / `src/filters/gpu-video.ts:1` / `src/drivers/mp4/gapless.ts:1` / `src/dsp/resample.ts:1`), PASS ssim-psnr/property-invariant, then beat winner's wall by ≥10% via zero-copy. Variants: 5 as per instructions.
- [x] 4. `transcode/aac_to_mp3_mp4` — FAIL `TRANSCODE_AUDIO_PRESENTATION_COUNT_MISMATCH` — decoder emitted 12143 presentation frame(s); container declares 12288 — Winner: none (all FAIL) — **Goal:** Fix general logic (`src/api/decoded-display-rotation.ts:1` / `src/filters/gpu-video.ts:1` / `src/drivers/mp4/gapless.ts:1` / `src/dsp/resample.ts:1`), PASS ssim-psnr/property-invariant, then beat winner's wall by ≥10% via zero-copy. Variants: 5 as per instructions.
- [x] 5. `transcode/selfcheck_h264_resize_720p_tie` — FAIL `ORACLE_MISMATCH` — vs in-browser reference (source decoded + resized to 1280x720): SSIM mean 0.9794 (min 0.97 — Winner: none (all FAIL) — **Goal:** Fix general logic (`src/api/decoded-display-rotation.ts:1` / `src/filters/gpu-video.ts:1` / `src/drivers/mp4/gapless.ts:1` / `src/dsp/resample.ts:1`), PASS ssim-psnr/property-invariant, then beat winner's wall by ≥10% via zero-copy. Variants: 5 as per instructions.
- [ ] 6. `transcode/wav_to_flac` — FAIL `TRANSCODE_AUDIO_LOSSLESS_CONTENT_MISMATCH` — maximum decoded-sample error 0.0003218650817871094 exceeds 0.000030517578125 — Winner: none (all FAIL) — **Goal:** Fix general logic (`src/api/decoded-display-rotation.ts:1` / `src/filters/gpu-video.ts:1` / `src/drivers/mp4/gapless.ts:1` / `src/dsp/resample.ts:1`), PASS ssim-psnr/property-invariant, then beat winner's wall by ≥10% via zero-copy. Variants: 5 as per instructions.
- [x] 7. `transcode/wav_to_mp3_mp4` — FAIL `TRANSCODE_AUDIO_PRESENTATION_COUNT_MISMATCH` — decoder emitted 239710 presentation frame(s); container declares 240000 — Winner: none (all FAIL) — **Goal:** Fix general logic (`src/api/decoded-display-rotation.ts:1` / `src/filters/gpu-video.ts:1` / `src/drivers/mp4/gapless.ts:1` / `src/dsp/resample.ts:1`), PASS ssim-psnr/property-invariant, then beat winner's wall by ≥10% via zero-copy. Variants: 5 as per instructions.
- [ ] 8. `remux/huge_h264_1080p_600s_mov_to_mp4` — NA_ENGINE `cached: [AIBRUSH_FRAMEWORK_CAPABILITY_MISS] aibrush-media@dev.remux: mp4 remux cannot preserve gener` — Winner: mediabunny/mp4box — **Goal:** Close coverage: support streaming remux (≤128MiB) or drop `tmcd` with typed warning + `RemuxOutputPlan` honest NA. Never mid-op ERROR. Learn from mp4box drop-unknown-trak but with bounded memory.
- [ ] 9. `remux/massive_h264_1080p_2h_mp4_to_mkv` — NA_ENGINE `cached: [AIBRUSH_FINITE_REMUX_OUTPUT_SIZE_UNSUPPORTED] finite remux publication for 1144401376 decla` — Winner: mediabunny/mp4box — **Goal:** Close coverage: support streaming remux (≤128MiB) or drop `tmcd` with typed warning + `RemuxOutputPlan` honest NA. Never mid-op ERROR. Learn from mp4box drop-unknown-trak but with bounded memory.
- [ ] 10. `trim/huge_h264_mov_copy_peakmem` — NA_ENGINE `cached: [AIBRUSH_FRAMEWORK_CAPABILITY_MISS] aibrush-media@dev.trim: mp4 remux cannot preserve generi` — Winner: mediabunny/mp4box — **Goal:** Close coverage: support streaming remux (≤128MiB) or drop `tmcd` with typed warning + `RemuxOutputPlan` honest NA. Never mid-op ERROR. Learn from mp4box drop-unknown-trak but with bounded memory.
- [ ] 11. `mux/mp4_streaming_target` — LOSE vs `mediabunny@1.48.0` ratio 275.64x ab 5788.4ms vs winner 21.0ms — family `mux` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 12. `encryption/perf_cenc_ctr_decrypt_throughput` — LOSE vs `ffmpeg.wasm@0.12.15` ratio 181.44x ab 16057.5ms vs winner 88.5ms — family `encryption` — **Goal:** Beat `ffmpeg.wasm@0.12.15` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from ffmpeg.wasm@0.12.15 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 13. `performance/metamorphic-transcode-idempotent-source-res` — LOSE vs `mediabunny@1.48.0` ratio 92.94x ab 5111.6ms vs winner 55.0ms — family `performance` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 14. `demux/empty_audio_zero_packets` — LOSE vs `mediabunny@1.48.0` ratio 85.89x ab 3.9ms vs winner 0.0ms — family `demux` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 15. `mux/mp4_faststart_reserve` — LOSE vs `mediabunny@1.48.0` ratio 61.27x ab 1164.2ms vs winner 19.0ms — family `mux` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 16. `decode-seek/decode_h264_4k` — LOSE vs `remotion@4.0.479` ratio 51.32x ab 1148.0ms vs winner 22.4ms — family `decode-seek` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 17. `decode-seek/decode_av1` — LOSE vs `remotion@4.0.479` ratio 39.94x ab 1660.4ms vs winner 41.6ms — family `decode-seek` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 18. `probe/hls_aes128_playlist_key_free` — LOSE vs `mediabunny@1.48.0` ratio 38.78x ab 1.7ms vs winner 0.0ms — family `probe` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 19. `remux/h264_1080p_5s_mov_to_mp4` — LOSE vs `mp4box@2.3.0` ratio 37.86x ab 37.9ms vs winner 1.0ms — family `remux` — **Goal:** Beat `mp4box@2.3.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mp4box@2.3.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 20. `decode-seek/decode_bframes_reorder` — LOSE vs `remotion@4.0.479` ratio 36.57x ab 755.8ms vs winner 20.7ms — family `decode-seek` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 21. `encryption/unencrypted_left_untouched_noop` — LOSE vs `ffmpeg.wasm@0.12.15` ratio 33.57x ab 115146.3ms vs winner 3430.0ms — family `encryption` — **Goal:** Beat `ffmpeg.wasm@0.12.15` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from ffmpeg.wasm@0.12.15 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 22. `mux/prop_vfr_mux_duration_mp4_to_mp4` — LOSE vs `mp4box@2.3.0` ratio 31.92x ab 31.9ms vs winner 1.0ms — family `mux` — **Goal:** Beat `mp4box@2.3.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mp4box@2.3.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 23. `mux/drop_audio_track_subset_to_mp4` — LOSE vs `mp4box@2.3.0` ratio 28.63x ab 28.6ms vs winner 1.0ms — family `mux` — **Goal:** Beat `mp4box@2.3.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mp4box@2.3.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 24. `performance/convert-peak-memory` — LOSE vs `remotion@4.0.479` ratio 28.44x ab 625.7ms vs winner 22.0ms — family `performance` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 25. `decode-seek/decode_vp9_alpha` — LOSE vs `mediabunny@1.48.0` ratio 28.25x ab 113.0ms vs winner 4.0ms — family `decode-seek` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 26. `probe/wav_s16` — LOSE vs `mediabunny@1.48.0` ratio 24.25x ab 1.0ms vs winner 0.0ms — family `probe` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 27. `decode-seek/decode_hevc` — LOSE vs `remotion@4.0.479` ratio 20.73x ab 296.9ms vs winner 14.3ms — family `decode-seek` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 28. `decode-seek/decode_mov_h264` — LOSE vs `remotion@4.0.479` ratio 19.47x ab 623.0ms vs winner 32.0ms — family `decode-seek` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 29. `probe/wav_s24` — LOSE vs `mediabunny@1.48.0` ratio 17.70x ab 0.9ms vs winner 0.0ms — family `probe` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 30. `metadata/read_no_tags_wav` — LOSE vs `mediabunny@1.48.0` ratio 17.00x ab 0.3ms vs winner 0.0ms — family `metadata` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 31. `probe/mp3_xing` — LOSE vs `mediabunny@1.48.0` ratio 16.86x ab 0.6ms vs winner 0.0ms — family `probe` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 32. `streaming-output/buffer_massive_h264_mp4` — LOSE vs `mediabunny@1.48.0` ratio 16.00x ab 16.0ms vs winner 1.0ms — family `streaming-output` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 33. `encryption/cenc_ctr_decrypt_eq_cleartext` — LOSE vs `ffmpeg.wasm@0.12.15` ratio 15.27x ab 28013.9ms vs winner 1835.0ms — family `encryption` — **Goal:** Beat `ffmpeg.wasm@0.12.15` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from ffmpeg.wasm@0.12.15 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 34. `probe/flac_seektable` — LOSE vs `mediabunny@1.48.0` ratio 14.50x ab 1.3ms vs winner 0.1ms — family `probe` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 35. `encryption/cenc_ctr_decrypt` — LOSE vs `ffmpeg.wasm@0.12.15` ratio 13.99x ab 26675.4ms vs winner 1907.0ms — family `encryption` — **Goal:** Beat `ffmpeg.wasm@0.12.15` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from ffmpeg.wasm@0.12.15 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 36. `mux/prop_h264_mux_duration_mp4_to_ts` — LOSE vs `mediabunny@1.48.0` ratio 13.34x ab 66.7ms vs winner 5.0ms — family `mux` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 37. `probe/realworld_mdn_trex_mp3` — LOSE vs `mediabunny@1.48.0` ratio 13.10x ab 0.7ms vs winner 0.0ms — family `probe` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 38. `probe/empty-audio-wav` — LOSE vs `mediabunny@1.48.0` ratio 12.38x ab 1.3ms vs winner 0.1ms — family `probe` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 39. `decode-seek/decode_h264_10bit` — LOSE vs `remotion@4.0.479` ratio 12.36x ab 396.0ms vs winner 32.0ms — family `decode-seek` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 40. `streaming-output/prop_probe_dur_fragmented_shape` — LOSE vs `mediabunny@1.48.0` ratio 12.33x ab 111.0ms vs winner 9.0ms — family `streaming-output` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 41. `demux/flac_noseektable` — LOSE vs `mediabunny@1.48.0` ratio 12.14x ab 19.7ms vs winner 1.6ms — family `demux` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 42. `transcode/ladder_tiny_h264_360p_resize_180p` — LOSE vs `mediabunny@1.48.0` ratio 12.00x ab 12.0ms vs winner 1.0ms — family `transcode` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 43. `decode-seek/decode_vfr_timing` — LOSE vs `remotion@4.0.479` ratio 11.39x ab 299.2ms vs winner 26.3ms — family `decode-seek` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 44. `decode-seek/decode_multitrack_select_video` — LOSE vs `remotion@4.0.479` ratio 11.32x ab 173.9ms vs winner 15.4ms — family `decode-seek` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 45. `audio-dsp/edge_longform_audio_resample_16k` — LOSE vs `ffmpeg.wasm@0.12.15` ratio 11.05x ab 766.0ms vs winner 69.3ms — family `audio-dsp` — **Goal:** Beat `ffmpeg.wasm@0.12.15` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from ffmpeg.wasm@0.12.15 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 46. `probe/wav_f32` — LOSE vs `mediabunny@1.48.0` ratio 10.97x ab 1.8ms vs winner 0.2ms — family `probe` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 47. `demux/vp9_alpha` — LOSE vs `mediabunny@1.48.0` ratio 10.74x ab 13.3ms vs winner 1.2ms — family `demux` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 48. `decode-seek/decode_size_large_h264_120s` — LOSE vs `ffmpeg.wasm@0.12.15` ratio 10.58x ab 671.0ms vs winner 63.4ms — family `decode-seek` — **Goal:** Beat `ffmpeg.wasm@0.12.15` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from ffmpeg.wasm@0.12.15 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 49. `decode-seek/decode_size_large_vp9_120s` — LOSE vs `ffmpeg.wasm@0.12.15` ratio 10.15x ab 649.1ms vs winner 64.0ms — family `decode-seek` — **Goal:** Beat `ffmpeg.wasm@0.12.15` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from ffmpeg.wasm@0.12.15 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 50. `probe/mp3_cbr_notoc` — LOSE vs `mediabunny@1.48.0` ratio 10.09x ab 0.6ms vs winner 0.1ms — family `probe` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 51. `probe/wav_5_1` — LOSE vs `mediabunny@1.48.0` ratio 9.93x ab 2.1ms vs winner 0.2ms — family `probe` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 52. `mux/edge_rotation_decode_mux_mov` — LOSE vs `mediabunny@1.48.0` ratio 9.87x ab 32081.0ms vs winner 3250.0ms — family `mux` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 53. `encryption/hls_aes128_decrypt_eq_cleartext` — LOSE vs `ffmpeg.wasm@0.12.15` ratio 9.57x ab 26742.0ms vs winner 2793.0ms — family `encryption` — **Goal:** Beat `ffmpeg.wasm@0.12.15` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from ffmpeg.wasm@0.12.15 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 54. `decode-seek/decode_mkv_h264` — LOSE vs `remotion@4.0.479` ratio 8.75x ab 328.0ms vs winner 37.5ms — family `decode-seek` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 55. `decode-seek/decode_extreme_fps_240` — LOSE vs `remotion@4.0.479` ratio 8.11x ab 107.9ms vs winner 13.3ms — family `decode-seek` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 56. `encryption/hls_aes128_decrypt` — LOSE vs `ffmpeg.wasm@0.12.15` ratio 7.85x ab 22554.2ms vs winner 2873.0ms — family `encryption` — **Goal:** Beat `ffmpeg.wasm@0.12.15` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from ffmpeg.wasm@0.12.15 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 57. `metadata/read_mp3_xing` — LOSE vs `mediabunny@1.48.0` ratio 7.80x ab 0.2ms vs winner 0.0ms — family `metadata` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 58. `mux/prop_av1_mux_duration_webm_to_mp4` — LOSE vs `mediabunny@1.48.0` ratio 7.57x ab 30.3ms vs winner 4.0ms — family `mux` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 59. `decode-seek/seek_vp9_keyframe` — LOSE vs `mediabunny@1.48.0` ratio 7.38x ab 155.6ms vs winner 21.1ms — family `decode-seek` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 60. `metadata/read_flac_seektable` — LOSE vs `mediabunny@1.48.0` ratio 7.33x ab 0.2ms vs winner 0.0ms — family `metadata` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 61. `decode-seek/decode_open_gop_first_frame` — LOSE vs `remotion@4.0.479` ratio 7.15x ab 176.2ms vs winner 24.7ms — family `decode-seek` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 62. `demux/flac_seektable` — LOSE vs `mediabunny@1.48.0` ratio 6.71x ab 10.0ms vs winner 1.5ms — family `demux` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 63. `probe/flac_noseektable` — LOSE vs `mediabunny@1.48.0` ratio 6.36x ab 1.2ms vs winner 0.2ms — family `probe` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 64. `probe/micro_h264_1frame` — LOSE vs `mediabunny@1.48.0` ratio 6.06x ab 1.5ms vs winner 0.2ms — family `probe` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 65. `mux/av1_opus_to_mp4` — LOSE vs `mediabunny@1.48.0` ratio 5.79x ab 127.4ms vs winner 22.0ms — family `mux` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 66. `decode-seek/seek_mkv_h264_keyframe` — LOSE vs `mediabunny@1.48.0` ratio 5.72x ab 80.3ms vs winner 14.0ms — family `decode-seek` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 67. `probe/micro_audio_short` — LOSE vs `mediabunny@1.48.0` ratio 5.65x ab 1.4ms vs winner 0.2ms — family `probe` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 68. `remux/large_vp9_1080p_120s_webm_to_mkv` — LOSE vs `ffmpeg.wasm@0.12.15` ratio 5.60x ab 1125.2ms vs winner 201.0ms — family `remux` — **Goal:** Beat `ffmpeg.wasm@0.12.15` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from ffmpeg.wasm@0.12.15 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 69. `remux/prop_mp3_to_mp4_duration_invariant` — LOSE vs `mediabunny@1.48.0` ratio 5.22x ab 10.4ms vs winner 2.0ms — family `remux` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 70. `decode-seek/decode_vp9` — LOSE vs `remotion@4.0.479` ratio 5.22x ab 215.7ms vs winner 41.3ms — family `decode-seek` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 71. `audio-dsp/edge_longform_audio_probe` — LOSE vs `mediabunny@1.48.0` ratio 5.18x ab 4.0ms vs winner 0.8ms — family `audio-dsp` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 72. `streaming-output/prop_faststart_reserve_duration_invariant` — LOSE vs `mediabunny@1.48.0` ratio 4.93x ab 74.0ms vs winner 15.0ms — family `streaming-output` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 73. `probe/h264_1fps_30s` — LOSE vs `mediabunny@1.48.0` ratio 4.90x ab 1.4ms vs winner 0.3ms — family `probe` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 74. `decode-seek/decode_h264_first_frames` — LOSE vs `remotion@4.0.479` ratio 4.89x ab 134.5ms vs winner 27.5ms — family `decode-seek` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 75. `decode-seek/decode_size_tiny_vp9_360p` — LOSE vs `remotion@4.0.479` ratio 4.54x ab 73.2ms vs winner 16.1ms — family `decode-seek` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 76. `robustness/edge_zero_length_probe` — LOSE vs `mp4box@2.3.0` ratio 4.50x ab 35.0ms vs winner 7.8ms — family `robustness` — **Goal:** Beat `mp4box@2.3.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mp4box@2.3.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 77. `audio-dsp/edge_gapless_aac_decode` — LOSE vs `mediabunny@1.48.0` ratio 4.47x ab 1.9ms vs winner 0.4ms — family `audio-dsp` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 78. `decode-seek/decode_vp8` — LOSE vs `remotion@4.0.479` ratio 4.37x ab 119.9ms vs winner 27.4ms — family `decode-seek` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 79. `remux/large_h264_1080p_120s_mp4_to_mkv` — LOSE vs `ffmpeg.wasm@0.12.15` ratio 4.35x ab 421.7ms vs winner 97.0ms — family `remux` — **Goal:** Beat `ffmpeg.wasm@0.12.15` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from ffmpeg.wasm@0.12.15 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 80. `probe/tiny_h264_360p_2s` — LOSE vs `mediabunny@1.48.0` ratio 4.26x ab 0.6ms vs winner 0.1ms — family `probe` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 81. `decode-seek/seek_zero` — LOSE vs `mediabunny@1.48.0` ratio 4.04x ab 83.9ms vs winner 20.7ms — family `decode-seek` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 82. `robustness/edge_dims_2x2_h264_probe` — LOSE vs `mp4box@2.3.0` ratio 4.00x ab 43.6ms vs winner 10.9ms — family `robustness` — **Goal:** Beat `mp4box@2.3.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mp4box@2.3.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 83. `mux/h264_aac_to_mp4` — LOSE vs `mp4box@2.3.0` ratio 4.00x ab 20.0ms vs winner 5.0ms — family `mux` — **Goal:** Beat `mp4box@2.3.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mp4box@2.3.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 84. `mux/prop_h264_mux_duration_mp4_to_mkv` — LOSE vs `mediabunny@1.48.0` ratio 3.97x ab 27.8ms vs winner 7.0ms — family `mux` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 85. `robustness/fuzz_truncated_h264_asset_demux` — LOSE vs `mp4box@2.3.0` ratio 3.96x ab 34.6ms vs winner 8.7ms — family `robustness` — **Goal:** Beat `mp4box@2.3.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mp4box@2.3.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 86. `probe/h264_rotated90` — LOSE vs `mediabunny@1.48.0` ratio 3.96x ab 5.0ms vs winner 1.3ms — family `probe` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 87. `audio-dsp/upmix_mono_to_stereo` — LOSE vs `mediabunny@1.48.0` ratio 3.94x ab 39.7ms vs winner 10.1ms — family `audio-dsp` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 88. `robustness/edge_extreme_fps_1_probe` — LOSE vs `mp4box@2.3.0` ratio 3.91x ab 41.6ms vs winner 10.6ms — family `robustness` — **Goal:** Beat `mp4box@2.3.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mp4box@2.3.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 89. `robustness/edge_extreme_fps_240_probe` — LOSE vs `mp4box@2.3.0` ratio 3.90x ab 40.5ms vs winner 10.4ms — family `robustness` — **Goal:** Beat `mp4box@2.3.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mp4box@2.3.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 90. `robustness/edge_video_only_micro_probe` — LOSE vs `mp4box@2.3.0` ratio 3.87x ab 40.1ms vs winner 10.4ms — family `robustness` — **Goal:** Beat `mp4box@2.3.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mp4box@2.3.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 91. `probe/truncated-header-graceful` — LOSE vs `mp4box@2.3.0` ratio 3.79x ab 114.7ms vs winner 30.2ms — family `probe` — **Goal:** Beat `mp4box@2.3.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mp4box@2.3.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 92. `decode-seek/seek_vp8_keyframe` — LOSE vs `mediabunny@1.48.0` ratio 3.79x ab 93.0ms vs winner 24.6ms — family `decode-seek` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 93. `demux/size_micro_micro_h264_1frame` — LOSE vs `mediabunny@1.48.0` ratio 3.75x ab 0.3ms vs winner 0.1ms — family `demux` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 94. `decode-seek/decode_size_tiny_h264_360p` — LOSE vs `remotion@4.0.479` ratio 3.73x ab 74.5ms vs winner 20.0ms — family `decode-seek` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 95. `robustness/fuzz_mp4_header_truncated_demux` — LOSE vs `mp4box@2.3.0` ratio 3.70x ab 34.8ms vs winner 9.4ms — family `robustness` — **Goal:** Beat `mp4box@2.3.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mp4box@2.3.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 96. `demux/h264_ts` — LOSE vs `mediabunny@1.48.0` ratio 3.68x ab 236.9ms vs winner 64.4ms — family `demux` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 97. `demux/graceful_truncated_h264` — LOSE vs `mp4box@2.3.0` ratio 3.61x ab 102.5ms vs winner 28.4ms — family `demux` — **Goal:** Beat `mp4box@2.3.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mp4box@2.3.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 98. `decode-seek/seek_repeated_same_target` — LOSE vs `mediabunny@1.48.0` ratio 3.55x ab 141.1ms vs winner 39.8ms — family `decode-seek` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 99. `robustness/edge_gapless_priming_probe` — LOSE vs `mp4box@2.3.0` ratio 3.54x ab 39.6ms vs winner 11.2ms — family `robustness` — **Goal:** Beat `mp4box@2.3.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mp4box@2.3.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 100. `remux/h264_ts_ts_to_mkv` — LOSE vs `ffmpeg.wasm@0.12.15` ratio 3.52x ab 341.7ms vs winner 97.0ms — family `remux` — **Goal:** Beat `ffmpeg.wasm@0.12.15` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from ffmpeg.wasm@0.12.15 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 101. `robustness/edge_audio_only_probe` — LOSE vs `mp4box@2.3.0` ratio 3.51x ab 40.5ms vs winner 11.6ms — family `robustness` — **Goal:** Beat `mp4box@2.3.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mp4box@2.3.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 102. `robustness/edge_video_only_probe` — LOSE vs `mp4box@2.3.0` ratio 3.50x ab 40.6ms vs winner 11.6ms — family `robustness` — **Goal:** Beat `mp4box@2.3.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mp4box@2.3.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 103. `performance/seek-ms` — LOSE vs `mediabunny@1.48.0` ratio 3.47x ab 78.4ms vs winner 22.6ms — family `performance` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 104. `probe/mislabeled_h264_content` — LOSE vs `mediabunny@1.48.0` ratio 3.37x ab 2.4ms vs winner 0.7ms — family `probe` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 105. `robustness/edge_audio_only_micro_probe` — LOSE vs `mp4box@2.3.0` ratio 3.32x ab 44.3ms vs winner 13.3ms — family `robustness` — **Goal:** Beat `mp4box@2.3.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mp4box@2.3.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 106. `robustness/edge_vfr_probe` — LOSE vs `mp4box@2.3.0` ratio 3.28x ab 42.6ms vs winner 13.0ms — family `robustness` — **Goal:** Beat `mp4box@2.3.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mp4box@2.3.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 107. `demux/h264_in_mkv` — LOSE vs `mediabunny@1.48.0` ratio 3.26x ab 104.0ms vs winner 31.9ms — family `demux` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 108. `transcode/h264_to_fragmented_mp4` — LOSE vs `mediabunny@1.48.0` ratio 3.21x ab 61.0ms vs winner 19.0ms — family `transcode` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 109. `remux/flac_seektable_flac_to_mkv` — LOSE vs `mediabunny@1.48.0` ratio 3.10x ab 9.3ms vs winner 3.0ms — family `remux` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 110. `audio-dsp/pcm_s24_to_f32` — LOSE vs `mediabunny@1.48.0` ratio 3.07x ab 40.8ms vs winner 13.3ms — family `audio-dsp` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 111. `probe/recorder_headerless` — LOSE vs `mediabunny@1.48.0` ratio 3.02x ab 1.5ms vs winner 0.5ms — family `probe` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 112. `probe/tiny_vp9_360p_2s` — LOSE vs `mediabunny@1.48.0` ratio 2.98x ab 0.7ms vs winner 0.2ms — family `probe` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 113. `decode-seek/seek_av1_keyframe` — LOSE vs `mediabunny@1.48.0` ratio 2.93x ab 41.5ms vs winner 14.2ms — family `decode-seek` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 114. `probe/longform_1h_audio` — LOSE vs `mp4box@2.3.0` ratio 2.91x ab 9.9ms vs winner 3.4ms — family `probe` — **Goal:** Beat `mp4box@2.3.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mp4box@2.3.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 115. `probe/opus` — LOSE vs `mediabunny@1.48.0` ratio 2.88x ab 0.7ms vs winner 0.3ms — family `probe` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 116. `probe/video_1x1` — LOSE vs `mediabunny@1.48.0` ratio 2.84x ab 1.6ms vs winner 0.6ms — family `probe` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 117. `demux/graceful_zero_length` — LOSE vs `mp4box@2.3.0` ratio 2.83x ab 96.3ms vs winner 34.0ms — family `demux` — **Goal:** Beat `mp4box@2.3.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mp4box@2.3.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 118. `audio-dsp/fade_in_out_f32` — LOSE vs `mediabunny@1.48.0` ratio 2.60x ab 26.3ms vs winner 10.1ms — family `audio-dsp` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 119. `decode-seek/meta_pts_monotonic_after_reorder` — LOSE vs `ffmpeg.wasm@0.12.15` ratio 2.57x ab 116.7ms vs winner 45.4ms — family `decode-seek` — **Goal:** Beat `ffmpeg.wasm@0.12.15` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from ffmpeg.wasm@0.12.15 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 120. `decode-seek/meta_seek_vs_linear_decode` — LOSE vs `mediabunny@1.48.0` ratio 2.54x ab 113.4ms vs winner 44.7ms — family `decode-seek` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 121. `demux/av1_720p_5s` — LOSE vs `ffmpeg.wasm@0.12.15` ratio 2.49x ab 240.0ms vs winner 96.4ms — family `demux` — **Goal:** Beat `ffmpeg.wasm@0.12.15` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from ffmpeg.wasm@0.12.15 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 122. `decode-seek/seek_negative` — LOSE vs `mediabunny@1.48.0` ratio 2.44x ab 51.7ms vs winner 21.2ms — family `decode-seek` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 123. `decode-seek/seek_backward_then_forward` — LOSE vs `mediabunny@1.48.0` ratio 2.43x ab 99.0ms vs winner 40.8ms — family `decode-seek` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 124. `mux/flac_to_mkv_audio` — LOSE vs `mediabunny@1.48.0` ratio 2.41x ab 7.2ms vs winner 3.0ms — family `mux` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 125. `demux/aac_adts` — LOSE vs `ffmpeg.wasm@0.12.15` ratio 2.39x ab 41.4ms vs winner 17.3ms — family `demux` — **Goal:** Beat `ffmpeg.wasm@0.12.15` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from ffmpeg.wasm@0.12.15 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 126. `robustness/fuzz_mp4_bitflip_probe` — LOSE vs `mp4box@2.3.0` ratio 2.39x ab 50.9ms vs winner 21.4ms — family `robustness` — **Goal:** Beat `mp4box@2.3.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mp4box@2.3.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 127. `decode-seek/seek_h264_keyframe` — LOSE vs `mediabunny@1.48.0` ratio 2.37x ab 50.2ms vs winner 21.2ms — family `decode-seek` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 128. `probe/vp9_alpha` — LOSE vs `mediabunny@1.48.0` ratio 2.31x ab 1.6ms vs winner 0.7ms — family `probe` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 129. `transcode/h264_resize_720p` — LOSE vs `remotion@4.0.479` ratio 2.30x ab 23.0ms vs winner 10.0ms — family `transcode` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 130. `mux/prop_vp9_decode_mux_webm_to_webm` — LOSE vs `mediabunny@1.48.0` ratio 2.29x ab 55.0ms vs winner 24.0ms — family `mux` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 131. `probe/h264_vfr` — LOSE vs `mediabunny@1.48.0` ratio 2.28x ab 6.6ms vs winner 2.9ms — family `probe` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 132. `metadata/neg_garbled_ilst_mp4_probe` — LOSE vs `mp4box@2.3.0` ratio 2.27x ab 52.7ms vs winner 23.2ms — family `metadata` — **Goal:** Beat `mp4box@2.3.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mp4box@2.3.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 133. `demux/vp8_720p_10s` — LOSE vs `ffmpeg.wasm@0.12.15` ratio 2.25x ab 23.3ms vs winner 10.4ms — family `demux` — **Goal:** Beat `ffmpeg.wasm@0.12.15` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from ffmpeg.wasm@0.12.15 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 134. `transcode/malformed_zero_length_transcode` — LOSE vs `remotion@4.0.479` ratio 2.20x ab 35.3ms vs winner 16.0ms — family `transcode` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 135. `robustness/edge_flac_with_seektable_probe` — LOSE vs `remotion@4.0.479` ratio 2.17x ab 41.4ms vs winner 19.1ms — family `robustness` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 136. `demux/size_micro_micro_audio_short` — LOSE vs `mediabunny@1.48.0` ratio 2.12x ab 0.3ms vs winner 0.1ms — family `demux` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 137. `robustness/fuzz_flac_bitflip_probe` — LOSE vs `remotion@4.0.479` ratio 2.10x ab 38.4ms vs winner 18.3ms — family `robustness` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 138. `probe/metamorphic-recorder-headerless-sane-duration` — LOSE vs `mediabunny@1.48.0` ratio 2.06x ab 2.9ms vs winner 1.4ms — family `probe` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 139. `robustness/edge_flac_without_seektable_probe` — LOSE vs `remotion@4.0.479` ratio 2.03x ab 38.2ms vs winner 18.8ms — family `robustness` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 140. `audio-dsp/resample_44k1_to_48k` — LOSE vs `mediabunny@1.48.0` ratio 2.02x ab 20.2ms vs winner 10.0ms — family `audio-dsp` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 141. `metadata/neg_garbled_id3_mp3_probe` — LOSE vs `remotion@4.0.479` ratio 1.98x ab 42.0ms vs winner 21.2ms — family `metadata` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 142. `streaming-output/prop_probe_dur_buffer_shape` — LOSE vs `mediabunny@1.48.0` ratio 1.94x ab 35.0ms vs winner 18.0ms — family `streaming-output` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 143. `demux/size_tiny_tiny_vp9_360p_2s` — LOSE vs `mediabunny@1.48.0` ratio 1.94x ab 2.4ms vs winner 1.2ms — family `demux` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 144. `robustness/fuzz_mp3_header_truncated_probe` — LOSE vs `remotion@4.0.479` ratio 1.91x ab 35.1ms vs winner 18.4ms — family `robustness` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 145. `robustness/fuzz_webm_header_truncated_demux` — LOSE vs `remotion@4.0.479` ratio 1.90x ab 37.7ms vs winner 19.8ms — family `robustness` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 146. `trim/large_h264_frame_accurate_throughput` — LOSE vs `mediabunny@1.48.0` ratio 1.86x ab 69.0ms vs winner 37.0ms — family `trim` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 147. `audio-dsp/resample_48k_to_16k` — LOSE vs `mediabunny@1.48.0` ratio 1.86x ab 17.1ms vs winner 9.2ms — family `audio-dsp` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 148. `decode-seek/seek_past_eof` — LOSE vs `mediabunny@1.48.0` ratio 1.82x ab 74.8ms vs winner 41.0ms — family `decode-seek` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 149. `transcode/roundtrip_leg1_h264_to_vp9` — LOSE vs `mediabunny@1.48.0` ratio 1.82x ab 40.0ms vs winner 22.0ms — family `transcode` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 150. `metadata/read_opus` — LOSE vs `mediabunny@1.48.0` ratio 1.81x ab 0.2ms vs winner 0.1ms — family `metadata` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 151. `mux/neg_zero_tracks_empty_audio_to_mp4` — LOSE vs `mediabunny@1.48.0` ratio 1.81x ab 93.5ms vs winner 51.7ms — family `mux` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 152. `robustness/edge_mislabeled_container_probe` — LOSE vs `remotion@4.0.479` ratio 1.80x ab 37.9ms vs winner 21.1ms — family `robustness` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 153. `audio-dsp/pcm_s16_to_f32` — LOSE vs `mediabunny@1.48.0` ratio 1.78x ab 40.4ms vs winner 22.7ms — family `audio-dsp` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 154. `transcode/vp9_to_h264_mp4` — LOSE vs `remotion@4.0.479` ratio 1.75x ab 21.0ms vs winner 12.0ms — family `transcode` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 155. `remux/vp9_1080p_10s_webm_to_webm` — LOSE vs `remotion@4.0.479` ratio 1.75x ab 38.4ms vs winner 22.0ms — family `remux` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 156. `audio-dsp/resample_48k_to_44k1` — LOSE vs `mediabunny@1.48.0` ratio 1.73x ab 17.0ms vs winner 9.8ms — family `audio-dsp` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 157. `decode-seek/seek_h264_nonkeyframe` — LOSE vs `mediabunny@1.48.0` ratio 1.72x ab 60.4ms vs winner 35.0ms — family `decode-seek` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 158. `robustness/fuzz_encrypted_mp4_ciphertext_decode` — LOSE vs `mediabunny@1.48.0` ratio 1.72x ab 41.2ms vs winner 24.0ms — family `robustness` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 159. `demux/vp9_1080p_10s` — LOSE vs `mediabunny@1.48.0` ratio 1.69x ab 206.3ms vs winner 122.0ms — family `demux` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 160. `probe/ts_discontinuity` — LOSE vs `mediabunny@1.48.0` ratio 1.63x ab 9.4ms vs winner 5.8ms — family `probe` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 161. `audio-dsp/gain_half_f32` — LOSE vs `mediabunny@1.48.0` ratio 1.63x ab 19.0ms vs winner 11.6ms — family `audio-dsp` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 162. `performance/size-ladder-iterate-packets-medium` — LOSE vs `web-demuxer@4.0.0` ratio 1.62x ab 8.7ms vs winner 5.3ms — family `performance` — **Goal:** Beat `web-demuxer@4.0.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from web-demuxer@4.0.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 163. `mux/prop_h264_decode_mux_mp4_to_mp4` — LOSE vs `mediabunny@1.48.0` ratio 1.60x ab 33.6ms vs winner 21.0ms — family `mux` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 164. `decode-seek/decode_extreme_fps_1` — LOSE vs `remotion@4.0.479` ratio 1.57x ab 32.0ms vs winner 20.4ms — family `decode-seek` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 165. `robustness/edge_seek_negative` — LOSE vs `mediabunny@1.48.0` ratio 1.56x ab 92.1ms vs winner 59.0ms — family `robustness` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 166. `decode-seek/decode_size_micro_h264_1frame` — LOSE vs `mediabunny@1.48.0` ratio 1.55x ab 4.9ms vs winner 3.1ms — family `decode-seek` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 167. `encryption/cenc_ctr_missing_key_negative` — LOSE vs `mediabunny@1.48.0` ratio 1.53x ab 43.3ms vs winner 28.3ms — family `encryption` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 168. `robustness/fuzz_ogg_opus_header_truncated_probe` — LOSE vs `mediabunny@1.48.0` ratio 1.53x ab 34.4ms vs winner 22.5ms — family `robustness` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 169. `robustness/fuzz_adts_aac_bitflip_probe` — LOSE vs `remotion@4.0.479` ratio 1.53x ab 33.1ms vs winner 21.7ms — family `robustness` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 170. `robustness/edge_headerless_recorder_probe` — LOSE vs `remotion@4.0.479` ratio 1.51x ab 38.4ms vs winner 25.4ms — family `robustness` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 171. `audio-dsp/gain_minus6db_s16` — LOSE vs `mediabunny@1.48.0` ratio 1.51x ab 19.1ms vs winner 12.7ms — family `audio-dsp` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 172. `decode-seek/seek_vfr_arbitrary` — LOSE vs `mediabunny@1.48.0` ratio 1.51x ab 37.9ms vs winner 25.2ms — family `decode-seek` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 173. `robustness/edge_seek_past_eof` — LOSE vs `mediabunny@1.48.0` ratio 1.50x ab 118.4ms vs winner 78.8ms — family `robustness` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 174. `transcode/hevc_to_h264_mp4` — LOSE vs `remotion@4.0.479` ratio 1.50x ab 21.0ms vs winner 14.0ms — family `transcode` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 175. `robustness/fuzz_ts_zeroed_spans_demux` — LOSE vs `mediabunny@1.48.0` ratio 1.50x ab 49.6ms vs winner 33.2ms — family `robustness` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 176. `audio-dsp/downmix_stereo_to_mono` — LOSE vs `mediabunny@1.48.0` ratio 1.49x ab 18.4ms vs winner 12.3ms — family `audio-dsp` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 177. `encryption/hls_sample_aes_requested_as_aes128_negative` — LOSE vs `mediabunny@1.48.0` ratio 1.49x ab 34.1ms vs winner 22.9ms — family `encryption` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 178. `robustness/edge_longform_probe` — LOSE vs `remotion@4.0.479` ratio 1.48x ab 49.4ms vs winner 33.3ms — family `robustness` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 179. `encryption/hls_aes128_wrong_iv_negative` — LOSE vs `mediabunny@1.48.0` ratio 1.48x ab 36.4ms vs winner 24.6ms — family `encryption` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 180. `demux/realworld_mdn_flower_webm` — LOSE vs `ffmpeg.wasm@0.12.15` ratio 1.47x ab 19.8ms vs winner 13.5ms — family `demux` — **Goal:** Beat `ffmpeg.wasm@0.12.15` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from ffmpeg.wasm@0.12.15 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 181. `robustness/edge_dims_1x1_probe` — LOSE vs `mediabunny@1.48.0` ratio 1.44x ab 37.4ms vs winner 25.9ms — family `robustness` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 182. `transcode/bframe_reorder_h264_to_h264` — LOSE vs `remotion@4.0.479` ratio 1.43x ab 20.0ms vs winner 14.0ms — family `transcode` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 183. `decode-seek/meta_vfr_seek_lands_on_true_pts` — LOSE vs `ffmpeg.wasm@0.12.15` ratio 1.42x ab 62.4ms vs winner 43.9ms — family `decode-seek` — **Goal:** Beat `ffmpeg.wasm@0.12.15` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from ffmpeg.wasm@0.12.15 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 184. `mux/neg_h264_into_ogg_illegal` — LOSE vs `mediabunny@1.48.0` ratio 1.39x ab 282.8ms vs winner 203.0ms — family `mux` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 185. `remux/h264_1080p_30s_mp4_to_ts` — LOSE vs `ffmpeg.wasm@0.12.15` ratio 1.36x ab 111.9ms vs winner 82.0ms — family `remux` — **Goal:** Beat `ffmpeg.wasm@0.12.15` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from ffmpeg.wasm@0.12.15 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 186. `probe/perf-extract-metadata-large` — LOSE vs `mediabunny@1.48.0` ratio 1.35x ab 14.9ms vs winner 11.0ms — family `probe` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 187. `robustness/edge_headerless_recorder_remux` — LOSE vs `remotion@4.0.479` ratio 1.33x ab 50.0ms vs winner 37.5ms — family `robustness` — **Goal:** Beat `remotion@4.0.479` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from remotion@4.0.479 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 188. `decode-seek/seek_hevc_keyframe` — LOSE vs `mediabunny@1.48.0` ratio 1.33x ab 28.0ms vs winner 21.0ms — family `decode-seek` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 189. `mux/neg_h264_into_wav_illegal` — LOSE vs `mediabunny@1.48.0` ratio 1.31x ab 348.0ms vs winner 264.9ms — family `mux` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.
- [ ] 190. `remux/prop_recorder_headerless_duration_materialized` — LOSE vs `mediabunny@1.48.0` ratio 1.31x ab 37.9ms vs winner 29.0ms — family `remux` — **Goal:** Beat `mediabunny@1.48.0` wall by ≥5% beyond tie band, p95 ≤ winner, geomean win. Learn from mediabunny@1.48.0 strategy but implement better: fewer copies, fused color/resize, zero-copy, or better container fast-path. Add 5 variants.

## NEXT UP — one, by, one (top first)
1. P0 `decode-seek/decode_rotated_display_matrix` — FAIL
2. P0 `performance/convert-longtasks` — FAIL
3. P0 `performance/convert-webm-resize-320x180` — FAIL
4. P0 `transcode/aac_to_mp3_mp4` — FAIL
5. P0 `transcode/selfcheck_h264_resize_720p_tie` — FAIL
6. P0 `transcode/wav_to_flac` — FAIL
7. P0 `transcode/wav_to_mp3_mp4` — FAIL
8. P0 `remux/huge_h264_1080p_600s_mov_to_mp4` — NA
9. P0 `remux/massive_h264_1080p_2h_mp4_to_mkv` — NA
10. P0 `trim/huge_h264_mov_copy_peakmem` — NA
11. P0 `mux/mp4_streaming_target` — lose vs mediabunny@1.48.0
12. P0 `encryption/perf_cenc_ctr_decrypt_throughput` — lose vs ffmpeg.wasm@0.12.15
13. P0 `performance/metamorphic-transcode-idempotent-source-res` — lose vs mediabunny@1.48.0
14. P0 `demux/empty_audio_zero_packets` — lose vs mediabunny@1.48.0
15. P0 `mux/mp4_faststart_reserve` — lose vs mediabunny@1.48.0
16. P0 `decode-seek/decode_h264_4k` — lose vs remotion@4.0.479
17. P0 `decode-seek/decode_av1` — lose vs remotion@4.0.479
18. P0 `probe/hls_aes128_playlist_key_free` — lose vs mediabunny@1.48.0
19. P0 `remux/h264_1080p_5s_mov_to_mp4` — lose vs mp4box@2.3.0
20. P0 `decode-seek/decode_bframes_reorder` — lose vs remotion@4.0.479
21. P0 `encryption/unencrypted_left_untouched_noop` — lose vs ffmpeg.wasm@0.12.15
22. P0 `mux/prop_vfr_mux_duration_mp4_to_mp4` — lose vs mp4box@2.3.0
23. P0 `mux/drop_audio_track_subset_to_mp4` — lose vs mp4box@2.3.0
24. P0 `performance/convert-peak-memory` — lose vs remotion@4.0.479
25. P0 `decode-seek/decode_vp9_alpha` — lose vs mediabunny@1.48.0
26. P0 `probe/wav_s16` — lose vs mediabunny@1.48.0
27. P0 `decode-seek/decode_hevc` — lose vs remotion@4.0.479
28. P0 `decode-seek/decode_mov_h264` — lose vs remotion@4.0.479
29. P0 `probe/wav_s24` — lose vs mediabunny@1.48.0
30. P0 `metadata/read_no_tags_wav` — lose vs mediabunny@1.48.0
31. P0 `probe/mp3_xing` — lose vs mediabunny@1.48.0
32. P0 `streaming-output/buffer_massive_h264_mp4` — lose vs mediabunny@1.48.0
33. P0 `encryption/cenc_ctr_decrypt_eq_cleartext` — lose vs ffmpeg.wasm@0.12.15
34. P0 `probe/flac_seektable` — lose vs mediabunny@1.48.0
35. P0 `encryption/cenc_ctr_decrypt` — lose vs ffmpeg.wasm@0.12.15
36. P0 `mux/prop_h264_mux_duration_mp4_to_ts` — lose vs mediabunny@1.48.0
37. P0 `probe/realworld_mdn_trex_mp3` — lose vs mediabunny@1.48.0
38. P0 `probe/empty-audio-wav` — lose vs mediabunny@1.48.0
39. P0 `decode-seek/decode_h264_10bit` — lose vs remotion@4.0.479
40. P0 `streaming-output/prop_probe_dur_fragmented_shape` — lose vs mediabunny@1.48.0
41. P0 `demux/flac_noseektable` — lose vs mediabunny@1.48.0
42. P0 `transcode/ladder_tiny_h264_360p_resize_180p` — lose vs mediabunny@1.48.0
43. P0 `decode-seek/decode_vfr_timing` — lose vs remotion@4.0.479
44. P0 `decode-seek/decode_multitrack_select_video` — lose vs remotion@4.0.479
45. P0 `audio-dsp/edge_longform_audio_resample_16k` — lose vs ffmpeg.wasm@0.12.15
46. P0 `probe/wav_f32` — lose vs mediabunny@1.48.0
47. P0 `demux/vp9_alpha` — lose vs mediabunny@1.48.0
48. P0 `decode-seek/decode_size_large_h264_120s` — lose vs ffmpeg.wasm@0.12.15
49. P0 `decode-seek/decode_size_large_vp9_120s` — lose vs ffmpeg.wasm@0.12.15
50. P0 `probe/mp3_cbr_notoc` — lose vs mediabunny@1.48.0
51. P0 `probe/wav_5_1` — lose vs mediabunny@1.48.0
52. P0 `mux/edge_rotation_decode_mux_mov` — lose vs mediabunny@1.48.0
53. P0 `encryption/hls_aes128_decrypt_eq_cleartext` — lose vs ffmpeg.wasm@0.12.15
54. P0 `decode-seek/decode_mkv_h264` — lose vs remotion@4.0.479
55. P0 `decode-seek/decode_extreme_fps_240` — lose vs remotion@4.0.479
56. P0 `encryption/hls_aes128_decrypt` — lose vs ffmpeg.wasm@0.12.15
57. P0 `metadata/read_mp3_xing` — lose vs mediabunny@1.48.0
58. P0 `mux/prop_av1_mux_duration_webm_to_mp4` — lose vs mediabunny@1.48.0
59. P0 `decode-seek/seek_vp9_keyframe` — lose vs mediabunny@1.48.0
60. P0 `metadata/read_flac_seektable` — lose vs mediabunny@1.48.0
61. P0 `decode-seek/decode_open_gop_first_frame` — lose vs remotion@4.0.479
62. P0 `demux/flac_seektable` — lose vs mediabunny@1.48.0
63. P0 `probe/flac_noseektable` — lose vs mediabunny@1.48.0
64. P0 `probe/micro_h264_1frame` — lose vs mediabunny@1.48.0
65. P0 `mux/av1_opus_to_mp4` — lose vs mediabunny@1.48.0
66. P0 `decode-seek/seek_mkv_h264_keyframe` — lose vs mediabunny@1.48.0
67. P0 `probe/micro_audio_short` — lose vs mediabunny@1.48.0
68. P0 `remux/large_vp9_1080p_120s_webm_to_mkv` — lose vs ffmpeg.wasm@0.12.15
69. P0 `remux/prop_mp3_to_mp4_duration_invariant` — lose vs mediabunny@1.48.0
70. P0 `decode-seek/decode_vp9` — lose vs remotion@4.0.479
71. P0 `audio-dsp/edge_longform_audio_probe` — lose vs mediabunny@1.48.0
72. P0 `streaming-output/prop_faststart_reserve_duration_invariant` — lose vs mediabunny@1.48.0
73. P0 `probe/h264_1fps_30s` — lose vs mediabunny@1.48.0
74. P0 `decode-seek/decode_h264_first_frames` — lose vs remotion@4.0.479
75. P0 `decode-seek/decode_size_tiny_vp9_360p` — lose vs remotion@4.0.479
76. P0 `robustness/edge_zero_length_probe` — lose vs mp4box@2.3.0
77. P0 `audio-dsp/edge_gapless_aac_decode` — lose vs mediabunny@1.48.0
78. P0 `decode-seek/decode_vp8` — lose vs remotion@4.0.479
79. P0 `remux/large_h264_1080p_120s_mp4_to_mkv` — lose vs ffmpeg.wasm@0.12.15
80. P0 `probe/tiny_h264_360p_2s` — lose vs mediabunny@1.48.0
81. P0 `decode-seek/seek_zero` — lose vs mediabunny@1.48.0
82. P0 `robustness/edge_dims_2x2_h264_probe` — lose vs mp4box@2.3.0
83. P0 `mux/h264_aac_to_mp4` — lose vs mp4box@2.3.0
84. P0 `mux/prop_h264_mux_duration_mp4_to_mkv` — lose vs mediabunny@1.48.0
85. P0 `robustness/fuzz_truncated_h264_asset_demux` — lose vs mp4box@2.3.0
86. P0 `probe/h264_rotated90` — lose vs mediabunny@1.48.0
87. P0 `audio-dsp/upmix_mono_to_stereo` — lose vs mediabunny@1.48.0
88. P0 `robustness/edge_extreme_fps_1_probe` — lose vs mp4box@2.3.0
89. P0 `robustness/edge_extreme_fps_240_probe` — lose vs mp4box@2.3.0
90. P0 `robustness/edge_video_only_micro_probe` — lose vs mp4box@2.3.0
91. P0 `probe/truncated-header-graceful` — lose vs mp4box@2.3.0
92. P0 `decode-seek/seek_vp8_keyframe` — lose vs mediabunny@1.48.0
93. P0 `demux/size_micro_micro_h264_1frame` — lose vs mediabunny@1.48.0
94. P0 `decode-seek/decode_size_tiny_h264_360p` — lose vs remotion@4.0.479
95. P0 `robustness/fuzz_mp4_header_truncated_demux` — lose vs mp4box@2.3.0
96. P0 `demux/h264_ts` — lose vs mediabunny@1.48.0
97. P0 `demux/graceful_truncated_h264` — lose vs mp4box@2.3.0
98. P0 `decode-seek/seek_repeated_same_target` — lose vs mediabunny@1.48.0
99. P0 `robustness/edge_gapless_priming_probe` — lose vs mp4box@2.3.0
100. P0 `remux/h264_ts_ts_to_mkv` — lose vs ffmpeg.wasm@0.12.15
101. P0 `robustness/edge_audio_only_probe` — lose vs mp4box@2.3.0
102. P0 `robustness/edge_video_only_probe` — lose vs mp4box@2.3.0
103. P0 `performance/seek-ms` — lose vs mediabunny@1.48.0
104. P0 `probe/mislabeled_h264_content` — lose vs mediabunny@1.48.0
105. P0 `robustness/edge_audio_only_micro_probe` — lose vs mp4box@2.3.0
106. P0 `robustness/edge_vfr_probe` — lose vs mp4box@2.3.0
107. P0 `demux/h264_in_mkv` — lose vs mediabunny@1.48.0
108. P0 `transcode/h264_to_fragmented_mp4` — lose vs mediabunny@1.48.0
109. P0 `remux/flac_seektable_flac_to_mkv` — lose vs mediabunny@1.48.0
110. P0 `audio-dsp/pcm_s24_to_f32` — lose vs mediabunny@1.48.0
111. P0 `probe/recorder_headerless` — lose vs mediabunny@1.48.0
112. P0 `probe/tiny_vp9_360p_2s` — lose vs mediabunny@1.48.0
113. P0 `decode-seek/seek_av1_keyframe` — lose vs mediabunny@1.48.0
114. P0 `probe/longform_1h_audio` — lose vs mp4box@2.3.0
115. P0 `probe/opus` — lose vs mediabunny@1.48.0
116. P0 `probe/video_1x1` — lose vs mediabunny@1.48.0
117. P0 `demux/graceful_zero_length` — lose vs mp4box@2.3.0
118. P0 `audio-dsp/fade_in_out_f32` — lose vs mediabunny@1.48.0
119. P0 `decode-seek/meta_pts_monotonic_after_reorder` — lose vs ffmpeg.wasm@0.12.15
120. P0 `decode-seek/meta_seek_vs_linear_decode` — lose vs mediabunny@1.48.0
121. P0 `demux/av1_720p_5s` — lose vs ffmpeg.wasm@0.12.15
122. P0 `decode-seek/seek_negative` — lose vs mediabunny@1.48.0
123. P0 `decode-seek/seek_backward_then_forward` — lose vs mediabunny@1.48.0
124. P0 `mux/flac_to_mkv_audio` — lose vs mediabunny@1.48.0
125. P0 `demux/aac_adts` — lose vs ffmpeg.wasm@0.12.15
126. P0 `robustness/fuzz_mp4_bitflip_probe` — lose vs mp4box@2.3.0
127. P0 `decode-seek/seek_h264_keyframe` — lose vs mediabunny@1.48.0
128. P0 `probe/vp9_alpha` — lose vs mediabunny@1.48.0
129. P0 `transcode/h264_resize_720p` — lose vs remotion@4.0.479
130. P0 `mux/prop_vp9_decode_mux_webm_to_webm` — lose vs mediabunny@1.48.0
131. P0 `probe/h264_vfr` — lose vs mediabunny@1.48.0
132. P0 `metadata/neg_garbled_ilst_mp4_probe` — lose vs mp4box@2.3.0
133. P0 `demux/vp8_720p_10s` — lose vs ffmpeg.wasm@0.12.15
134. P0 `transcode/malformed_zero_length_transcode` — lose vs remotion@4.0.479
135. P0 `robustness/edge_flac_with_seektable_probe` — lose vs remotion@4.0.479
136. P0 `demux/size_micro_micro_audio_short` — lose vs mediabunny@1.48.0
137. P0 `robustness/fuzz_flac_bitflip_probe` — lose vs remotion@4.0.479
138. P0 `probe/metamorphic-recorder-headerless-sane-duration` — lose vs mediabunny@1.48.0
139. P0 `robustness/edge_flac_without_seektable_probe` — lose vs remotion@4.0.479
140. P0 `audio-dsp/resample_44k1_to_48k` — lose vs mediabunny@1.48.0
141. P0 `metadata/neg_garbled_id3_mp3_probe` — lose vs remotion@4.0.479
142. P0 `streaming-output/prop_probe_dur_buffer_shape` — lose vs mediabunny@1.48.0
143. P0 `demux/size_tiny_tiny_vp9_360p_2s` — lose vs mediabunny@1.48.0
144. P0 `robustness/fuzz_mp3_header_truncated_probe` — lose vs remotion@4.0.479
145. P0 `robustness/fuzz_webm_header_truncated_demux` — lose vs remotion@4.0.479
146. P0 `trim/large_h264_frame_accurate_throughput` — lose vs mediabunny@1.48.0
147. P0 `audio-dsp/resample_48k_to_16k` — lose vs mediabunny@1.48.0
148. P0 `decode-seek/seek_past_eof` — lose vs mediabunny@1.48.0
149. P0 `transcode/roundtrip_leg1_h264_to_vp9` — lose vs mediabunny@1.48.0
150. P0 `metadata/read_opus` — lose vs mediabunny@1.48.0
151. P0 `mux/neg_zero_tracks_empty_audio_to_mp4` — lose vs mediabunny@1.48.0
152. P0 `robustness/edge_mislabeled_container_probe` — lose vs remotion@4.0.479
153. P0 `audio-dsp/pcm_s16_to_f32` — lose vs mediabunny@1.48.0
154. P0 `transcode/vp9_to_h264_mp4` — lose vs remotion@4.0.479
155. P0 `remux/vp9_1080p_10s_webm_to_webm` — lose vs remotion@4.0.479
156. P0 `audio-dsp/resample_48k_to_44k1` — lose vs mediabunny@1.48.0
157. P0 `decode-seek/seek_h264_nonkeyframe` — lose vs mediabunny@1.48.0
158. P0 `robustness/fuzz_encrypted_mp4_ciphertext_decode` — lose vs mediabunny@1.48.0
159. P0 `demux/vp9_1080p_10s` — lose vs mediabunny@1.48.0
160. P0 `probe/ts_discontinuity` — lose vs mediabunny@1.48.0
161. P0 `audio-dsp/gain_half_f32` — lose vs mediabunny@1.48.0
162. P0 `performance/size-ladder-iterate-packets-medium` — lose vs web-demuxer@4.0.0
163. P0 `mux/prop_h264_decode_mux_mp4_to_mp4` — lose vs mediabunny@1.48.0
164. P0 `decode-seek/decode_extreme_fps_1` — lose vs remotion@4.0.479
165. P0 `robustness/edge_seek_negative` — lose vs mediabunny@1.48.0
166. P0 `decode-seek/decode_size_micro_h264_1frame` — lose vs mediabunny@1.48.0
167. P0 `encryption/cenc_ctr_missing_key_negative` — lose vs mediabunny@1.48.0
168. P0 `robustness/fuzz_ogg_opus_header_truncated_probe` — lose vs mediabunny@1.48.0
169. P0 `robustness/fuzz_adts_aac_bitflip_probe` — lose vs remotion@4.0.479
170. P0 `robustness/edge_headerless_recorder_probe` — lose vs remotion@4.0.479
171. P0 `audio-dsp/gain_minus6db_s16` — lose vs mediabunny@1.48.0
172. P0 `decode-seek/seek_vfr_arbitrary` — lose vs mediabunny@1.48.0
173. P0 `robustness/edge_seek_past_eof` — lose vs mediabunny@1.48.0
174. P0 `transcode/hevc_to_h264_mp4` — lose vs remotion@4.0.479
175. P0 `robustness/fuzz_ts_zeroed_spans_demux` — lose vs mediabunny@1.48.0
176. P0 `audio-dsp/downmix_stereo_to_mono` — lose vs mediabunny@1.48.0
177. P0 `encryption/hls_sample_aes_requested_as_aes128_negative` — lose vs mediabunny@1.48.0
178. P0 `robustness/edge_longform_probe` — lose vs remotion@4.0.479
179. P0 `encryption/hls_aes128_wrong_iv_negative` — lose vs mediabunny@1.48.0
180. P0 `demux/realworld_mdn_flower_webm` — lose vs ffmpeg.wasm@0.12.15
181. P0 `robustness/edge_dims_1x1_probe` — lose vs mediabunny@1.48.0
182. P0 `transcode/bframe_reorder_h264_to_h264` — lose vs remotion@4.0.479
183. P0 `decode-seek/meta_vfr_seek_lands_on_true_pts` — lose vs ffmpeg.wasm@0.12.15
184. P0 `mux/neg_h264_into_ogg_illegal` — lose vs mediabunny@1.48.0
185. P0 `remux/h264_1080p_30s_mp4_to_ts` — lose vs ffmpeg.wasm@0.12.15
186. P0 `probe/perf-extract-metadata-large` — lose vs mediabunny@1.48.0
187. P0 `robustness/edge_headerless_recorder_remux` — lose vs remotion@4.0.479
188. P0 `decode-seek/seek_hevc_keyframe` — lose vs mediabunny@1.48.0
189. P0 `mux/neg_h264_into_wav_illegal` — lose vs mediabunny@1.48.0
190. P0 `remux/prop_recorder_headerless_duration_materialized` — lose vs mediabunny@1.48.0
