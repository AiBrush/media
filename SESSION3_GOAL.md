Finish building **aibrush-media** — a SOTA, capability-routed, in-browser media engine — to 100% of its Definition of Done, in /Users/tarekbadr/Home/software/projects/aibrush/aibrush.lib/media. **main is GREEN**; DO NOT STOP until the DoD is green.

FIRST read in full (binding): @BUILD_INSTRUCTIONS_SESSION3.md (remaining-work plan + exact mechanics + verified state), BUILD_INSTRUCTIONS.md §2 (the DoD), CLAUDE.md, docs/architecture/. Restate the plan concretely, then build.

STATE (verified — build on it, do NOT redo): main GREEN — typecheck 0, `biome ci` 0, **1486 tests pass**, build+budgets ok. Landed + browser-verified: **#61 DTS packet-seam** (`Packet {chunk,dtsUs?}`, ADR-045 — flipped demux/h264_bframes + ~all mp4→mkv remux); **Gap#5 audio packets()** adts/mp3/ogg (ffprobe oracles, +51 tests); **streaming-output** (Sink+StreamTarget, fragmented mp4, ADR-034 — flipped mp4_fragmented_cmaf + mp4_progressive_buffer + faststart_none).

HOW TO WORK — MANDATORY: **ULTRATHINK at MAX reasoning effort before EVERY step** (restate goal; enumerate edge cases — B-frames, VFR, open-GOP, seek, cancel, frame lifetime, backpressure, truncated input; design note; THEN code). **Drive this with the Workflow tool — fan out parallel opus agents (MAX effort) on STRICTLY DISJOINT files, as much as possible.** Pattern: understand → fan-out implement → adversarially verify (skeptic agents) → LEADER integrates (full gate + harness serially, re-vendors). Per agent: ULTRATHINK → FAILING strict can-fail oracle first on ≥5 real DOWNLOADED files (ffprobe/sips/afinfo) → SOTA implement → pass → fresh benchmark → self-verify (typecheck filtered to its files + `vitest run <its files>`) → return a structured summary. Keep agents WARM. **NEVER run an agent workflow concurrently with a browser harness run** — the browser starves and the run hangs (Session-2 lesson); serialize them.

PRIORITY (SESSION3 §3): (A) **BLOCKER — fix the streaming-output `target:'stream'` HANG** (`prop_probe_dur_stream_shape` hangs full chromium runs past the 30s timeout; make every stream-target scenario settle PASS or honest NA — this gates ALL measurement); (B) 2 FAILs — `opus_to_ogg`/`vorbis_to_ogg` duration short ~0.067s (Opus granule/pre-skip, ogg-write.ts) + `bframes_1080p_mp4_to_mkv` duration long ~0.134s (webm endMs under B-frames); (C) **ogg/ts createMuxer accept foreign packets** (#75 — fan out, one agent per container); (D) **image decode** in-browser + register in defaults.ts + adapter; (E) verify Gap#5 in-browser decode via `--feature transcode` (aac→pcm/mp3→aac/opus→aac); (F) **coverage ≥90% branch** (wasm-mp3/aac frame-header tests; v8-ignore browser-only image; ADR-044); (G) #65 worker + #66 fuzz (#62 already settled, ADR-043). Then (H) **re-vendor → full 558 chromium → `bun scripts/aggregate.mjs` → target biggest NA buckets → webkit + firefox → iterate until aggregate WIN vs all 7 engines holds cross-browser.**

PRIME DIRECTIVES (non-negotiable): ULTRATHINK max effort before every step. NEVER STOP until DoD §2 100% green; blocked → choose SOTA, record an ADR (ADR-046+), continue. EVERY LINE SOTA (strict TS, zero `any`/`!`, typed errors, exhaustive, no dead code; every VideoFrame/AudioData closed once). NO FEATURE WITHOUT TESTS (real downloaded corpus + can-fail oracle + fresh benchmark; one-file green is a fail). DOCS ARE LAW (change → doc + ADR same commit). NEVER FAKE (honest NA, no oracle that can't fail). NEVER run git. Keep main green.

MECHANICS (full detail in SESSION3 §4): bun 1.3.14; gate = typecheck + `biome ci` + `vitest run --coverage` + build. Re-vendor (build+vendor-wasm, WIPE old vendor, copy dist) before each harness run; clear the chromium cache; score with `bun scripts/aggregate.mjs`; the adapter is the harness's live file (not vendored); on a hang, pkill the run tree and take the partial. Begin now: read @BUILD_INSTRUCTIONS_SESSION3.md, create the tasks, fix the §3.A hang first, then launch the fan-out workflows. Build until done.
