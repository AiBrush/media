# Dependencies
node_modules

# Build output
dist
*.tsbuildinfo

# Generated wasm-tail crate build dirs (the committed surface is crate/{Cargo.toml,Cargo.lock,src} plus
# the vendored ../*_wasm_bg.wasm + ../*-core.js; the cargo/wasm-pack output dirs are never committed).
**/crate/pkg
**/crate/target

# Test / coverage
coverage
.vitest

# Downloaded real-media corpus (committed: only fixtures/manifest.json + small goldens)
fixtures/media

# OS / editor
.DS_Store
*.log
