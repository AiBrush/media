# Session 13 per-feature speed ledger

This is the source-of-truth ledger required by `BUILD_INSTRUCTIONS_SESSION13.md`. Rows marked
`PROVISIONAL` come from the handoff or earlier focused exports and do not satisfy the qualified comparison
rules. They must be superseded by a fresh same-export, rotation-on, warm `n>=5` sweep before close-out.
Historical exports use both `remotion-webcodecs` and `remotion-media-parser` because those are distinct
registered engine ids; neither name is silently rewritten into the other.

| feature | family | our median ms | best passing rival | rival ms | ratio | status | root cause | optimization idea(s) | closing evidence |
|---|---|---:|---|---:|---:|---|---|---|---|
| `probe/h264_4k_10s` | probe | 3.790 | mediabunny | 2.970 | 1.276 | PARITY | the current bounded metadata parser categorically excludes known-size video sources above 256 KiB, so large fast-start inputs read and parse a broader 32 KiB window despite media payload size being irrelevant to `moov` truth | lift the payload-size gate, prove a bounded top-level metadata walk on large fast-start/tail-`moov` AVC/HEVC/VFR/B-frame inputs, and retain exact malformed/fragmented/cancellation fallback | `chromium-2026-07-12T04-54-09-350Z.json`, rotated `01.mp4` SHA `05f832e490…`, six engines PASS, warm `n=7`; aibrush trails mediabunny by 0.820 ms inside 2.060 ms combined MAD, memory unavailable; remaining rotations open |
| `probe/hevc_1080p_10s` | probe | 4.305 | mediabunny | 2.215 | 1.944 | BEHIND | the same >256 KiB payload-size gate prevents the small metadata proof, and its accepted track-shape proof is AVC-only even though the full metadata parser already preserves exact `hvc1`/`hev1` configuration and timing | generalize the bounded metadata proof to HEVC and large sources without reading media payloads; validate exact codec/config/dimensions/duration/fps/rotation/color plus tail-`moov`, malformed, VFR/B-frame, and cancellation behavior | `chromium-2026-07-12T04-56-27-116Z.json`, baked `hevc_1080p_10s.mp4`, six engines PASS, warm `n=7`; aibrush trails mediabunny by 2.090 ms with a 1.125 ms negative sum-MAD margin, memory unavailable; sole rotation |
| `probe/h264_vfr` | probe | 4.285 | mediabunny | 2.070 | 2.070 | BEHIND | the 2.28 MiB VFR source is excluded from the small metadata proof by payload size, so probe retains broad-window parsing even though exact VFR duration/fps derive from bounded `moov` timing tables | carry the general large-source bounded metadata walk through VFR `stts` runs while preserving exact duration, average fps, edits, B-frame composition truth, malformed fallback, and cancellation | `chromium-2026-07-12T05-01-57-527Z.json`, baked `h264_vfr.mp4`, six engines PASS, warm `n=7`; aibrush trails mediabunny by 2.215 ms with a 0.945 ms negative sum-MAD margin, memory unavailable; remaining rotations open |
| `probe/large_h264_1080p_120s` | probe | 2.915 | mediabunny | 7.420 | 0.393 | BEHIND | ADR-247's bounded metadata parse already wins this current rotation durably; the remaining closure gap is positive same-export memory and rotation coverage, while ADR-286's general smaller top-level walk may reduce fixed reads further | retain exact large-file duration/fps/config and bounded remote I/O; complete all rotations with positive memory rather than treating a missing sample as zero | `chromium-2026-07-12T05-04-19-036Z.json`, rotated `01.mp4` SHA `05f832e490…`, six engines PASS, warm `n=7`; aibrush leads mediabunny by 4.505 ms with a 1.145 ms positive sum-MAD margin, memory unavailable; remaining rotations open |
| `probe/vp9_alpha` | probe | 3.960 | mediabunny | 4.170 | 0.950 | PARITY | ADR-276 qualifies a complete VP9/AV1 header from a bounded incomplete first Block; ADR-279 skips an intermediate prefix when complete declarations prove terminal Cluster timing is required | retain exact alpha/config/duration/cadence with one bounded or one terminal read; one-read 5.5 KiB product routing is within 5 µs of the driver and has no honest remaining code seam, so complete rotations/memory and establish a durable browser margin | Four current PASS cohorts are raw-fastest: baked 3.990 vs 4.755 ms; rotated `01.webm` 10.420 vs 11.035; `03.webm` 3.880 vs 4.695; and `02.webm` (`chromium-2026-07-12T04-07-03-046Z.json`) 3.960 vs 4.170. Every lead remains inside combined MAD (the `02` margin is 0.210 ms versus 1.005 ms combined MAD), memory is unavailable, and one of four rotations remains pending |
| `audio-dsp/throughput_decode_s24` | audio-dsp | 42.785 | mediabunny | 43.930 | 0.974 | BEHIND | ADR-248 removes planar Float64/interleave churn and ADR-249 releases terminal sources, but current seekable Blob decode still retains roughly one decoded-signal-sized extra allocation at peak | attribute native `AudioData` backing, consumer copy/close timing, deferred-stream last-value reachability, range window, and source release; keep exact fused Float32 bits and reject smaller range windows that regress wall without closing the full memory gap | Brave `02.wav` `brave-2026-07-12T04-34-41-845Z.json`, PASS/PASS, warm `n=7`; aibrush leads raw wall by 1.145 ms inside 10.105 ms combined MAD, but peak 33,729,168 B (`n=1`) exceeds mediabunny 24,049,595 B (`n=2`) by 9,679,573 B. Chromium `03.wav` is likewise within-noise and memory-unavailable; rotations remain open |
| `audio-dsp/meta_idempotent_resample_same_rate` | audio-dsp | 7.435 | mediabunny | 9.265 | 0.802 | PARITY | ADR-253's lightweight copy planner authors a fresh multipart WAV header over validated PCM bytes and closes the old full-materialization/DSP loss | retain exact fresh-container identity output; gather a durable browser margin and positive same-export memory rather than optimize the 0.043-0.052 ms product floor speculatively | `chromium-2026-07-12T04-37-12-509Z.json`, baked `wav_s16.wav`, PASS/PASS, warm `n=7`; aibrush leads raw wall by 1.830 ms inside 4.670 ms combined MAD. Aibrush memory is unavailable while mediabunny reports 34,083,588 B (`n=1`); sole rotation |
| `transcode/vp9_to_av1_webm` | transcode | 2255.710 | mediabunny | 1913.135 | 1.179 | BEHIND | ADR-252 closes the ordinary baked wall locally; ADR-284 retains one bounded exact packet prefix and continues the same one-shot reader through `wasm-vpx` only when native VP9 fails with a typed capability miss before emitting a frame, including cross-split typed Error identity | run a same-export qualified rotation and positive memory; retain exact packet/frame timeline, sampled SSIM, playback, close-once, and bounded replay before comparing wall | Historical `chromium-2026-07-12T04-23-41-319Z.json` was `NA_ENGINE` while mediabunny passed at 11500.235 ms. Fresh headed Chrome 149 native-success product control is 6636.0 ms median / 118.7 MAD (`n=5`), exact normalized fold `96046486`, closes 4482/4482, min SSIM 0.999745. Test-only fail-first real-file integration records six typed misses, loads actual `wasm-vpx-driver`/`vpx-core`, emits/encodes 4482 frames with the same fold/closes, min SSIM 0.999736 and playback; its 6966.4/131.6 ms is diagnostic, not leadership evidence. Browser memory unavailable; same-export rotations remain open |
| `transcode/vp9_alpha_to_vp9_keepalpha` | transcode | 17.490 | mediabunny | 164.945 | 0.106 | BEHIND | ADR-270 proves declared/actual alpha semantic copy; ADR-283 allows redundant `audio:false` only after metadata proves the source has zero audio tracks, while real drops still transcode | retain exact 60/60 colour+alpha payloads, keys, PTS/DTS/durations, and every non-redundant selector decline; complete rotations and positive rival memory | `chromium-2026-07-12T04-41-11-273Z.json`, rotated `01.webm`, PASS/PASS, warm `n=7`; aibrush leads by 147.455 ms with a 143.300 ms positive sum-MAD margin. Aibrush peak is 84,513,517 B (`n=3`) while rival memory is unavailable; rotated `03.webm` product exact semantic rewrite is 0.388 ms but needs a post-ADR browser rerun |
| `transcode/h264_rotate_270_dimswap` | transcode | 1444.685 | mediabunny | 1478.800 | 0.977 | PARITY | encode/filter variance consumes the raw 34.115 ms median advantage | profile rotation/filter/encoder scheduling and establish a margin without changing display geometry, timestamps, or quality | PROVISIONAL active full export, baked `h264_rotated90.mp4`, PASS/PASS, warm `n=5`; lead is inside 79.335 ms combined MAD; memory unqualified; rotations 1/4 |
| `decode-seek/decode_extreme_fps_240` | decode-seek | 332.560 | remotion-webcodecs | 119.180 | 2.790 | BEHIND | current product decode is 64.600 ms full / 157.900 ms with the strengthened full-RGBA oracle, close to the 59.400 ms direct-native floor; the qualified public path adds another ~175 ms, so decoder configure, pooling, queue depth, and mux work are not the root cause | attribute the public engine/source/oracle boundary using only public product shapes (default vs worker-disabled engine, Blob/File vs range source, engine reuse, full RGBA/materialization) while preserving ADR-203's barrier, exact 480-frame clock/digest, cancellation, and close-once ownership | `chromium-2026-07-12T05-43-51-762Z.json`, baked `video_240fps.mp4`, four engines PASS, warm `n=7`; aibrush trails remotion-webcodecs by 213.380 ms with a 208.200 ms negative sum-MAD margin, memory unavailable. Product delivered/closed 480/480 and matched every timestamp/duration plus the full RGBA assertion; public-boundary attribution is open |
| `decode-seek/decode_size_large_vp9_120s` | decode-seek | 1080.590 | mediabunny | 913.865 | 1.182 | BEHIND | the exported bundle is stale: current product profiling of the exact 60-frame RGBA work shape is already 415.3 ms, while a complete 3,600-frame public drain is within about 0.1% of direct native VP9 decode; packet wrappers, promises, queue depth, and startup are not a material root cause | retain ADR-203's exact acceleration barrier, bounded queue, prompt cancellation, and close-once ownership; the committed headed profile uses warmup 3/`n=11`, per-run MAD, handed/closed counts, and an untimed 12-frame full-RGBA SHA-256 | PROVISIONAL active full export, baked `large_vp9_1080p_120s.webm`, PASS three-engine cohort, warm `n=5`; old 211.130 ms durable loss. Current headed warmup1/`n=5` product is 415.3 ms for the 60-frame RGBA shape versus the old passing rival's 913.865 ms, with exact frame count/timestamp parity; current-bundle same-export wall/memory rerun pending; rotations 1/4 |
| `decode-seek/meta_vfr_seek_lands_on_true_pts` | decode-seek | 48.215 | mediabunny | 29.485 | 1.635 | BEHIND | current headed product profiling measures 18.8 ms with exact true-PTS landing; decoder barrier/pooling are not the old loss root, so the baseline is stale and memory remains the active risk | retain true PTS/keyframe semantics and close every discarded frame once; rerun current bundle before further lifecycle change | PROVISIONAL active full export, baked `h264_vfr.mp4`, PASS/PASS, warm `n=5`; old 25.480 ms durable wall loss and 19.7 MB memory loss. Current product is faster than the old rival but cannot close same-export wall/memory evidence; sole rotation |
| `performance/size-ladder-iterate-packets-large` | performance | 71.285 | mp4box | 45.550 | 1.565 | BEHIND | the old cold demux repeated the top-level scan and then paid allocation-heavy AVC classification/packet projection | ADR-251 fuses `moov` discovery with strict `mdat` validation; ADR-269 removes classifier and row-object churn without changing B-frame/VFR/picture truth | PROVISIONAL active full export, rotated `01.mp4` SHA `05f832e490…`, PASS/PASS, warm `n=5`; old loss. Current 89,573,913-byte product warmup3/`n=21` median is 0.576 ms for exact 9,226 rows; public same-export wall/memory/rotations pending |
| `performance/metamorphic-vfr-probe-duration` | performance | 2.440 | remotion-media-parser | 2.250 | 1.084 | PARITY | VFR scalar timing is correct, but remaining fixed metadata routing/read cost leaves the raw medians effectively tied | carry ADR-246/247 range/timing reductions into the final bundle and establish a durable all-rotation margin | PROVISIONAL active full export, baked `h264_vfr.mp4`, PASS/PASS, warm `n=5`; our 0.190 ms raw deficit is inside 0.260 ms combined MAD; memory unavailable; rotations 1/4 |
| `mux/h264_aac_to_mov` | mux | 18.415 | mediabunny | 21.065 | 0.874 | PARITY | ADR-271 prepared MOV plus ADR-282 first-party packet fusion remove the old durable loss, but the current raw lead remains inside runtime variance | preserve presentation origin, leading-empty edits, B-frame/VFR DTS/PTS, transactional provenance, cancellation, and generic fallback; establish a durable margin | `chromium-2026-07-12T04-41-11-273Z.json`, rotated `02.mp4`, PASS/PASS, warm `n=7`; aibrush leads by 2.650 ms inside 6.255 ms combined MAD. Peak 43,012,507 B (`n=2`) is <= mediabunny 48,411,816 B (`n=3`); wall margin remains open |
| `demux/flac_noseektable` | demux | 5.970 | mediabunny | 5.460 | 1.093 | PARITY | ADR-250/262 reduce exact frame scanning and one-chunk ownership to a sub-millisecond product floor; current raw browser loss is inside fixed transport/runtime variance | retain CRC/header/frame-boundary truth and terminal release; establish a strict raw lead and positive same-export memory without weakening no-SEEKTABLE scanning | `chromium-2026-07-12T04-46-50-502Z.json`, baked `flac_noseektable.flac`, four engines PASS, warm `n=7`; aibrush trails mediabunny by 0.510 ms inside 1.080 ms combined MAD, memory unavailable; rotations open |
| `demux/flac_seektable` | demux | 3.065 | mediabunny | 4.580 | 0.669 | BEHIND | ADR-250/262 preserve exact SEEKTABLE-guided frame boundaries while collapsing packet-info ownership to one chunk; the current wall lead is durable but positive same-export memory evidence is absent | retain CRC/header/frame-boundary truth and terminal release; qualify positive memory against every passing rival and complete rotations | `chromium-2026-07-12T04-46-50-502Z.json`, rotated `03.flac`, four engines PASS, warm `n=7`; aibrush leads mediabunny by 1.515 ms with a 0.155 ms positive sum-MAD margin, memory unavailable; remaining rotations open |
| `audio-dsp/edge_longform_audio_probe` | audio-dsp | 2.180 | mediabunny | 3.670 | 0.594 | PARITY | aibrush has the lower median but probe/module/I/O variance consumes the lead | reuse bounded exact-source metadata and remeasure all rotations with positive memory | PROVISIONAL active full export, rotated `02.wav` SHA `e5346c53c3…`, PASS/PASS, warm `n=5`; 1.490 ms lead is inside 1.895 ms combined MAD; memory unqualified; rotations 1/4 |
| `mux/audio_only_aac_to_mp4` | mux | 2.650 | mediabunny | 3.740 | 0.709 | PARITY | tiny-output fixed costs and rival variance consume the median advantage | profile setup/table-authoring fixed cost and gather a durable multi-rotation margin | PROVISIONAL active full export, rotated `01.aac` SHA `423b0786df…`, PASS/PASS, warm `n=5`; 1.090 ms lead is inside 1.415 ms combined MAD; memory unavailable; rotations 1/4 |
| `metadata/read_no_tags_wav` | metadata | 3.010 | remotion-webcodecs | 4.290 | 0.702 | PARITY | bounded WAV metadata work is faster by median but inside combined runtime noise | reduce lazy-import/source-read fixed cost and remeasure every rotation | PROVISIONAL active full export, baked `wav_s16.wav`, PASS/PASS, warm `n=5`; 1.280 ms lead is inside 1.515 ms combined MAD; memory unavailable; rotations 1/4 |
| `decode-seek/decode_vfr_timing` | decode-seek | 393.685 | remotion-webcodecs | 382.330 | 1.030 | BEHIND | current exact VFR delivery is close on wall, but the public decode reports excessive peak memory, consistent with decoded/GPU frame queue lifetime rather than parser scheduling | profile native decoder queue depth, handed-frame close timing, RGBA oracle buffers, source/packet release, and GPU resource teardown; reduce bounded in-flight frames without changing 111 native timestamps, cancellation, or close-exactly-once ownership | `chromium-2026-07-12T04-31-09-678Z.json`, baked `h264_vfr.mp4`, four engines PASS, warm `n=5`; aibrush trails remotion-webcodecs by 11.355 ms with a 4.195 ms negative sum-MAD margin. Peak 672,616,697 B (`n=2`) exceeds the leanest passing rival 107,579,095 B by 565,037,602 B; sole rotation |
| `demux/size_micro_micro_audio_short` | demux | 22.765 | remotion-media-parser | 16.675 | 1.365 | BEHIND | OPEN — profile fixed routing, module-load, MP4 audio sample-table, and packet-wrapper costs on short inputs | eliminate fixed work that is unnecessary for any small audio-only MP4 while retaining exact packet bytes, timing, cancellation, and stream backpressure | PROVISIONAL active full export, rotated `02.mp4` SHA `125e368ec2…`, PASS with a three-engine cohort, warm `n=5`; 7.245 ms negative durable margin; memory unavailable; rotations 1/4 |
| `performance/metamorphic-decode-remux` | performance | 21.620 | mediabunny | 13.910 | 1.554 | BEHIND | OPEN — profile repeated MP4 demux setup, packet projection, and mux finalization in the public metamorphic composition | carry general cold-demux and one-pass MP4 authoring wins through the public composition without recognizing the input or bypassing either operation | PROVISIONAL active full export, rotated `02.mp4` SHA `d01b447eda…`, PASS/PASS, warm `n=5`; 10.400 ms negative durable margin; memory unavailable; rotations 1/4 |
| `transcode/aac_to_opus_webm` | transcode | 56.190 | mediabunny | 82.900 | 0.678 | BEHIND | ADR-272 removed the below-bound queue-drain promise cadence and closes wall durably; retained source/codec/mux buffers still make peak memory exceed the leanest passing rival | keep the 128-item native queue bound and exact close-once ownership while profiling/releasing operation-local ADTS, decoded-frame, encoder, and WebM materialization buffers | `chromium-2026-07-12T03-58-40-292Z.json`, baked `aac_adts.aac`, three engines PASS, warm `n=7`; aibrush leads wall by 26.710 ms with a 25.360 ms positive sum-MAD margin, but peak 32,421,157 B exceeds remotion-webcodecs 25,392,133 B by 7,029,024 B. Row remains BEHIND on memory; ffmpeg errors and the other selected engines are inapplicable |
| `transcode/h264_rotate_normalize` | transcode | 24.690 | remotion-webcodecs | 29.600 | 0.834 | BEHIND | ADR-263/264 prove exact semantic stream copy; ADR-281 composes validated default Blob/File output from immutable source slices without copying media payload | retain topology/sample-in-`mdat` validation and all actual transform, non-zero rotation, cross-brand, unsafe, explicit-sink, URL/stream declines; obtain positive same-export memory | `chromium-2026-07-12T04-41-11-273Z.json`, rotated `03.mp4`, PASS/PASS, warm `n=7`; aibrush leads by 4.910 ms with a 1.225 ms positive sum-MAD margin, but neither engine reports memory. Earlier baked positive memory was a 102,319-byte aibrush loss before ADR-281, so current memory remains open |
| `mux/video_plus_audio_to_mp4` | mux | 201.130 | mediabunny | 48.295 | 4.165 | BEHIND | ADR-287 exact descriptor-clone support does not activate at the qualified public boundary, proving the caller supplies a different stream/collection shape; arbitrary wrappers must remain untrusted because they own independent backpressure/cancellation semantics | attribute the public shape using only public APIs/exported assets, then add a separate transactional general seam only if it can prove complete immutable input without forgeable wrapper provenance, locked/partial consumption, or external-stream trust | `chromium-2026-07-12T05-46-58-626Z.json`, baked H.264+ADTS pair, PASS/PASS, warm `n=7`; aibrush trails mediabunny by 152.835 ms with a 121.215 ms negative sum-MAD margin, memory unavailable. ADR-287 product identity/shallow/structured clones remain byte-identical 30,918,082 B at 5.798/5.986/5.274 ms with zero host chunks; public-shape attribution has resumed |
| `performance/size-ladder-extract-metadata-medium` | performance | 3.420 | remotion-media-parser | 3.140 | 1.089 | PARITY | fixed MP4 routing/read/parse variance consumes the sub-millisecond raw gap | carry the general ADR-246/247 metadata-read reductions into the final bundle and establish a durable all-rotation margin | PROVISIONAL active full export, rotated `01.mp4` SHA `f9bac3dfa3…`, PASS/PASS, warm `n=5`; 2.070 ms negative noise-adjusted margin; memory unavailable; rotations 1/4 |
| `probe/micro_h264_1frame` | probe | 2.700 | mp4box | 2.805 | 0.963 | PARITY | tiny-input fixed routing/module cost leaves the raw lead inside runtime noise | reduce general MP4 probe setup while retaining exact codec/config/duration facts and no whole-file special case | PROVISIONAL active full export, baked `micro_h264_1frame.mp4`, PASS/PASS, warm `n=5`; 0.105 ms raw lead is inside 0.595 ms combined MAD; memory unavailable; sole rotation |
| `probe/realworld_mdn_flower_mp4` | probe | 2.750 | mediabunny | 3.860 | 0.712 | PARITY | the median lead is consumed by combined probe/runtime variance | carry general bounded MP4 metadata work into the final bundle and gather a durable all-rotation lead | PROVISIONAL active full export, rotated `03.mp4` SHA `baf03cd8ba…`, PASS/PASS, warm `n=5`; 1.110 ms lead is inside 1.140 ms combined MAD; memory unavailable; rotations 1/4 |
| `remux/aac_adts_adts_to_mp4` | remux | 5.195 | mediabunny | 5.370 | 0.967 | PARITY | ADTS parse plus MP4 authoring fixed costs leave the raw advantage inside noise | reduce general ADTS packet projection and one-pass MP4 finalization cost while retaining exact AAC framing and duration | PROVISIONAL active full export, baked `aac_adts.aac`, PASS/PASS, warm `n=5`; 0.175 ms lead is inside 0.860 ms combined MAD; our positive peak is 271,658,955 B while rival memory is unavailable; sole rotation |
| `remux/av1_720p_5s_webm_to_mkv` | remux | 6.430 | mediabunny | 8.075 | 0.796 | PARITY | WebM demux/remux variance consumes the raw median lead | reduce general EBML packet/finalization fixed cost and establish a durable margin without changing Block timing or codec-private bytes | PROVISIONAL active full export, baked `av1_720p_5s.webm`, PASS/PASS, warm `n=5`; 1.645 ms lead is inside 3.015 ms combined MAD; rival positive peak is 576,243,431 B while our memory is unavailable; sole rotation |
| `remux/opus_ogg_to_mkv` | remux | 5.550 | mediabunny | 6.745 | 0.823 | BEHIND | ADR-255/275 close packet/gapless projection and cancellation-aware ownership, but rotated `02.ogg` exposes a roughly decoded-signal-sized memory loss and only a within-noise wall lead | attribute source/packet-view/output Blob reachability and browser memory baseline across short Ogg rotations; preserve every payload, timestamp/duration, codec-private byte, pre-skip/CodecDelay, and cancellation edge | Brave `02.ogg` `brave-2026-07-12T04-44-58-476Z.json`, PASS/PASS, warm `n=7`; aibrush leads raw wall by 1.195 ms inside 1.540 ms combined MAD, but peak 33,478,114 B (`n=1`) exceeds mediabunny 23,543,593.5 B (`n=2`) by 9,934,520.5 B. Chromium baked `opus.ogg` is a durable wall win but has only ambiguous `n=1` memory; rotations remain open |
| `demux/size_massive_massive_h264_1080p_2h` | demux | 1349.035 | remotion-webcodecs | 40.685 | 33.159 | BEHIND | ADR-269 makes the exact 553,501-row packet table cheap, but the public demux drain still constructs immutable native chunks for 1.14 GiB and the consumer performs 553,501 `read()`/`await` steps | ADR-278 amortizes producer pulls with a zero-HWM 256 KiB/256-packet/single-window batch; larger batches are rejected because they break post-delivery abort and add memory, while substituting packet metadata would violate the native payload contract | `chromium-2026-07-12T03-56-46-686Z.json`, baked input, all three engines PASS, warm `n=5`; aibrush trails by 1308.350 ms and reports no positive memory sample, while the fastest rival reports 90,552,778 B. Product-only exact packet hash improves 1278.6→1052.745 ms, but the qualified row remains a hard contract-floor loss; sole rotation |
| `performance/op-sweep-demux` | performance | 57.805 | mediabunny | 37.930 | 1.524 | BEHIND | OPEN — profile cold MP4 layout reads plus 2,308 packet-row projection and stream iteration overhead | combine ADR-251 one-scan layout with a general lower-allocation packet iteration path while preserving exact bytes, DTS/PTS, cancellation, and bounded demand | PROVISIONAL active full export, baked `h264_1080p_30s.mp4`, PASS/PASS, warm `n=5`; 23.245 ms negative durable margin; memory unavailable; sole rotation |
| `transcode/h264_to_mkv` | transcode | 1913.845 | mediabunny | 1660.265 | 1.153 | BEHIND | ADR-288 attributes the wall to the native implicit H.264 encoder: packet extraction is 3.0-4.5 ms and mux/materialization 5.4-12.9 ms, while encoder latency is 1.59-2.26 s; accepted hardware forcing is neutral | implicit-only realtime latency reduces the exact selected product path 2259.5→1606.2 ms while preserving configured rate/profile, 321 packets, 25 fps, 12.841667 s, playback decode, 324/324 close-once ownership, and minimum sampled SSIM 0.999346; rerun the exact public row and positive memory | PROVISIONAL active full export, rotated `01.mp4` SHA `f9bac3dfa3…`, PASS/PASS, warm `n=5`; old 384.985 ms negative durable margin. ADR-288 product A/B is warm `n=7` and general controls pass, but cannot supersede same-export public evidence; memory unavailable; rotations 1/4 |
| `transcode/hevc_to_av1_webm` | transcode | 1735.145 | mediabunny | 1841.735 | 0.942 | BEHIND | ADR-252's ordinary-cadence realtime AV1 policy closes the native codec wall without changing exact WebM clocks, SSIM, or playback | retain measured native decoder/encoder floor; obtain positive aibrush memory before any queue/resource change | `chromium-2026-07-12T04-27-34-092Z.json`, baked `hevc_1080p_10s.mp4`, PASS/PASS, warm `n=5`; aibrush leads by 106.590 ms with a 48.025 ms positive sum-MAD margin. Mediabunny peak is 30,965,027 B (`n=1`) while aibrush memory is unavailable, so wall is closed but row remains BEHIND on memory; sole rotation |
| `decode-seek/decode_open_gop_first_frame` | decode-seek | 281.790 | remotion-webcodecs | 270.105 | 1.043 | BEHIND | exact open-GOP startup is close by median but decoder/runtime variance consumes both directions; memory is an explicit loss | reduce general decoder startup/projection overhead while retaining all 12 digest-identical frames and the true first decodable picture, and release decoder/source state promptly | PROVISIONAL active full export, baked `h264_open_gop_1080p.mp4`, PASS three-engine cohort, warm `n=5`; 27.225 ms negative noise-adjusted margin; our peak 332,425,831 B exceeds rival 306,778,955 B; sole rotation |
| `mux/swap_audio_video_with_opus_to_mkv` | mux | 202.890 | mediabunny | 56.185 | 3.611 | BEHIND | the frozen ADR-283 export confirms the public boundary still constructs per-packet host objects despite ADR-257's 19.600 ms exact native product; the same copied-descriptor miss addressed generally by ADR-287 is the leading current hypothesis | rebuild ADR-287 and rerun the exact public pair; retain private stream authority and generic handling for any wrapped/locked/pulled/external stream rather than propagating forgeable provenance | `chromium-2026-07-12T05-23-48-099Z.json`, baked `h264_1080p_30s.mp4+opus.ogg`, PASS/PASS, warm `n=7`; aibrush trails mediabunny by 146.705 ms with a 138.610 ms negative sum-MAD margin, memory unavailable. ADR-257 exact product remains 19.600 ms; integrated ADR-287 rerun pending |
| `performance/size-ladder-extract-metadata-large` | performance | 3.060 | mediabunny | 3.825 | 0.800 | PARITY | aibrush leads by raw median but combined fixed-cost variance consumes the margin against one rival | carry ADR-246/247 into the final bundle and establish a durable all-rotation lead | PROVISIONAL active full export, rotated `03.mp4` SHA `dcea729fb3…`, PASS with a four-engine cohort, warm `n=5`; 0.765 ms raw lead is inside 0.870 ms combined MAD; memory unavailable; rotations 1/4 |
| `probe/h264_bframes_1080p` | probe | 2.315 | remotion-media-parser | 3.380 | 0.685 | PARITY | exact MP4 metadata has the lower median but runtime variance consumes the small lead | retain ADR-246/247's bounded single-pass metadata work and gather durable all-rotation evidence | PROVISIONAL active full export, rotated `01.mp4` SHA `f9bac3dfa3…`, PASS/PASS, warm `n=5`; 1.065 ms lead is inside 1.240 ms combined MAD; memory unavailable; rotations 1/4 |
| `probe/h264_rotated90` | probe | 2.165 | mediabunny | 2.925 | 0.740 | PARITY | exact rotation/config metadata has the lower median, but fixed browser and range-read variance consumes the raw lead | retain the general bounded MP4 metadata path and establish a durable margin across every rotated selection without special-casing display matrices | PROVISIONAL active full export, rotated `01.mp4` SHA `0cd83d944a…`, PASS/PASS, warm `n=5`; 0.760 ms lead is 0.020 ms smaller than the 0.780 ms combined MAD boundary; memory unavailable; rotations 1/4 |
| `probe/realworld_mdn_flower_webm` | probe | 4.130 | mediabunny | 6.080 | 0.679 | BEHIND | ADR-258's 8 KiB strict WebM metadata window and ADR-246's missing-suffix range extension close the former durable wall loss; positive same-export memory and remaining rotations are still absent | retain exact two-track declarations, duration/config, bounded incomplete-input fallback, and no payload scan; qualify positive memory and complete all rotations | `chromium-2026-07-12T05-22-11-206Z.json`, rotated `01.webm` SHA `42b0dd1803…`, five engines PASS, warm `n=7`; aibrush leads mediabunny by 1.950 ms with a 0.225 ms positive sum-MAD margin, memory unavailable; remaining rotations open |
| `decode-seek/decode_extreme_fps_1` | decode-seek | 79.980 | web-demuxer | 31.715 | 2.522 | BEHIND | current product decode is 7.600 ms full / 25.900 ms with the strengthened full-RGBA oracle versus a 6.200 ms native floor; the qualified public path adds ~54 ms, so the old decoder-startup hypothesis is disproven and the public engine/source/oracle boundary is active | compare default/worker-disabled engine construction, Blob/File/range normalization, per-iteration reuse, and full RGBA/materialization using only public APIs; retain the exact barrier, 30-frame clock/digest, cancellation, and close-once ownership | `chromium-2026-07-12T05-41-34-849Z.json`, baked `h264_1fps_30s.mp4`, four engines PASS, warm `n=7`; aibrush trails web-demuxer by 48.265 ms with a 36.155 ms negative sum-MAD margin, memory unavailable. Product delivered/closed 30/30, matched every timestamp/duration, and produced full RGBA digest `ccef2555976d…b91c7e8`; public-boundary attribution is open |
| `decode-seek/seek_negative` | decode-seek | 38.780 | mediabunny | 32.190 | 1.205 | BEHIND | current headed product zero-clamp seek is 5.7 ms with exact landing; decoder pooling/barrier removal gives no durable benefit and risks retained resources | preserve the mathematically general clamp, exact landed PTS, cancellation, and close-once semantics; rerun current bundle | PROVISIONAL active full export, baked `h264_1080p_30s.mp4`, PASS/PASS, warm `n=5`; old 9.205 ms durable loss. Current product is faster than old rival but qualified public wall/memory is pending; sole rotation |
| `remux/prop_adts_to_mp4_duration_invariant` | remux | 4.420 | mediabunny | 5.070 | 0.872 | PARITY | aibrush leads by median but both short-audio operations are dominated by fixed routing/authoring variance | ADR-255 reuses the validated ADTS frame layout and ADR-254 removes the second faststart `moov`; gather durable current-bundle wall/memory evidence | PROVISIONAL active full export, baked `aac_adts.aac`, PASS/PASS, warm `n=5`; 0.650 ms raw lead is inside 2.805 ms combined MAD; memory unavailable; sole rotation |
| `transcode/h264_to_hevc_mp4` | transcode | 6832.665 | mediabunny | 6971.830 | 0.980 | PARITY | the multi-second hardware HEVC encode has the faster median and materially stronger SSIM, but runtime variance consumes its 139.165 ms lead | reduce general decoder/encoder startup and bounded packet projection overhead without trading away the 0.9866 mean SSIM or playback truth | PROVISIONAL active full export, rotated `03.mp4` SHA `58dc001d18…`, PASS/PASS, warm `n=5`; lead is inside 307.335 ms combined MAD; memory unavailable; rotations 1/4 |
| `demux/size_large_large_h264_1080p_120s` | demux | 122.125 | remotion-webcodecs | 306.775 | 0.398 | BEHIND | the old export predates ADR-260's terminal revocation; current all-rotation attribution finds no product-owned source, sample-table, range-window, or host-chunk retainer after EOF and close, so only a positive current-bundle browser sample can determine whether the 103.9 MB loss remains | rerun ADR-260's current artifact against the passing rival on rotated `02.mp4`, then complete all four rotations; do not add speculative allocation or GC behavior while exact terminal ownership is already proven | PROVISIONAL active full export, rotated `02.mp4` SHA `42704b1cc6…`, PASS/PASS, warm `n=5`; old peak 189,426,678 B exceeds rival 85,555,893 B by 103,870,785 B. Current exact byte/Blob attribution over `01/02/03` preserves 9,982/1,808/13,885 packets and payload/timeline hashes 993,752,762/4,249,479,337/3,929,844,223; every Blob route reaches 155 B live ArrayBuffers and zero live chunks after EOF+close+GC. Qualified rerun pending; rotations 1/4 |
| `metadata/write_mp4_tags` | metadata | 28.380 | mediabunny | 21.890 | 1.296 | BEHIND | ADR-274 proves same-brand ordinary sample storage and ADR-280 composes immutable Blob slices around the exact patched `moov`; current product attribution finds a 0.494 ms public `02.mp4` route, 0.291 ms output readback, 0.025 ms reprobe, and only one residual 0.211 ms duplicate ADR-251 `moov` proof while the 6.490 ms browser raw loss remains inside combined noise | retain conservative declines; reject a new cross-layer proof token for a 0.211 ms ceiling that cannot close the browser gap; rerun current-bundle `01.mp4` and obtain positive memory after the shared loading freeze | Current ADR-280 exports: Chromium baked `h264_1080p_30s.mp4` PASS/PASS 59.165 vs 227.305 ms with 164.565 ms positive noise margin; Brave `03.mp4` PASS/PASS 42.010 vs 86.990 ms; Chromium `02.mp4` (`chromium-2026-07-12T04-05-46-121Z.json`) PASS/PASS 28.380 vs 21.890 ms, a 6.490 ms raw loss inside 8.825 ms combined MAD. All memory is unavailable; original `01.mp4` remains pending. Ten-file strict warm3/`n=21` Blob-direct is 2.114 ms versus full-remux 8.935 ms; isolated current public routes are 0.537/0.494/0.581/2.156 ms for `01`/`02`/`03`/baked and read only bounded headers plus two `moov` passes, never `mdat` |
| `performance/encode-fps` | performance | 4916.685 | mediabunny | 4139.805 | 1.188 | BEHIND | ADR-288's same-run 900-frame control confirms the implicit H.264 native encoder dominates and realtime halves that product transcode wall; the absolute product operation is not the public encode-fps shape | retain the implicit-only latency policy, exact 30 s/900-packet cadence, HTML playback, 903/903 close-once ownership, and near-unity SSIM, then remeasure the actual public operation and positive memory | PROVISIONAL active full export, baked `h264_1080p_30s.mp4`, PASS/PASS, warm `n=5`; old 826.565 ms negative durable margin. Integrated product warm `n=7` realtime 6331.7 ms vs forced quality 11856.1 ms with minimum sampled SSIM 0.999978/0.999976; these non-equivalent absolute walls are attribution only, not row closure; memory unavailable |
| `performance/size-ladder-iterate-packets-large4k` | performance | 70.320 | mp4box | 38.865 | 1.809 | BEHIND | ADR-269 removed classifier/row-allocation work, but the public operation still constructs one immutable native WebCodecs chunk and performs one consumer `read()`/`await` per packet | ADR-278 lets one zero-HWM producer pull enqueue at most 256 KiB/256 packets from one retained source window; larger queues are rejected because they break post-delivery abort and increase the active memory loss | `chromium-2026-07-12T03-49-57-076Z.json`, rotated `01.mp4`, PASS/PASS, warm `n=7`; aibrush trails by 31.455 ms with a 26.075 ms negative sum-MAD margin. Remotion rivals are slower and mediabunny/web-demuxer fail exact truth; memory unavailable. Product-only multi-real evidence preserves exact hashes and measures 21.155 ms for 1,808 packets, but cannot supersede this same-export loss; rotations remain incomplete |
| `performance/size-ladder-iterate-packets-medium` | performance | 16.235 | mp4box | 12.950 | 1.254 | BEHIND | ADR-269 removed resident AVC classifier allocation, while ADR-278 amortizes producer pulls; the remaining operation floor is one immutable native chunk construction and one consumer `read()`/`await` for every packet | retain the exact 256 KiB/256-packet/single-window batch; larger batches are rejected for abort and memory regressions, and metadata-only substitution would weaken the native payload contract | `chromium-2026-07-12T03-54-43-954Z.json`, rotated `01.mp4`, PASS/PASS, warm `n=7`; aibrush trails mp4box by 3.285 ms inside 5.825 ms combined MAD, while both faster header-only engines fail exact truth and remotion rivals are slower. Memory unavailable; rotations incomplete |
| `transcode/av1_to_h264_mp4` | transcode | 15708.260 | mediabunny | 15921.860 | 0.987 | PARITY | aibrush has the faster median and stronger SSIM, but multi-second hardware variance consumes the raw advantage | reduce general decoder/encoder startup and packet projection overhead while retaining 0.9970 mean SSIM and playback truth | PROVISIONAL active full export, rotated `02.webm` SHA `4e6548894b…`, PASS/PASS, warm `n=5`; 213.600 ms lead is inside 392.160 ms combined MAD; memory unavailable; rotations 1/4 |

The complete feature table will be generated from the first qualified Session 13 export. No absent row is
implicitly a win.
