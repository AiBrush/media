# Measured Evidence

> The evidence appendix behind the docs: facts that could **not** be re-derived from `src/` — measured
> numbers/benchmark results, browser/runtime quirks, ADR rationale, competitive gaps, known bugs, and fixture
> provenance. The family docs cite this file (`measured-evidence.md`) wherever they state a measured value or a
> browser-specific behavior, so the number's provenance survives even though the original per-session notes
> were retired. Built deterministically (no LLM authored the numbers) from 672 facts distilled out of the 235
> pre-rebuild `.md` files; each bullet cites its original source doc/ADR. Nothing here is invented. The
> reusable-architecture subset of these facts is promoted into the [decision log](decisions/README.md); this
> file keeps the full measured detail. See also the [documentation index](architecture/README.md).


## Measured numbers & benchmark results  (225)

- 313 of the benchmark wins (56%) ran with coopCoep not-required AND wasmThreads 0; no winner anywhere used wasm threads (ffmpeg.wasm won all 129 in single-thread mode).  _(src: docs/architecture/background/benchmark-summary.md)_
- A fresh integrated public 1 fps H.264 decode run measured 79.980 ms despite 7.600 ms full close-only and 25.900 ms full-RGBA product controls; at least ~31 ms of the remainder could not be attributed to any tested product API shape and black-box discipline forbade assigning it to an acceptance implementation detail.  _(src: docs/notes/session13-webcodecs-decoder-startup.md)_
- A harness re-measure with --warmup 0 --iters 1 takes ≈25 min and does not change any PASS/NA verdict (perf throughput only); a full perf run takes ≈110 min.  _(src: BUILD_INSTRUCTIONS_SESSION6.md)_
- AAC-to-Opus codec-phase staging A/B on aac_adts.aac (163,811 bytes, 470 frames, stereo 48 kHz, 10.026667 s) was byte-identical (128,350-byte WebM SHA f7723973…, 502 Opus packets, 481,296 sample frames) but regressed wall 56.37 vs 46.64 ms and peak delta 2,735,883 vs 2,339,651 bytes, so the staging code was removed.  _(src: docs/notes/session13-aac-opus-memory.md)_
- AAC-to-Opus WebM scheduling fix: making below-threshold WebCodecs input submission synchronous measured 0.000368 ms vs 0.000521 ms (1.42x) per unit over 2,000 ten-frame graphs; the provisional passing row is 24.840 ms vs mediabunny 21.185, and a fused private AudioDecoder→AudioEncoder→WebM path was rejected as duplicating capability routing for removable scheduling.  _(src: docs/notes/session13-aac-opus-webm-fixed-cost.md)_
- ADR-002: benchmark winners collapse to WebCodecs 67%, WASM 25%, pure-JS/TS 8%, with native <video> ~never winning; hence the default codec ladder is hardware WebCodecs → GPU (filters) → native/sw WebCodecs → WASM, containers in hand-written TS.  _(src: docs/architecture/02-decision-records.md ADR-002)_
- ADR-006: 56% of benchmark wins (and every win) ran with coopCoep:not-required and wasmThreads:0, so the default build requires no cross-origin isolation and WASM SIMD+threads (SharedArrayBuffer) are opt-in; enableThreads defaults to crossOriginIsolated.  _(src: docs/architecture/02-decision-records.md ADR-006)_
- ADR-018: 206 WEAK-GATE plus 3 SUSPECT benchmark 'wins' passed loose/shortcut gates (duration-only, SSIM exactFrames==0, 'didn't crash', hardcoded per-asset shortcuts), motivating strict bit-exact/structural self-validation oracles.  _(src: docs/architecture/02-decision-records.md ADR-018)_
- ADR-020: tiny-work tier thresholds are inputBytes ≤ 64 KiB, outputPixels ≤ 4096 (64×64), mediaSeconds ≤ 1, or audioFrames ≤ 48000; below these the router re-ranks cheaper in-process/native work ahead of GPU/WASM setup; telemetry includes real tiny media like 2x2-green.mp4 (3503 bytes, 0.1818 s).  _(src: docs/architecture/02-decision-records.md ADR-020)_
- ADR-036: Symphonia's pure-Rust symphonia-codec-vorbis compiles to a 157 kB .wasm via wasm-pack; wasm-opt is disabled because the environment's Binaryen predates the bulk-memory ops LLVM emits; Vorbis encode is a typed CapabilityError (no production pure-Rust Vorbis encoder).  _(src: docs/architecture/02-decision-records.md ADR-036)_
- ADR-039: Symphonia symphonia-codec-aac compiles to a 145 kB .wasm; AAC-LC decode yields exactly 1024 samples/channel per frame; scope is AAC-LC mono/stereo only (Symphonia rejects SBR/HE-AAC/>2ch).  _(src: docs/architecture/02-decision-records.md ADR-039)_
- ADR-044: strict noUncheckedIndexedAccess creates provably-unreachable '?? fallback' branches counted as uncovered: aac.ts measured 100% statements / 73.5% branch, mp3.ts 95% / 70.4% branch; these artifact branches are excluded from the ≥90% branch gate rather than lowering the threshold.  _(src: docs/architecture/02-decision-records.md ADR-044)_
- ADR-058: PCM resample uses a cached rational-rate polyphase bank (up to 4096 phases) for longform inputs; a 1-hour mono 44.1 kHz→16 kHz WAV (317,520,044 input bytes → 115,200,044-byte output) completes in ~6.7 s (resample ~4.2 s), with bench-dsp resample aggregate ~730x realtime, worst ~401x, checksum 439301100 across the 8-file WAV corpus.  _(src: docs/architecture/02-decision-records.md ADR-058)_
- ADR-083: preload() warms real router paths idempotently; bench-preload records ~20,900 warmups/sec geomean on the local Bun 1.3.14 baseline.  _(src: docs/architecture/02-decision-records.md ADR-083)_
- ADR-086: the pure-TS FLAC encoder (FIXED-predictor orders 0–4, partitioned Rice, stereo decorrelation, VERBATIM floor) is bit-exact lossless, achieves compression ratios 0.05–0.71, and runs 5–35 MB/s single-thread; LPC analysis is a future ratio improvement.  _(src: docs/architecture/02-decision-records.md ADR-086)_
- ADR-092: the leak-free eager kernel measured ~54 kB and the first-op app bundle ~254 kB (both just over the DoD targets of ~50 kB and ~150–250 kB); check-budgets.ts ceilings were raised to KERNEL_BUDGET=58 kB and TYPICAL_APP_BUDGET=264 kB as explicit temporary regression ceilings with a tracked fix.  _(src: docs/architecture/02-decision-records.md ADR-092)_
- ADR-104: Session-6 Chromium raised from a 2026-06-27 baseline of 518 PASS/36 NA_ENGINE/7 NA_BROWSER/0 FAIL to 555 PASS/4 NA_ENGINE/2 NA_BROWSER/0 FAIL/0 ERROR across 561 scenarios (98.9%); the gate ran 142 Vitest files/2215 tests, coverage 92.24% statements/90.01% branches, budgets 49.95/50.00 kB eager and 246.18/256.00 kB first-op, 45 anti-cheat checks green.  _(src: docs/architecture/02-decision-records.md ADR-104)_
- ADR-106: Session-6 cross-browser partials before stalls — WebKit 124 PASS/3 NA_BROWSER/4 NA_ENGINE/14 FAIL/8 ERROR (153/561 rows, stalled on transcode/ladder_tiny_h264_360p_resize_180p), Firefox 40 PASS/1 NA_BROWSER/8 NA_ENGINE/2 FAIL (51/561 rows, stalled on robustness/fuzz_adts_aac_bitflip_probe); WebKit NA_BROWSER exposure is AV1 encode, MP3 encode, AV1 decode.  _(src: docs/architecture/02-decision-records.md ADR-106)_
- ADR-110: WebKit's complete Session-7 run is 428 PASS/119 NA_BROWSER/14 NA_ENGINE/0 FAIL/0 ERROR (561 rows); WebKit codec gaps are AV1 decode/encode and MP3 encode.  _(src: docs/architecture/02-decision-records.md ADR-110)_
- ADR-112: robustness/edge_longform_probe (one-hour AAC M4A) timed out at 479957 ms because public probe() built a full live Demuxer; adding optional ContainerDriver.probe() metadata hook dropped it to 178 ms; readMovie() parses the head moov in ~5 ms, and a metadata-light readMovieMetadata() brought massive-file metadata probe to a ~24.14 ms median, and WebM av1_720p_5s probe to 116 ms (~9.92 ms median).  _(src: docs/architecture/02-decision-records.md ADR-112)_
- ADR-189: implicit offline video rate control uses 20 aggregate bits per output pixel per second (was 9.216 Mb/s implicit) with a 300 kb/s floor and per-codec efficiency scaling; a repeated-dimensions identity resize was forcing an avoidable Canvas2D YUV→RGB→YUV round trip, and eliminating it moved rotated H.264 SSIM from 0.968371 to 0.981680/0.986473/0.9943 and reached 545.99 fps on the identity case.  _(src: docs/architecture/02-decision-records.md ADR-189)_
- ADR-202 benchmark (Bun 1.3.14, warmup 2 + 7 samples, all eight real huge/massive rotations, 4.86 GiB total): demux setup median 42.033 ms (peakRSS+=14.52 MiB); drain packets median 3452.371 ms for 2,045,145 packets with 2,878 packetRanges/pullPromises, 2,042,283 synchronous pulls, rangeGiB 8.70, peakRSS+=14.94 MiB. Selected huge/massive pair (02.mov+01.mp4, 480,852 packets) measures 10.015 ms setup and 384.000 ms drain.  _(src: docs/notes/session11-mp4-demux-resident-ranges.md)_
- ADR-202 fresh black-box output: huge MP4 demux cell 986.1 ms, huge packet iteration 615.4 ms, paired huge demux 447.8 ms, massive packet iteration 2,836.3 ms; selected huge rotation declares 42,276 packets, massive rotation 438,577.  _(src: docs/notes/session11-mp4-demux-resident-ranges.md)_
- ADR-203 exported fair-harness codec fixed-overhead losses: decode-seek/decode_size_tiny_vp9_360p 476.315 ms vs 117.845 ms rival; tiny H.264 decode row 265.385 ms vs 105.940 ms rival.  _(src: docs/notes/session11-webcodecs-video-exact-acceleration.md)_
- ADR-203 validation: focused WebCodecs selection/startup tests 66/66 with 169 assertions; VPx alpha-normalization 3/3; exact Router suite 30/30 with 75 assertions (both keep→discard and discard→keep orders); eager bundle 49.68 KiB against the 50 KiB guard.  _(src: docs/notes/session11-webcodecs-video-exact-acceleration.md)_
- ADR-204 I/O floor on 01.mov after warmup: current coalesced payload scan median 105.453 ms with 310 range reads and 609,092,550 bytes requested; optimistic 64-byte prefixes 132.910 ms payload + 4.826 ms parse with 13,721 range reads and 1,272,638 bytes. Coalescing bridged gaps: 16 KiB → 9,612 windows/33,330,935 B; 64 KiB → 2,888/274,542,931; 256 KiB → 391/564,505,173.  _(src: docs/notes/session11-mp4-avc-key-picture-proof.md)_
- ADR-205 CPU profile of massive/01.mp4 (438,577 packets) attributes drain to ~276 ms native ReadableStream pull/reader scheduling, 93 ms packet emission (incl. 64 ms required sample Uint8Array views), 84 ms file-window I/O/alloc, 23 ms buildSamples, 10.5 ms second range-validation scan, 5.5 ms window-item alloc/sort, 9 ms minimal fake Encoded*Chunk.  _(src: docs/notes/session11-mp4-packet-drain-floor.md)_
- ADR-205 packet-drain optimization: two warmup-two/median-seven runs measured 352.963 ms and 349.595 ms vs 382.048 ms baseline on huge/02.mov+massive/01.mp4 (7.6–8.5% reduction, 480,852 packets, 812 reads, checksum 1,670,818,688). A controlled CPU-profile run on massive/01.mp4 moved drain from 365.644 ms to 262.731 ms. Eight-rotation 4.86 GiB run stays 3,451.076 ms vs 3,452.371 ms baseline (indistinguishable; 8.70 GiB repeated I/O dominates), checksum 4,067,077,818.  _(src: docs/notes/session11-mp4-packet-drain-floor.md)_
- ADR-207 focused Router suite passes 33/33; after the cache plus eager codec-frame split, production measurements are 49.70 KiB eager closure (0.30 KiB below the 50 KiB guard) and 216.68 KiB default-driver first-operation closure.  _(src: docs/notes/session11-router-exact-positive-cache.md)_
- ADR-208 Matroska attachment stream-copy: WebM suite 153/153 with independent ffprobe truth; regression subject scenarios/metadata/write_mkv_tags/03.mkv declares application/json attachment followed by JPEG cover attachment alongside H.264 and AAC.  _(src: docs/notes/session11-matroska-attachment-stream-copy.md)_
- ADR-209 deficit-gate benchmark: seven-run warm benchmark over the current 50-export, 19 MiB public result set measured 0.08 s median wall (range 0.08–0.09 s) to parse, cohort, render, and atomically replace each report.  _(src: docs/notes/session11-rotated-deficit-gate.md)_
- ADR-258's 8 KiB WebM first metadata window cut the flower path to one transport concept: driver-only current 8 KiB (3.867 ms, 1 read, 8,192 B) vs former 4 KiB→64 KiB (7.791 ms, 2 reads, 69,632 B); a 64 KiB first request was rejected as eight times the retained bound and 4 KiB was rejected because a modest legal Tracks declaration deterministically adds a round trip.  _(src: docs/notes/session13-webm-metadata-window.md)_
- ADR-259/269 optimized massive in-memory AVC packet-info: full packet-info fell to ~31-32.6 ms median (from ~78 ms), 1.68x faster than the 54.770 ms passing rival; the direct whole-view classifier is 13.728 ms vs 27.024 ms for the former promise/batch (1.968x); strict result is 553,501 rows, 341,101 key flags, checksum 2,336,086,988, seven reads.  _(src: docs/notes/session13-mp4-avc-in-memory.md)_
- ADR-277 range-less sequential WAV decode on real 03.wav (7,904,256 bytes) held wall parity (3.438 vs 3.428 ms) while cutting median positive ArrayBuffer peak from 18,463,056 to 10,558,688 bytes (exactly one 7,904,368-byte source allocation); both returned 1,315,328 frames in 322 chunks with checksum 658366885.  _(src: docs/notes/session13-wav-memory-lifecycle.md)_
- ADR-282/287 native packet provenance fusion: for the h264_1080p_30s.mp4 + aac_adts.aac pair producing a 30,918,082-byte MP4 (SHA 6517ad7b53b1…), exact descriptor identity took 4.962 ms constructing zero host chunks, while shallow/structured clones took 8.550/7.163 ms constructing 1,370 Encoded*Chunk objects; the public MP4/MOV mux browser row was 203.115 ms.  _(src: docs/notes/session13-native-track-clone-provenance.md)_
- ADR-283 metadata-proved redundant track exclusion: the selected video-only VP9-alpha rotation with redundant audio:false measured 0.388 ms median (MAD 0.038) locally versus a qualified pre-fix browser wall of 300.495 ms and the passing rival at 74.120 ms; the fresh 3,862-byte rewrite (SHA 25dd20c3…) matched all 60 color and 60 alpha packets of the 5,487-byte source exactly.  _(src: docs/notes/session13-semantic-noop-convert.md)_
- After CENC change: fail-first run observed 232 imports on the committed ffmpeg twin and 216 on the real CBCS case; both reduced to exactly one non-extractable key per KID and a measured peak of 16 concurrent native transforms (bounded worker window); six-case real-corpus benchmark reports 0.36–1.77 ms per file; focused matrix 175/175.  _(src: docs/notes/session11-cenc-prepared-key-parallelism.md)_
- After closing pcm_s16be_to_s16le, backlog regenerated to 195 active deficits, severity split 0/0/13/182 plus the ADR-130 parity exemption.  _(src: docs/notes/session9-aiff-pcm-byte-rewrite.md)_
- After live input, runtime controls, declarative jobs, and composed remux metadata landed, an isolated clean build measured 57.73 kB eager against the 50.00 kB ceiling; recovery target was at least 7.98 kB to restore the binding 0.25 kB guard band.  _(src: docs/notes/session12-eager-budget-recovery.md)_
- After meta_idempotent_resample_same_rate fix, backlog: 286 active deficits, severity 0/0/71/215 plus the ADR-130 parity exemption.  _(src: docs/notes/session9-wav-identity-transcode.md)_
- After opus_to_webm_audio fix, backlog: 299 active deficits (0/0/86/213), 0 exempt.  _(src: docs/notes/session9-ogg-audio-webm-mux-fast-path.md)_
- After Session 3, main was green with 1486 Vitest tests passing.  _(src: BUILD_INSTRUCTIONS_SESSION3.md)_
- After the ADTS direct-writer fix, aac_to_pcm_wav_extract closing run: aibrush-media 28.460 ms median vs ffmpeg.wasm 31.995 ms; earlier fresh run had 17.455 vs 23.760 ms.  _(src: docs/notes/session9-adts-aac-direct-wav-s16.md)_
- After the eager-budget recovery refactor, final production artifacts measure 48.98 kB eager against 50.00 kB (1.02 kB margin) and 224.16 kB typical first-operation JS against 256.00 kB (31.84 kB margin).  _(src: docs/notes/session12-eager-budget-recovery.md)_
- After the lazy filter-proxy split: eager bundle 48.53 kB / 50.00 kB, default/probe 232.91 kB / 256.00 kB.  _(src: docs/notes/session9-lazy-muxer-filter-proxies.md)_
- An HTTP Range vs plain-GET A/B for full-source fetches kept Range faster: ADTS 0.500 vs 0.600 ms and Opus 0.400 vs 0.500 ms (5 warmups, 31 samples), so transport method was ruled out as the loss and partial-content semantics were preserved.  _(src: docs/notes/session13-probe-leadership-2026-07-13.md)_
- An identity Canvas2D resize (1280x720 target already 1280x720) caused SSIM 0.9735 (FAIL); omitting the identity resize raised it to 0.9943 (PASS) at 545.99 fps, proving the YUV->RGB->YUV round trip, not quantization, was the dominant loss.  _(src: docs/notes/h264-resize-quality.md)_
- audio-dsp/edge_gapless_aac_decode closed at aibrush-media 9.040 ms median vs mediabunny 10.600 ms; oracle decodedSamples=44673 at 44100 Hz, rawAacFrameSamples=46080, primingSamples=1024, sampleDelta=0.  _(src: docs/notes/session9-gapless-aac-identity-trim.md)_
- audio-dsp/edge_longform_audio_probe (one-hour PCM WAV): pre-fix aibrush 4.115 ms vs mediabunny 3.145 ms; closed at aibrush 4.570 ms vs mediabunny 5.490 ms after adding WavDriver.probe.  _(src: docs/notes/session9-wav-bounded-probe.md)_
- audio-dsp/edge_longform_audio_resample_16k (one-hour mono 44.1kHz s16 -> 16kHz): the 44.1->16 conversion uses the long 80 dB kernel producing 57.6 million output frames and a 115.2 MB s16 payload; pre-fix aibrush 12415.135 ms vs ffmpeg.wasm 3998.530 ms, closed at aibrush 3610.680 ms with peak memory 462,048,953 bytes.  _(src: docs/notes/session9-wav-s16-direct-resample.md)_
- audio-dsp/edge_longform_audio_resample_16k (one-hour mono 44.1kHz s16 WAV -> 16kHz WAV s16 = 57.6M output frames, 115.2 MB s16 payload) pre-fix aibrush-media 12415.135 ms median vs ffmpeg.wasm 3998.530 ms; after direct polyphase FIR fix aibrush-media 3610.680 ms, peak memory 462,048,953 bytes.  _(src: docs/notes/session9-wav-s16-direct-resample.md)_
- audio-dsp/gain_half_f32 pre-fix Chromium: aibrush-media 35.630 ms median, ffmpeg.wasm 16.425 ms, mediabunny 30.720 ms; all PASS.  _(src: docs/notes/session9-wav-f32-direct-gain.md)_
- audio-dsp/gain_half_f32: pre-fix aibrush 35.630 ms vs ffmpeg.wasm 16.425 ms vs mediabunny 30.720 ms; closed at aibrush 10.235 ms (488.520x realtime, peak 33,172,658 B) vs ffmpeg 14.185 ms (352.485x) vs mediabunny 21.895 ms.  _(src: docs/notes/session9-wav-f32-direct-gain.md)_
- audio-dsp/meta_idempotent_resample_same_rate pre-fix Chromium: aibrush-media 22.170 ms, mediabunny 4.465 ms, ffmpeg.wasm 24.965 ms; after identity-rewrite fix aibrush-media 8.010 ms, mediabunny 9.635 ms, ffmpeg.wasm 35.770 ms.  _(src: docs/notes/session9-wav-identity-transcode.md)_
- audio-dsp/meta_idempotent_resample_same_rate: pre-fix aibrush 22.170 ms vs mediabunny 4.465 ms vs ffmpeg.wasm 24.965 ms; closed at aibrush 8.010 ms vs mediabunny 9.635 ms vs ffmpeg 35.770 ms.  _(src: docs/notes/session9-wav-identity-transcode.md)_
- audio-dsp/pcm_s16be_to_s16le (big-endian s16 AIFF to canonical LE WAV): pre-fix aibrush-media 21.550 ms vs ffmpeg.wasm 14.485 ms; after byte-order rewrite fix aibrush-media 11.770 ms vs ffmpeg.wasm 17.315 ms.  _(src: docs/notes/session9-aiff-pcm-byte-rewrite.md)_
- audio-dsp/pcm_s16be_to_s16le (big-endian s16 AIFF to LE WAV) baseline aibrush-media 21.550 ms vs ffmpeg.wasm 14.485 ms; closed at aibrush 11.770 ms vs ffmpeg 17.315 ms.  _(src: docs/notes/session9-aiff-pcm-byte-rewrite.md)_
- audio-dsp/pcm_s24be_to_s16le (big-endian s24 AIFF to LE s16 WAV) baseline aibrush-media 29.925 ms vs ffmpeg.wasm 20.015 ms; closed at aibrush 14.340 ms vs ffmpeg 15.290 ms.  _(src: docs/notes/session9-aiff-s24be-s16le-direct-wav.md)_
- audio-dsp/pcm_s24be_to_s16le (big-endian s24 AIFF to LE s16 WAV): pre-fix aibrush-media 29.925 ms median vs ffmpeg.wasm 20.015 ms; after fix aibrush-media 14.340 ms vs ffmpeg.wasm 15.290 ms.  _(src: docs/notes/session9-aiff-s24be-s16le-direct-wav.md)_
- bench-session12-container-routing-aes: direct AES-128 over 12 real TS/ADTS segments (50,944 encrypted bytes/batch) median 1.086 ms at 46.90 MB/s, SHA-256 04f543...48c9; native alias remux over m2ts/mts/mpegts/aac (429,864 input bytes/batch) median 2.197 ms at 195.66 MB/s, SHA-256 4c8dc0...f6cf; raw 188/192/204-byte sniff median 3.438 ms per 6,000 predicates.  _(src: docs/notes/session12-container-routing-and-direct-aes.md)_
- bench-session12-deterministic-routing (3 warmups, 21 samples): 1.640 µs per uncached forced-software selection, 0.744 µs per exact cached selection, 14.607 µs per public bounded image-decline operation; force-software image decline reads at most the 4 KiB routed prefix.  _(src: docs/notes/session12-force-software-routing.md)_
- bench-session12-gapless over all 11 exact files (5 native, 5 public rotations, 1 global control; warmup 2, 7 samples): setup median 18.826 ms, complete packet-drain median 27.779 ms, stable packet count 88,497.  _(src: docs/notes/session12-gapless-corpus-and-native-suppression.md)_
- bench-session12-high-bit-depth-profiles (Bun 1.3.14, 5 warmups, 21 samples, 10,000 iter/sample): profile/level config matrix median 48.858 ms (3,274,777 plans/s, p95 53.869 ms); depth/filter/alpha lifecycle matrix median 3.233 ms (52,575,959 plans/s); public encode-route guards median 0.454 ms (132,085,856 plans/s); geomean 28,331,859 plans/s, checksum -502217872; 191/191 tests green.  _(src: docs/notes/session12-high-bit-depth-profiles.md)_
- bench-session12-job-runner baseline geomean 219,883 jobs/s across documented/canonical-fusion/unsafe-order/typed-rejection cases (5 warmups, 21 samples, 1,000 jobs/sample); confirmation run measured 199,077 jobs/s with 1.18–1.42 MB peak process-heap.  _(src: docs/notes/session12-declarative-job-runner.md)_
- bench-session12-packet-drain (3 warmups, 11 samples, Session 12 M4 machine): median known-track drain 1.055334 ms for 5,000 packets (4,737,837 packets/s); 250 fail-first sibling groups 1.571459 ms (159,088 groups/s); 1,000 public task handles attach/remove caller-signal listeners in 5.174792 ms (193,244 handles/s); peak process heap 2,509,178 bytes, peak RSS 160,677,888 bytes, retained 20,195 heap / 999,424 RSS bytes (below 64 MiB bound).  _(src: docs/notes/session12-packet-drain-lifecycle.md)_
- bench-session12-remux-metadata: WAV full-track selection + tags 0.135 ms confirmation median (675.59 MB/s, 1.97 MB heap); WebM Vorbis selection→tagged Ogg 0.827 ms confirmation median (516.84 MB/s, 6.23 MB heap); tests 38/38 metadata-composition, 52/52 public codec-operation, 4/4 remux-runner.  _(src: docs/notes/session12-remux-select-tags.md)_
- bench-session12-runtime-controls (3 warmups, 11 samples): exact pinned codec route 0.787 µs/pick (2,500/sample); profile+root normalization+asset resolution 2.265 µs/set (20,000/sample); createMedia({enableThreads:false,assetBaseUrl,worker:false}) 1.881 µs/engine (20,000/sample); tests 98/98, 41/41, 277/277.  _(src: docs/notes/session12-runtime-controls.md)_
- bench-session12-stream-input (2 warmups, 7 samples) over 5 real corpus files (H.264/MP4, VP9/WebM, s24/WAV, native FLAC, MP3; 190,813 bytes total): every byte-backed and direct-stream sample matched public metadata digest 19b614d3...539a; direct path performed exactly 194 pulls/sample, median 0.569 ms vs 0.186 ms for replayable bytes; direct-stream RSS deltas 999,424–1,867,776 bytes.  _(src: docs/notes/session12-readable-stream-input.md)_
- bench-session12-video-pad (3 warmups, 21 samples) over a 640×360 RGBA source centered into a 720×480 transparent canvas: median 1.373 ms (251.6 MPix/s).  _(src: docs/notes/session12-video-pad-design.md)_
- bench-session12-wav-frame-encode (2 warmups, 7 samples, 143,834 input bytes / 51,200 frames/sample): median 1.566 ms / 91.863 MB/s, output checksum 2910552623; peak ArrayBuffer growth 7,771,680 bytes, RSS growth 1,736,704–6,651,904 bytes, process heap 7.84–11.12 MB, retention below 64 MiB; suite 24/24 and codec matrix 55/55.  _(src: docs/notes/session12-wav-frame-encode.md)_
- bench-session12-wav-pcm-stream on real 03.wav (2 warmups, 7 samples): 0.135 ms median first chunk after 65,536 returned bytes, 12.497 ms median complete drain of 1,315,328 frames (checksum 3860784884), 9.286 ms legacy full-buffer median (checksum 2936041591).  _(src: docs/notes/session12-public-truth-boundaries.md)_
- bench-streaming (2026-06-25 baseline, 14 real corpus files = 7 MP4 + 7 TS/HLS, 21 iterations, 3 warmups): MP4 output 108.5 MB/s via WritableStream and 142.6 MB/s via callback; TS 1,318.3 MB/s and 1,439.0 MB/s respectively.  _(src: docs/notes/session12-element-sinks.md)_
- Benchmark anti-cheat integrity roll-up of 558 features: 349 REAL, 206 WEAK-GATE, 3 SUSPECT, 0 CHEAT, 0 INCONCLUSIVE; WEAK-GATE concentrated in transcode (47), trim (31), robustness (29).  _(src: docs/architecture/background/benchmark-summary.md)_
- Binding bundle budgets: eager kernel ≤ ~50 kB; typical app first-op JS ≤ 256 kB (target ~150–250 kB); a probe-only app must pull zero WASM; WASM is lazy, miss-only, and same-origin (verified by a bundle-analysis test).  _(src: BUILD_INSTRUCTIONS.md)_
- Budget after ADTS fix: eager kernel 47.73 kB / 50.00 kB, default/probe first-operation closure 254.94 kB / 256.00 kB.  _(src: docs/notes/session9-adts-aac-direct-wav-s16.md)_
- Build toolchain verified present: emcc 6.0.1, rustc/cargo 1.94.0, wasm-pack 0.14.0, wasm-opt (Binaryen) 124 (now supports --enable-bulk-memory, so the old homebrew-wasm-opt rejecting memory.copy gotcha no longer applies); meson and nasm MISSING.  _(src: docs/notes/wasm-codec-cores.md)_
- By substrate, benchmark wins concentrate to three: WebCodecs 374 wins (67%, zero bundle cost), WASM/libav 139 (25%, high bundle), pure-JS parsers 42 (8%, tiny); native <video>/MediaRecorder wins ~0 (its probe path is 600-7000x slower).  _(src: docs/architecture/background/benchmark-summary.md)_
- CENC prepared-key change: median Bun wall on two 2.1 MiB fair-corpus files dropped from 23.202 to 15.640 ms (CENC) and from 5.903 to 3.336 ms (CBCS); before change a direct instrumented run made 387 importKey and 387 AES-CTR calls for CENC.  _(src: docs/notes/session11-cenc-prepared-key-parallelism.md)_
- cenc_ctr.mp4 (5.02s/2.2MB) decrypt makes 387 subtle.encrypt calls (video 151 + audio 236, ~1 per CENC sample since each sample has its own IV = the WebCrypto floor), with 16-way in-flight concurrency.  _(src: docs/perf/goal26-evidence.md)_
- Chromium harness eager kernel budget is 50.00 kB and default/probe first-operation closure budget is 256.00 kB; representative green readings were eager 47.73–47.90 kB and closure 254.94–255.22 kB across Session 9 audio work.  _(src: docs/notes/session9-adts-aac-direct-wav-s16.md)_
- Cold MP4 demux profiling: full parsing measured 0.206 ms and mp4PacketMetadata 0.298 ms on an 11,050-packet shape; the cold URL-like demux issued seven range reads ([0,16],[32,48],[32,138778],[0,16],[32,48],[138778,138794],[138786,138802]) moving only 138,842 bytes, isolating a duplicated top-level header walk as the cause; URL/element inputs over 512 KiB get a 256 KiB read-ahead window.  _(src: docs/notes/session13-mp4-demux-layout.md)_
- Container-routing AES fail-first run produced 21 failures; after implementation the focused alias/AES/container matrix passed 120/120 and central review passed a broader 337/337 matrix.  _(src: docs/notes/session12-container-routing-and-direct-aes.md)_
- demux/aac_adts closed with aibrush-media 4.015 ms median (PASS golden-packets) vs mediabunny 5.920 ms; oracle detail 470 packets, maxPtsDrift=0.  _(src: docs/notes/session9-adts-payload-free-packet-info.md)_
- demux/opus (Ogg Opus packet table): pre-fix aibrush 14.830 ms vs mediabunny 8.150 ms; closed at aibrush 6.690 ms vs mediabunny 7.235 ms vs ffmpeg.wasm 12.235 ms.  _(src: docs/notes/session9-ogg-demux-packet-table.md)_
- demux/realworld_mdn_trex_mp3 closed at aibrush-media 3.235 ms median (golden-packets) vs mediabunny 3.305 ms; oracle detail 81 packets, 1 compared track, maxPtsDriftUs=0.  _(src: docs/notes/session9-mp3-payload-free-packet-info.md)_
- demux/wav_s24: baseline 7.355 ms; uncached payload-free URL helper reached low single digits but stayed noisy, full byte-backed fetch regressed, and a cached raw-4KiB-prefix URL helper closed it at aibrush 0.210 ms vs mediabunny stored 3.0 ms. Cache: 4 KiB RIFF prefix, 60 s, 64 entries.  _(src: docs/notes/session9-wav-pcm-packet-info.md)_
- Detailed VFR native decode attribution: close-only public 60-frame drain 17.5 ms, full RGBA readback 226.1 ms, fresh native 111-frame decode 24.2 ms, proved-decoder reuse 20.2 ms; retaining the complete public output kept 111 (VFR) and 182 (B-frame control) native frames live, confirming caller-retained surfaces cause the large process peak, and the JS heap only rose 4,310,048→4,838,363 bytes while 111 native frames were retained.  _(src: docs/notes/session13-vfr-decoder-surface-memory.md)_
- Eager kernel budget is <=~50 kB; a typical app (kernel + a couple ops + selected drivers) targets <=256 kB; the ~500 kB ceiling covers JS glue only, with WASM cores outside it.  _(src: docs/architecture/08-packaging-and-loading.md)_
- Earlier Session 13 integration gate: 207 Vitest files / 3,774 tests, 93.08% statements and 90.11% branches, 50 anti-cheat checks, eager kernel 49.75 kB against a 50.00 kB / 0.25 kB guard, and typical first-op closure 250.87 kB against 256.00 kB.  _(src: docs/notes/session13-baseline-and-gate-repair.md)_
- element-sink materializer coverage 98.06% statements/lines, 92.92% branches, 96.15% functions (34/34 tests); after splitting materializers the eager static closure is 48.68 kB (1.32 kB below 50 kB) and typical first-operation closure 222.75 kB (33.25 kB below 256 kB).  _(src: docs/notes/session12-element-sinks.md)_
- Faststart one-pass moov (ADR): a direct writer A/B measured faststart at 1.383375 ms vs 0.888334 ms non-faststart (0.495041 ms penalty, 1.557x) before the fix, and 0.894417 vs 0.903084 ms after (penalty gone, checksum 964116104); an array-only MOV routing specialization was implemented then reverted after it regressed (1.941 vs 1.547 ms).  _(src: docs/notes/session13-mp4-faststart-one-pass.md)_
- Final exact-tree canonical gate: 212 test files / 3,925 tests, statements 92.90%, branches exactly 90.00%, eager closure 48.66 KiB, typical default-operation closure 53.83 KiB.  _(src: docs/notes/session13-probe-leadership-2026-07-13.md)_
- First sealed no-reuse Chromium rotation chromium-2026-07-10T23-25-04-841Z.json completed all 563 cells with no timeout/OOM: 482 PASS, 59 N/A, 20 FAIL, 2 ERROR. After source-truth repairs and a full headed frame bake (543 filled, 54 honest partial, 15 honest failures), a no-reuse rerun of all 22 reds plus both header-destroyed rows returned 19 PASS, 3 FAIL, 2 ERROR.  _(src: docs/notes/session11-fair-harness-boundary-audit.md)_
- FLAC copy-trim stored deficits: trim/audio_flac_seektable_copy 167.4 ms vs ffmpeg.wasm 6.9 ms, and trim/audio_flac_noseektable_copy 157.1 ms vs ffmpeg.wasm 10.3 ms; closed on Chromium 149 at aibrush 6.295 ms vs ffmpeg 9.155 ms (seektable) and aibrush 10.530 ms vs ffmpeg 11.175 ms (noseektable).  _(src: docs/notes/session9-flac-keyframe-trim-fast-path.md)_
- FLAC needs only the 42-byte mandatory fLaC+STREAMINFO structure; the probe reduces the ordinary seekable read to 64 bytes (from a fixed 4 KiB prefix) and, for a declared ID3v2 prelude, jumps directly to 42 bytes at the declared FLAC offset after decoding the synchsafe length.  _(src: docs/notes/session13-probe-leadership-2026-07-13.md)_
- For the H.264 2Mb/s portrait/60 subject (58dc001d..., High 1080x1920 60fps 626 distinct frames, 10.433s, 5.72Mb/s source), the best VideoToolbox config (High, 60fps, 2Mb/s) hit FFmpeg SSIM 0.954306 / local RGB mean 0.921032; libx264 medium at requested 2Mb/s hit SSIM 0.960279 (1.849Mb/s); the black-box cell stays red at mean 0.902773/min 0.891974 vs a 0.95 gate.  _(src: docs/notes/h264-2mbps-quality-bound.md ADR-193)_
- Fragmented-audio init-probe fresh local benchmark (warmup 3 + median 9 over four range-backed files): median 0.500 ms, rangeCalls=5, rangeBytes=807022, maxEnd=675950, peakRSS+=0.02 MiB, checksum 901134169.  _(src: docs/notes/session11-fragmented-audio-init-probe.md)_
- Fresh Chromium 149 FLAC probe/demux medians: probe/flac_seektable aibrush 5.270 ms vs remotion-media-parser 6.525 ms; probe/flac_noseektable aibrush 4.055 ms vs remotion 6.010 ms; demux/flac_seektable aibrush 5.230 ms vs mediabunny 6.435 ms; demux/flac_noseektable aibrush 4.645 ms vs ffmpeg.wasm 11.520 ms; demux/metamorphic_flac_seektable_invariance aibrush 4.785 ms vs mediabunny 6.995 ms.  _(src: docs/notes/session9-flac-probe-demux-packet-info.md)_
- Fused 5.1-to-mono BS.775 remix (drops LFE, 1/sqrt(2) coefficients) on 10 s 48 kHz 6-channel PCM, 51 samples after 5 warmups: wall improved 1.084 ms → 0.477 ms (2.27× faster); input throughput 2.66 → 6.04 Gsample/s; identical checksum. Removed two Float64Array allocations (16 temporary bytes/source frame) and one full signal walk.  _(src: docs/notes/session12-remix-performance.md)_
- Fused signed-24 WAV decode (emitting interleaved f32 directly, skipping planar Float64) decoded 1,315,328 stereo frames in 3.012 ms median versus 9.433 ms canonical planar-plus-interleave, a 3.13x speedup; both made nine bounded reads for 7,904,256 bytes with checksum 2064061439.  _(src: docs/notes/session13-wav-s24-fused.md)_
- gain_half_f32 after direct f32 gain fix: aibrush-media 10.235 ms median, throughput 488.520x realtime, peak memory 33,172,658 B; ffmpeg.wasm 14.185 ms (352.485x); mediabunny 21.895 ms.  _(src: docs/notes/session9-wav-f32-direct-gain.md)_
- Goal-26 landed adapter win (double-demux elimination): mux() multi-source previously demuxed each source twice (prepareMuxTracks then #muxMultiSource re-demux); packing already-demuxed EncodedTracks took swap_opus_to_mkv 173→278→599 throughputRealtime (vs mb 584, +3% win) and video_plus_audio_to_mp4 126→262→540 (vs mb 660, -7%), from 4x losses to parity.  _(src: docs/perf/goal26-evidence.md)_
- Goal-26 n=15 converged standing: wins confirmed at n=15 include swap_opus_to_mkv (653 vs 606, +8%), size_micro_1frame_to_mp4 (407 vs 371, +10%), read_no_tags_wav (9.6 vs 10.8, +12%); real small losses ~7-8% (not noise): seek_mkv (21.5 vs 20.1), seek_av1 (24.5 vs 22.5), demux/size_micro_h264_1frame (3.0 vs 2.8).  _(src: docs/perf/goal26-evidence.md)_
- Goal-26 pooled bounded direct decode win root cause: baked micro decode fixtures carry no manifest sizeBytes so the direct-decode gate (required known size ≤512KB) never fired; adding VideoDecoder pooling + bounded N-frame path took decode_size_micro 60→236 decodeFps (vs remotion 186) and decode_tiny_dims_2x2 544→1623-1998 (vs mediabunny 1274-1555).  _(src: docs/perf/goal26-evidence.md)_
- Goal-26 resample fast-path (tryPreparedWavIdentityTranscode returns canonical WAV bytes directly, skipping engine.pcm dynamic import + routing) took meta_idempotent_resample_same_rate from 21.37 to 13.45 agg (vs mediabunny ~12.8-17.5), now competitive/winning with oracle PASS.  _(src: docs/perf/goal26-evidence.md)_
- Goal-26 seek pooling (WarmVideoDecoderPool in engine.seek): seek_av1 won at n=13 (27.7 vs 32.4, ~+17%), seek_mkv co-win (33.7 vs 33.3), but seek_negative still loses ~25% because it is moov-parse-bound on the 30s mp4 (needs lazy moov parse, not decoder pooling).  _(src: docs/perf/goal26-evidence.md)_
- Goal-26: aibrush was measured winning ~15/26 named benchmark rows; solid repeatable wins (>15% margin) include decode_tiny_dims_2x2 (2173 vs ~1300 decodeFps), decode_size_micro (236 vs 200), demux/vp9_alpha, mux/audio_only_aac_to_mp4 (2650), mux/pcm_s24_to_wav (694), aiff & caf probe, op-sweep, size-ladder, read_flac_seektable, read_pcm_s16be.  _(src: docs/perf/goal26-evidence.md)_
- Hardware WebCodecs decode/encode was benchmarked at 20-35x faster than single-thread WASM, which is why the router prefers WebCodecs first.  _(src: docs/architecture/01-goals-and-requirements.md)_
- Hardware WebCodecs measured 20-35x faster than single-thread WASM: transcode/av_downmix_stereo_to_mono was mediabunny 2,598 ms vs ffmpeg.wasm 88,342 ms (~34x); bframe_reorder_h264_to_h264 ~23.9x.  _(src: docs/architecture/background/benchmark-summary.md)_
- Headed no-reuse Chromium export chromium-2026-07-11T14-55-47-618Z.json completed 563/563 cells with 499 PASS, 61 N/A, two FAIL, one ERROR; the strict aggregate has nine historical/current reds: four gapless operation-window rows, disproved s24 golden, stripped Matroska attachment projection, Chromium HEVC platform decode, stripped massive packet sizes, and detached HLS context.  _(src: docs/notes/session12-public-truth-boundaries.md)_
- HEVC Main10 warm 51-sample planning benchmark reports ~1.63 million Main10 config builds per second.  _(src: docs/notes/session12-hevc-main10-output.md)_
- High-cadence AV1 rate control scales the AV1 bitrate budget by the square root of cadence above 30.5 fps (capped at the H.264 budget); the 30.5 fps boundary tolerates rational-clock representation noise around nominal 30 fps.  _(src: docs/architecture/09-operations.md ADR-252)_
- HLS re-readable source: focused create-media/HLS matrix passes 79/79, including unhinted byte manifests, ambiguous MIME/extension manifests, split single-use manifest, non-HLS stream replay, AES-128 OpenSSL twins, and SAMPLE-AES.  _(src: docs/notes/session11-hls-rereadable-source.md)_
- Implicit H.264 encode may seed native rate control with 3 disposable clones at unique earlier PTS; high-cadence AV1 with 8; their timestamps are suppressed from output and the real first frame stays a forced keyframe.  _(src: docs/architecture/09-operations.md ADR-252)_
- Implicit H.264 realtime latencyMode reduced the native encoder wall from 2,259.5 ms (quality) to 1,606.2 ms at the identical 18,432,000 bit/s configuration — a 653.3 ms / 28.9% reduction — with minimum sampled SSIM only dropping 0.000362 (0.999708 to 0.999346); forcing the accepted prefer-hardware hint instead measured 2,298.9 vs 2,278.5 ms no-preference so hardware-hint caching was rejected.  _(src: docs/notes/session13-h264-implicit-realtime.md)_
- Implicit offline video bitrate uses a 20-bits-per-output-pixel-per-second quality budget with codec-efficiency scaling and a 300 kb/s floor; the public h264 encode string is floored at level L3.0 for tiny-MP4 playback/seek compatibility.  _(src: docs/architecture/09-operations.md ADR-084)_
- In the 558-feature browser-media benchmark (chromium 149 / Apple M1 Max, suite 0.1.0) across 7 engines, wins were: mediabunny 313 (56.1%), ffmpeg.wasm 129 (23.1%), remotion-webcodecs 38 (6.8%), remotion-media-parser 26 (4.7%), platform/chrome-149 23 (4.1%), mp4box 16 (2.9%), web-demuxer 10 (1.8%), and 3 features (0.5%) had no passing engine.  _(src: docs/architecture/background/benchmark-summary.md)_
- job-runner.test.ts 67/67 green with runner-only V8 coverage 97.76% statements/lines, 90.74% branches, 100% functions; integration trim(0,0.1) on sfx-pcm-s16.wav emits exactly 9,600 payload bytes (4,800 mono s16 frames) probing WAV pcm-s16 48 kHz mono 0.1 s.  _(src: docs/notes/session12-declarative-job-runner.md)_
- Lazy audio-container defaults: default-driver static closure dropped from 306.25 KiB baseline to 216.68 KiB (28.32 KiB below 245 KiB target, 39.32 KiB below the unchanged 256 KiB hard ceiling); MP4/WebM stay statically registered while WAV/MP3/Ogg/ADTS/AIFF/CAF load via lazy ContainerDriver proxies. Container matrix 327 passed / 0 failed.  _(src: docs/notes/session11-lazy-audio-container-defaults.md)_
- Live-media Node adapter benchmark (2 warmups, 7 samples, 10,000 frames/sample, 200 forced pending-read cancellations): median 4.991 ms (2,003,490 frames/s, exactly 10,001 pulls/sample); median cancellation 3.374 µs; all 1,400 late frames closed exactly once; post-GC RSS retention 3,817,472 bytes (below the pinned 32 MiB allowance).  _(src: docs/notes/session12-live-media-input.md)_
- Local ffmpeg SSIM (raw YUV without range/transfer normalisation) reads the geometric-filter defect as ~0.90, harsher than the fair harness which reported 0.946 (transcode/h264_rotate_normalize) and 0.950 (transcode/multitrack_select_default_audio) against a 0.98 gate.  _(src: docs/notes/filter-colorspace-ssim.md)_
- Lost-features 2026-07-13 exhaustive Chromium export (56 losses): winning frameworks were mediabunny 36, remotion 10, mp4box 7, ffmpeg.wasm 3; restricting to rows with all six engine results yields 54 confirmed losses (2 provisional rows lack a remotion result).  _(src: lost-features-chromium-2026-07-13.md)_
- Lost-features Batch I local gate: FLAC fused packet-info measured 2.444 ms median vs 2.626 ms composed baseline; ADTS retained-layout 0.000327 ms vs 0.000668 ms for a repeated frame walk (bench-session13-flac-packet-info warmup 5, 21 samples).  _(src: docs/notes/lost-features-2026-07-13-design.md)_
- Lost-features exhaustive baseline transcode walls (aibrush vs winner): h264_resize_4k_to_1080p 63282.36 ms vs remotion 8417.18; av1_to_vp9_webm 20785.7 vs remotion 13972.28; h264_to_hevc_mp4 20609.66 vs mediabunny 9372.85; roundtrip_leg2_vp9_to_h264 30088.01 vs remotion 18788.99; extreme_fps_240 24171.39 vs mediabunny 24150.85.  _(src: lost-features-chromium-2026-07-13.md)_
- Lost-features remediation results: demux/realworld_mdn_flower_webm WON (aibrush 2.305 ms vs fastest competitor 6.695 ms), demux/vp8_720p_10s WON (2.405 vs 12.220), demux/size_tiny_tiny_vp9_360p_2s WON (2.290 vs 7.150), demux/size_large_large_vp9_1080p_120s WON (81.595 vs 155.080) — all via payload-free WebM packet metadata (Demuxer.packetTable()).  _(src: docs/perf/lost-features-2026-07-13-checklist.md)_
- Lost-features still-losing demux rows: h264_4k_10s aibrush 50.965 vs mp4box 28.925 ms, h264_rotated90 19.565 vs mp4box 12.275, realworld_mdn_flower_mp4 12.610 vs mp4box 4.410, vp9_1080p_10s 35.545 vs mediabunny 19.230, demux/hls_vod 63.850 vs mediabunny 43.345.  _(src: docs/perf/lost-features-2026-07-13-checklist.md)_
- Matroska attachment packet-seam: WebM/packet suite 232/232; benchmark runs 8 distinct real WebM/MKV files (four rotated MKVs plus AV1, VP9, tiny, real-world WebM). Attachment payload SHA-256 values retained across all routes are 94809623… and 831953157….  _(src: docs/notes/session11-matroska-attachment-packet-seam.md)_
- Matroska attachment stream-copy 2026-07-11 local run (warmup 3, n=9): 8.986 ms median for the 924,924-byte input producing a 923,988-byte genuine re-layout, 1.77 MiB RSS delta, stable full-output checksum.  _(src: docs/notes/session11-matroska-attachment-stream-copy.md)_
- Matroska tags: append implementation failed all 9 fail-first cases; offset-stable passes 9/9 and combined metadata matrix 54/54. bench-session12-matroska-tags (3 warmups, 15 samples): median 0.210 ms for three generations across five fixtures (590,465 rewritten bytes, 2,806.74 MB/s), output SHA-256 f613...298b; retained process heap 77,352 bytes / RSS 13,041,664 bytes after GC (below 32 MiB bound).  _(src: docs/notes/session12-matroska-tag-replacement.md)_
- MP4 AAC ASC-geometry benchmark bench-session11-mp4-aac-metadata: 8 rotations, n=9, median 3.733ms, peak RSS +3.95MiB; 118 focused MP4 tests green.  _(src: docs/notes/mp4-aac-asc-geometry.md ADR-190)_
- MP4 metadata-only demux+packetTable of the 553,501-row two-hour AVC takes 32.0 ms, whereas draining the identical rows through native packet streams takes 1,278.6 ms; batching improved the massive drain to 1,052.745 ms (17.7%) but the qualified rival is 43.75 ms — the row stays BEHIND because native immutable chunk construction must still copy 1.14 GiB.  _(src: docs/notes/session13-mp4-packet-stream-batching.md)_
- MP4 metadata-write (same-family remux with tags) public path measured 3.098 ms median on the 5,339,207-byte rotated source versus the already-shipped direct tag writer at 0.558 ms; the MP4 driver advertising both mp4 and mov made the single-format metadata shortcut decline a same-MP4 request, forcing a full sample-table reparse and a second full-movie allocation.  _(src: docs/notes/session13-mp4-metadata-write.md)_
- MP4 multitrack array crossover retained route is narrow (non-fragmented default-faststart mp4, >=2 descriptors, all packetsArray, no readable packets, >=256 combined packets); measured speedups were 1.09x at 256, 1.32x at 512, 1.11x at 1,024, 1.15x at 2,048 packets, while 2/8/16/32/64-packet shapes were within noise or slower, and MOV stayed generic because its specialization regressed.  _(src: docs/notes/session13-mp4-multitrack-array-crossover.md)_
- MP4 packet-truth benchmark bench-session11-mp4-packet-truth: 250,000 AVC classifications + 232 MOV packets in 7.001ms median, 4.05MiB RSS delta; the selected 725MiB/two-hour rotation produced a full 535,606-row table in 263.1ms with exactly 1,941 video key pictures (1,680 declared sync samples + 261 non-IDR intra pictures).  _(src: docs/notes/mp4-packet-truth.md)_
- MP4 VFR mux fix benchmark bench-session11-mp4-vfr-mux: 500x626 packets, n=9, median 8.739ms, peak RSS +0.69MiB; browser result 626 packets, one keyframe, zero PTS/DTS differences.  _(src: docs/notes/mp4-vfr-encoder-mux.md ADR-191)_
- MP4/MOV public MOV prepared-writer route (ADR-268 one-chunk exact-owned Blob): three warm-five/n=21 runs on the 2,308-packet 31.2 MiB H.264+AAC source measured public Blob 4.032/4.215/4.367 ms vs readable-packet generic 4.548/4.624/4.377; the long input exposed a correctness bug where prepared authoring omitted the generic muxer's leading empty edit for a later-starting track (48-byte output mismatch).  _(src: docs/notes/session13-mp4-public-fixed-cost.md)_
- mux/audio_only_aac_to_mp4 closed with aibrush-media 6.240 ms median vs ffmpeg.wasm 10.140 ms, aibrush reporting 1607.532x median realtime throughput and 27,053,108 bytes median peak memory; output duration 10.026666666666667 s.  _(src: docs/notes/session9-adts-aac-mp4-prepared-mux.md)_
- mux/flac_to_mkv_audio closed on Chromium with aibrush-media@dev 2.725 ms median vs mediabunny@1.48.0 6.420 ms vs ffmpeg.wasm@0.12.15 9.755 ms; eager kernel budget reported 49.66 kB against the 50.00 kB cap.  _(src: docs/notes/session9-flac-mkv-mux-fast-path.md)_
- mux/mp3_to_mp3: baseline aibrush 11.420 ms, fastest rival mediabunny 7.5 ms; closed at aibrush 3.900 ms median (property-invariant outDurationSec=10.031020408163265, tolerance 1.5 s).  _(src: docs/notes/session9-mp3-prepared-same-container-mux.md)_
- mux/mp3_to_mp4_audio (mp3_xing.mp3 to MP4 with MP3 frames as mp4a.6b samples): baseline aibrush 10.425 ms vs mediabunny 7.505 ms vs ffmpeg 9.545 ms; after route change aibrush 3.880 ms vs mediabunny 6.250 ms vs ffmpeg 11.840 ms; active deficits fell 161->160.  _(src: docs/notes/session9-mp3-prepared-mp4-mux.md)_
- mux/opus_to_ogg: after fixing the parser it passed but was slow at aibrush 14.545 ms vs mediabunny 8.350 ms; closed at aibrush 9.655 ms vs mediabunny 11.765 ms vs ffmpeg.wasm 15.965 ms.  _(src: docs/notes/session9-ogg-prepared-packet-mux.md)_
- mux/opus_to_webm_audio closed at aibrush-media 5.765 ms median vs mediabunny 7.540 ms vs ffmpeg.wasm 15.805 ms (all PASS property-invariant, n=9).  _(src: docs/notes/session9-ogg-audio-webm-mux-fast-path.md)_
- mux/opus_to_webm_audio closing Chromium run: aibrush-media 5.765 ms median vs mediabunny 7.540 ms vs ffmpeg.wasm 15.805 ms (n=9), all PASS property-invariant.  _(src: docs/notes/session9-ogg-audio-webm-mux-fast-path.md)_
- mux/pcm_s16_to_wav pre-fix: aibrush-media 110.4 ms vs mediabunny 4.0 ms on the same passing probe-duration oracle; after fast-path fix, focused all-engine n=5: aibrush-media 6.550 ms, mediabunny 6.825 ms, ffmpeg.wasm 47.765 ms.  _(src: docs/notes/session9-wav-pcm-mux-fast-path.md)_
- mux/pcm_s16_to_wav: stored aibrush 110.4 ms vs mediabunny 4.0 ms; closed on all-engine Chromium 149 overlay at aibrush 6.550 ms vs mediabunny 6.825 ms vs ffmpeg.wasm 47.765 ms.  _(src: docs/notes/session9-wav-pcm-mux-fast-path.md)_
- Native packet fusion (ADR-282) median fused mux+Blob+reprobe times: 5.005 ms for the 31.42 MiB baked H.264+ADTS input, 1.936 ms MOV shape, 0.397 ms tiny, 0.646 ms VFR, 0.507 ms ordinary H.264; the fused route constructs zero Encoded*Chunk host objects versus the generic path's per-packet construction.  _(src: docs/notes/session13-native-packet-fusion.md)_
- Ogg-to-Matroska remux (ADR-255) public Blob medians are already 0.063-0.065 ms (Opus) and 0.128-0.133 ms (Vorbis); a 256-operation pass saw a positive 6,485,376-byte ArrayBuffer peak with only 0-19,796 bytes retained after forced collection, so the row stays BEHIND only pending same-export browser rival wall/memory.  _(src: docs/notes/session13-ogg-matroska-public.md)_
- On a 900-frame 1080p/30 control, realtime latencyMode measured 6,331.7 ms versus forced quality 11,856.1 ms (46.6% same-run win); on a 377-frame VFR/B-frame control 2,685.3 vs 4,353.7 ms (38.3%).  _(src: docs/notes/session13-h264-implicit-realtime.md)_
- On real mp4s the demux gap is aibrush ~27ms vs mediabunny ~9ms; profiling showed mp4PacketInfoFromBytes is fast in Node (0.335ms for 4.7MB/671-packet, 0.079ms for 700KB/201-packet, ~2000-2500 pkt/ms) so the browser gap is fetch/IO range-request round-trips inside the measured window, not the pure-TS parse.  _(src: docs/perf/goal26-evidence.md)_
- On the 1,144,400,182-byte two-hour H.264 base, the optimized in-memory arm made seven reads requesting 1,156,851,704 bytes with peak RSS 2,075,131,904 bytes and retained ArrayBuffers 1,144,400,294 bytes; the unchanged range-backed control used 142 reads, 1,150,414,286 requested bytes, and 97.115 ms, producing identical output.  _(src: docs/notes/session13-mp4-avc-in-memory.md)_
- On the 4K row (74,425,089 bytes), the first qualified same-export post-change rotation improved aibrush from 126.400 to 70.320 ms (MAD 0.680) while mp4box passed at 38.865 ms (MAD 4.700), so the batching win is retained but the 4K row remains BEHIND.  _(src: docs/notes/session13-mp4-packet-stream-batching.md)_
- On the exact ordinary VP9→AV1 shape, current implicit rate control measured 1645.1 ms vs prior local quality-mode 2059.6 ms and passing rival 1913.135 ms; the exact VP9-alpha shape measured 266.7 ms vs a public baseline of 1197.965 ms; the old same-export ledger reported VP9→AV1 2255.710 vs 1913.135 and HEVC→AV1 2275.900 vs 1885.705.  _(src: docs/notes/session13-video-rate-alpha.md)_
- pcm_s16_to_wav benchmarks were run on Chromium 149; after the fix backlog reported 313 active deficits (0/16/86/211 by severity).  _(src: docs/notes/session9-wav-pcm-mux-fast-path.md)_
- performance-deficits (auto-generated from 193 public result exports, freshness window 24h): the only active wall-time loss was audio-dsp/throughput_decode_s24 on rotated:03.wav — aibrush 58.3 ms vs mediabunny 27.7 ms (2.11x); 12 functional reds, 0 peak-memory losses, 0 coverage gaps.  _(src: docs/perf/performance-deficits.md)_
- performance/size-ladder-iterate-packets-massive requires preserving all 553,501 payload-free packet sizes through the public output of a two-hour MP4; aibrush's table matches independent ffprobe truth.  _(src: BUILD_INSTRUCTIONS_SESSION12.md)_
- Post-budget-refactor gate passes all 3,292 tests but reported 89.9323% branch coverage against the binding 90% floor — a shortfall of exactly twelve covered branches.  _(src: docs/notes/session12-branch-coverage-recovery.md)_
- probe/longform_1h_audio selected rotation took 70.375 ms because metadata probe parsed a complete 699-byte init moov, saw empty sample tables plus mvex, then range-read the entire 65,765,571-byte file only to rediscover the mvhd/mdhd-declared duration.  _(src: docs/notes/session11-fragmented-audio-init-probe.md)_
- probe/opus closed at aibrush-media@dev 2.320 ms median vs mediabunny@1.48.0 3.690 ms vs ffmpeg.wasm@0.12.15 6.785 ms (all golden-metadata PASS); an intermediate build had lost at aibrush 9.665 ms vs mediabunny 4.980 ms.  _(src: docs/notes/session9-ogg-opus-probe-fast-path.md)_
- probe/realworld_mdn_trex_mp3: baseline aibrush 14.555 ms vs mediabunny 2.595 ms; a prefix cache alone reached 6.145 ms and adding an MP3 probe() hook only reached 6.725 ms (browser URL/range overhead dominated), until the public probe prefix cache closed it at 0.330 ms median.  _(src: docs/notes/session9-public-probe-mp3-prefix.md)_
- proof-session12-hls (headed Chromium 2026-07-11) via createMedia().probe() on probe/hls_aes128/hls_aes128.m3u8 fetched the real key and five encrypted TS segments, decrypted/stitched, and reported ts / H.264 / AAC / 10.0266666667 s; bench-session12-hls (warmup 2, n=5) median resolve/decrypt/stitch 3.861 ms, output 4,598,668 bytes, SHA-256 27d7492e...db48.  _(src: docs/notes/session12-public-truth-boundaries.md)_
- proof-session12-video real H.264 two-pass on transcode/h264_two_pass_bitrate/01.mp4: five fresh samples median 2,802.3 ms, all outputs shared SHA-256 e768d3f0...814d, output facts avc1.64001F 1280×720 12.84 s 321 packets 810,678 video-payload bytes; cancellation returned 'aborted'. The requested HEVC Main10 target built exactly as hev1.2.4.L120.B0 but was classified NA_BROWSER (typed capability-miss) because Chromium had no driver for that encoder config.  _(src: docs/notes/session12-public-truth-boundaries.md)_
- pure-TS audio-dsp baseline (bun 1.3.x, median of 200 iters, 8 WAV files): remix mono→stereo ~85,800x realtime, remix stereo→mono (BS.775) ~33,700x, biquad highpass ~5,350x, gain -6dB ~2,850x, encode planar→s16 ~1,660x; resample (band-limited windowed-sinc, ADR-022) is the heaviest at ~270x geomean / ~146x worst, confirming it ships in-tier without a WASM soxr.  _(src: docs/architecture/14-benchmarks.md)_
- pure-TS colorspace kernel baseline (bun 1.3.14): rgba 709→2020 ~13.27 ms / ~7.6 MP/s, 2020→709 ~12.52 ms / ~8.0 MP/s; aggregate ~7.7 MP/s geomean / ~3.4 MB max peak RSS; fluent chain façade aggregate ~78,400 chains/sec geomean; preload aggregate ~20,900 warmups/sec geomean.  _(src: docs/architecture/14-benchmarks.md)_
- pure-TS containers baseline (bun 1.3.x, median of 21 iters): probe (header read) ~0.037 ms / ~26,700 probes/s; demux (table+gather) ~0.022 ms / ~196.2 MB/s; remux→mp4 ~0.30 ms / ~36.0 MB/s; remux→ts ~0.56 ms / ~313.2 MB/s; decrypt cenc ~0.43 ms / ~14.9 MB/s; decrypt hls-sample-aes ~6.35 ms / ~136.2 MB/s; mux→ogg ~7.92 ms / ~20.1 MB/s.  _(src: docs/architecture/14-benchmarks.md)_
- pure-TS image-probe baseline (bun 1.3.14, median of 7 batch means, 4000 probes/batch): test.jpeg 239x178 ~9,549,000 probes/s, test.png 100x100 ~3,181,000/s, test.webp 274x367 ~4,249,000/s, anim2.gif 480x360 36 frames 0.820s ~265,000/s, test.avif 100x100 ~608,000/s; aggregate ~1,835,000 probes/s geomean.  _(src: docs/architecture/14-benchmarks.md)_
- remux/flac_seektable_flac_to_ogg: baseline aibrush 11.720 ms vs ffmpeg.wasm 7.865 ms; teaching OggMuxer.write() to prefer Packet.data improved it to 8.385 ms (ffmpeg 6.880 ms same run); final streamCopyTargets route closed at aibrush 10.105 ms vs ffmpeg 10.330 ms.  _(src: docs/notes/session9-flac-ogg-streamcopy-target.md)_
- Router cost buckets mark a stage tiny when inputBytes <= 64 KiB, outputPixels <= 4096, mediaSeconds <= 1, or audioFrames <= 48000; video filters are tiny only when videoPixelWork <= 245,760 (an identity 64x64 source-read plus destination-write over 30 frames).  _(src: docs/architecture/04-capability-router-and-ladder.md ADR-020)_
- Session 1 reached ~80% of the Definition of Done with main green; eager bundle was 37.8 kB against the ≤50 kB budget, and ~1436 node tests passed.  _(src: BUILD_INSTRUCTIONS_SESSION2.md)_
- Session 11 baseline: WebKit stood at 428/0/0 (PASS/FAIL/ERROR); active speed deficits collapsed 289→22 on the fresh 2026-07-08 base, with tiers 0 catastrophic · 2 severe · 1 moderate · 19 minor · 1 lone timeout, and 18 functional reds remaining.  _(src: BUILD_INSTRUCTIONS_SESSION11.md)_
- Session 11 CENC decrypt fair-harness tail regression: 171.5 ms vs 30.3 ms rival for decrypt and 83.9 ms vs 26.8 ms for the clear-equivalence property (targeted for reduction).  _(src: docs/notes/session11-cenc-prepared-key-parallelism.md)_
- Session 11 eager codec-frame split: combined eager default-entry closure settled at 49.68 KiB vs 51.26 KiB pre-split build, leaving 0.32 KiB below the 50 KiB kernel ceiling; 339 focused tests pass. Raw PCM emitted in bounded 4,096-frame chunks in f32-planar layout.  _(src: docs/notes/session11-eager-codec-frame-split.md)_
- Session 11 speed tail: size-ladder-iterate-packets-huge 19.5× vs remotion-webcodecs; prop_webm_headerless_duration_materialized 10.2× vs mediabunny; demux/flac_noseektable 3.6× vs remotion-webcodecs; minor tail e.g. metadata/meta_consistent_mp4_to_mkv 2.76×, streaming-output/mp4_fragmented_cmaf 2.12×.  _(src: BUILD_INSTRUCTIONS_SESSION11.md)_
- Session 12 completed full Chromium export chromium-2026-07-11T14-55-47-618Z.json: 563 rows = 499 PASS, 57 NA_ASSET, 2 NA_BROWSER, 2 NA_ENGINE, 2 FAIL, 1 ERROR; the retained cross-export correctness board held 8 FAIL + 1 ERROR = 9 reds.  _(src: BUILD_INSTRUCTIONS_SESSION12.md)_
- Session 12 earlier full Chromium export chromium-2026-07-10T23-25-04-841Z.json had 563 rows: 482 PASS, 20 FAIL, 2 ERROR, 59 N/A; a broad 07-11T02-43 export had 545 rows (479 PASS, 4 FAIL, 2 ERROR, 60 N/A) but the launcher stopped after 1,800,000 ms.  _(src: BUILD_INSTRUCTIONS_SESSION12.md)_
- Session 12 H.264 two-pass retains analysis evidence packed to nine bytes per frame; HEVC Main10 output authors the codec string hev1.2.4.L120.B0 with 8-bit→10-bit encoder widening (no pixel copy).  _(src: BUILD_INSTRUCTIONS_SESSION12.md)_
- Session 12 one qualified contested wall loss: on 03.wav aibrush 58.270 ms vs Mediabunny 27.655 ms (2.107×); the deficit report was generated from 193 public result exports and showed 176 correctness-rotation gaps and 175 bake-blocked rotations.  _(src: BUILD_INSTRUCTIONS_SESSION12.md)_
- Session 12 perf fusions: BS.775 5.1→mono downmix fused to 2.27× faster (two temporary planes removed); the common stereo rational polyphase resampler went 1.570 ms→1.105 ms on one second of 48→44.1 kHz audio with bit-identical mono-path parity.  _(src: BUILD_INSTRUCTIONS_SESSION12.md)_
- Session 13 FLAC public-layer attribution on a 30,105-byte/8-frame source: fused parser 0.0038 ms, reused public engine 0.0121 ms, fresh engine after module cache 0.0672 ms, and URL-like source with injected 3 ms latency 3.7732 ms making exactly one range request transferring exactly 30,105 bytes and opening zero streams.  _(src: docs/notes/session13-flac-packet-info-fused.md)_
- Session 2's last fresh chromium leaderboard: 214 PASS / 96.4% conformance / 39.8% coverage, versus mediabunny at 91.4% coverage; 336 scenarios were NA and the engine tied 100% conformance in 9 of 13 families but won none.  _(src: BUILD_INSTRUCTIONS_SESSION2.md)_
- Session 5 starting chromium run (2026-06-26): 436 PASS / 1 FAIL / 1 ERROR / 113 NA_ENGINE / 10 NA_BROWSER of 561 = 99.54% conformance, 78.07% coverage, rank #2 (mediabunny 100%/91.4%).  _(src: BUILD_INSTRUCTIONS_SESSION5.md)_
- Session 6 final chromium evidence (2026-06-28): 555 PASS / 4 NA_ENGINE / 2 NA_BROWSER / 0 FAIL / 0 ERROR.  _(src: BUILD_INSTRUCTIONS_SESSION6.md)_
- Session 6 final gate: 142 test files / 2215 tests; coverage 92.24% statements, 90.01% branches; budgets 49.95/50.00 kB eager and 246.18/256.00 kB first-op; anti-cheat 45/45.  _(src: BUILD_INSTRUCTIONS_SESSION6.md)_
- Session 6 starting chromium run (2026-06-27 14:17): 518 PASS / 0 FAIL / 36 NA_ENGINE / 7 NA_BROWSER = 100% conformance, 92.3% coverage — up +104 PASS / −105 NA_ENGINE from 06-26 (Session 5 flipped 105 cells NA→PASS with 0 regressions).  _(src: BUILD_INSTRUCTIONS_SESSION6.md)_
- Session 7 cross-browser start: WebKit only 153/561 captured (124 PASS / 14 FAIL / 8 ERROR / 4 NA_ENGINE / 3 NA_BROWSER; 408 never ran, stopped at transcode/ladder_tiny_h264_360p_resize_180p); Firefox 51/561 (40 PASS / 2 FAIL / 8 NA_ENGINE / 1 NA_BROWSER; 510 never ran, stopped at robustness/fuzz_adts_aac_bitflip_probe).  _(src: BUILD_INSTRUCTIONS_SESSION7.md)_
- Session 8 AVI mux benchmark (scripts/bench-containers.ts, 5 real-packet cases) fresh local baseline: geomean 226.0 MB/s, worst 123.6 MB/s, max peak RSS 0.16 MB.  _(src: docs/notes/session8-avi-mux.md)_
- Session 8 metadata-write benchmark local run: write_wav_info_bext across 8 files at 0.254 ms median / 1952.84 MB/s; write_aiff_tags across 5 files at 0.252 ms / 1232.63 MB/s; write_caf_info across 5 files at 0.053 ms / 5512.02 MB/s; checksum 142218504.  _(src: docs/notes/session8-metadata-write.md)_
- Session 8 start (2026-06-30, chromium): #1 with 557 PASS / 0 FAIL / 0 ERROR / 7 NA — the 7 NA = 4 signed-off honest-NA (2× MP3 encode, HEVC Main10, H.264 two-pass) + 3 phantom retired IDs (image_*_probe_na).  _(src: BUILD_INSTRUCTIONS_SESSION8.md)_
- Session 9 deficits per family (count · worst ratio): probe 49·397×, transcode 45·4×, mux 43·28×, demux 35·1152×, decode-seek 35·8×, audio-dsp 26·7×, performance 23·409×, trim 23·24×, remux 19·7×, metadata 16·8×, streaming-output 8·3×, encryption 3·1×.  _(src: BUILD_INSTRUCTIONS_SESSION9.md)_
- Session 9 fixed-per-op overhead examples on tiny inputs: mux/pcm_s16_to_wav (header + copy) us 110 ms vs mediabunny 4 ms; probe/flac_seektable us 38 ms vs rivals 2–7 ms.  _(src: BUILD_INSTRUCTIONS_SESSION9.md)_
- Session 9 timing export (2026-07-01, chromium, all 8 engines carry bench.wall): of 444 scenarios where aibrush and ≥1 rival both PASS the identical oracle, a rival was faster in 325 (73%) — 6 catastrophic (≥100×), 21 severe (10–100×), 87 moderate (3–10×), 211 minor (<3×).  _(src: BUILD_INSTRUCTIONS_SESSION9.md)_
- Session 9 worst same-work speed deficits: demux/size_massive_massive_h264_1080p_2h 46,383 ms vs 40 ms (1152×); performance/size-ladder-iterate-packets-massive 409×; probe/perf-extract-metadata-massive 17,480 ms vs 44 ms (397×); size-ladder-extract-metadata-massive 345×; probe/massive_vp9_1080p_2h 336×; probe/massive_h264_1080p_2h 236×.  _(src: BUILD_INSTRUCTIONS_SESSION9.md)_
- Session13 demux/size_massive_massive_h264_1080p_2h is a 33.159x loss (aibrush 1349.035 ms vs remotion-webcodecs 40.685 ms): the 553,501-row packet table is cheap (ADR-269) but the public demux drain constructs immutable native chunks for 1.14 GiB and the consumer performs 553,501 read()/await steps; ADR-278 amortizes with a zero-HWM 256 KiB/256-packet batch, larger batches rejected because they break post-delivery abort.  _(src: docs/perf/session13-speed-ledger.md)_
- Session13 mux/audio_only_aac_to_mp4 wins (2.650 vs mediabunny 3.740 ms), performance/size-ladder-iterate-packets-large loses to mp4box (71.285 vs 45.550 ms), performance/op-sweep-demux loses to mediabunny (57.805 vs 37.930 ms), decode-seek/decode_extreme_fps_240 loses to remotion-webcodecs 2.790x (332.560 vs 119.180 ms).  _(src: docs/perf/session13-speed-ledger.md)_
- Session13 mux/video_plus_audio_to_mp4 is a 4.165x loss (aibrush 201.130 ms vs mediabunny 48.295 ms) and mux/swap_audio_video_with_opus_to_mkv is 3.611x (202.890 vs 56.185); ADR-257's exact native product is only 19.600 ms, so the gap is the public boundary constructing per-packet host objects (ADR-287 copied-descriptor miss).  _(src: docs/perf/session13-speed-ledger.md)_
- Session13 transcode/vp9_alpha_to_vp9_keepalpha is a 0.106x ratio win (aibrush 17.490 ms vs mediabunny 164.945) via ADR-270 semantic copy; transcode/aac_to_opus_webm wins wall (56.190 vs 82.900 ms) but loses memory (peak 32,421,157 B vs remotion 25,392,133 B).  _(src: docs/perf/session13-speed-ledger.md)_
- Stereo polyphase resampler specialization on 1 s 48→44.1 kHz stereo, 21 samples after 3 warmups: 1.570 ms → 1.105 ms (1.42×), 637× → 905× realtime; checksum stayed exactly 8.887679169; 19 focused resampler tests pass.  _(src: docs/notes/session12-stereo-resample-design.md)_
- The `inline:true` build flag (base64 a small WASM module into its lazy chunk) increases size by ~+33% and loses streaming compile; it is a single-file/strict-CSP escape hatch, not a default.  _(src: docs/architecture/08-packaging-and-loading.md)_
- The acceptance benchmark harness (../media-test/media-browser-test) originally had 558 features across 13 families (transcode, robustness, mux, probe, remux, demux, decode-seek, trim, audio-dsp, performance, streaming-output, metadata, encryption); the live suite grew to 561 (Session 5–7) and then 563 cells (Session 12).  _(src: BUILD_INSTRUCTIONS.md)_
- The fair harness op timeout is DEFAULT_OP_TIMEOUT_MS = 120000 ms per op and per oracle; some scenarios raise it (e.g. ladder_large… to 310 s). A timeout or OOM is scored as an ERROR (functional failure), strictly worse than a speed deficit.  _(src: BUILD_INSTRUCTIONS_SESSION10.md)_
- The full aggregate gate passed: strict typecheck, Biome over 541 files, 6 deficit-generator tests, 183 Vitest files with 3,286 tests at 92.01% lines / 90.02% branches coverage, production build, vendored-WASM checks, 10 distribution smoke tests, 49.36 kB eager and 220.80 kB typical bundle budgets, clean packed install, and all 45 anti-cheat assertions.  _(src: docs/notes/session12-gate-exit.md)_
- The fused BS.775 5.1->mono downmix kernel improved a 10-second/48 kHz benchmark from 1.084 ms to 0.477 ms (2.27x) and removed 16 temporary bytes per input frame (ADR-223).  _(src: docs/architecture/09-operations.md ADR-223)_
- The fused stereo polyphase resample improved 48->44.1 kHz work from 1.570 ms to 1.105 ms per second of stereo audio (1.42x), bit-exact vs two independent mono resamples (ADR-224).  _(src: docs/architecture/09-operations.md ADR-224)_
- The latest completed Chromium export contained 57 NA_ASSET rows but only two NA_ENGINE rows; 51 conversion-family rows rejected every exact source because their input requirements demanded the output codec (e.g. an H.264-to-VP9 conversion requiring VP9 in its H.264 input).  _(src: docs/notes/session12-catalog-input-requirements.md)_
- The MPEG-TS ADTS deframer stays a streaming single-pass scan at ~540 MB/s on the 30s fixture (bench-mpegts-demux.ts), guarding against O(n^2)/per-frame-copy regressions.  _(src: docs/notes/mpegts-aac-deframe.md ADR-184)_
- The package.json verify:package gate requires the measured eager probe-only bundle to stay within the 50 kB kernel budget and emit zero WASM assets when packed, installed into a fresh app, and browser-bundled.  _(src: docs/architecture/08-packaging-and-loading.md ADR-111)_
- The shared unwrapPackets() TransformStream wrapper measured 0.809 ms versus 0.326 ms for a direct high-water-mark-zero pull projection over 1,296 rows; complete byte-backed MP4 parsing plus packet-info was only 0.014-0.164 ms (27-671 packets), ruling out parsing as a ~7.2 ms browser deficit.  _(src: docs/notes/session13-audio-fixed-cost.md)_
- The terminal-collector single-chunk optimization (adopt an exact-owned full ArrayBuffer without re-copy) measured 0.009958 ms median with zero output allocation versus 0.505875 ms with an 8 MiB allocation for the always-copy control on an 8 MiB payload; a single subview or SharedArrayBuffer view still copies.  _(src: docs/notes/session13-terminal-collector.md)_
- Total eager JS for a typical app targets mediabunny-class ~150-250 kB.  _(src: docs/architecture/01-goals-and-requirements.md)_
- transcode/aac_to_pcm_wav_extract (aac_adts.aac, 163,811 bytes to WAV pcm-s16): closing fresh Chromium n=5 medians were aibrush-media 28.460 ms PASS vs ffmpeg.wasm 31.995 ms PASS, both with durationDeltaSec=0.0043333333333333.  _(src: docs/notes/session9-adts-aac-direct-wav-s16.md)_
- transcode/aac_to_pcm_wav_extract on aac_adts.aac (163,811-byte raw ADTS AAC-LC): pre-fix aibrush-media 52.865 ms median vs ffmpeg.wasm 24.440 ms; both passed property-invariant with durationDeltaSec=0.0043333333333333.  _(src: docs/notes/session9-adts-aac-direct-wav-s16.md)_
- transcode/opus_to_aac_mp4 closed at aibrush-media 99.835 ms median vs mediabunny 101.110 ms, aibrush reporting 100.235x median realtime throughput, 28,406,438 bytes median peak memory, and 0 ms median long tasks (durationDeltaSec=0.08366666666666767, tolerance 0.12).  _(src: docs/notes/session9-opus-aac-mp4-audio-pipeline.md)_
- transcode/wav_to_flac (wav_s16.wav, 5s stereo 48kHz s16 to native FLAC): pre-fix aibrush 118.230 ms vs ffmpeg.wasm 48.035 ms; closed at aibrush 28.805 ms (173.581x realtime, peak 29,959,494 B) vs ffmpeg 48.410 ms (103.284x).  _(src: docs/notes/session9-wav-s16-flac-verbatim-fast-path.md)_
- transcode/wav_to_flac (wav_s16.wav: 5s stereo 48kHz PCM s16 to native FLAC) pre-fix aibrush-media 118.230 ms vs ffmpeg.wasm 48.035 ms; after direct verbatim-FLAC fix aibrush-media 28.805 ms (173.581x, peak 29,959,494 B) vs ffmpeg.wasm 48.410 ms (103.284x).  _(src: docs/notes/session9-wav-s16-flac-verbatim-fast-path.md)_
- trim/audio_aac_adts_copy (raw ADTS trimmed 2s->7s) closed at aibrush-media 0.480 ms median (n=9), down from a stored 27.8 ms backlog vs ffmpeg.wasm 6.0 ms; outDurationSec=5.034666666666666 for requestedDurationSec=5.  _(src: docs/notes/session9-adts-url-trim-cache.md)_
- trim/audio_opus_ogg_copy: baseline aibrush 13.500 ms vs mediabunny 9.850 ms; closed at aibrush 7.160 ms vs mediabunny 12.615 ms, with aibrush throughput 1397.626x realtime (mediabunny 793.262x) and peak memory 27,666,391 B (mediabunny 28,187,649.5 B); outDurationSec=5.0135.  _(src: docs/notes/session9-ogg-opus-native-trim.md)_
- trim/audio_wav_pcm_copy first close: aibrush 4.76 ms median vs mediabunny 4.85 ms vs ffmpeg.wasm 28.315 ms (local sanity timing 0.276875 ms median on the same fixture).  _(src: docs/notes/session9-wav-pcm-trim-byte-slice.md)_
- trim/audio_wav_pcm_copy reopened (clean 960 KiB WAV URL): warmup1 aibrush 6.280 ms vs mediabunny 6.240 ms; warmup3 aibrush 7.025 ms vs mediabunny 4.555 ms; after the wavTrimFromUrl raw-byte cache, aibrush 0.595 ms vs mediabunny 8.785 ms with aibrush 8403.361x realtime throughput.  _(src: docs/notes/session9-wav-url-trim-cache.md)_
- Video-filter pure-planner benchmark: 100,000 mixed rate/filter/work plans, nine samples, 112.734ms median, 2.69MiB RSS delta on Bun 1.3.14; 170 focused tests pass.  _(src: docs/notes/video-filter-total-pixel-routing.md)_
- VideoToolbox temporal-layer configs L1T2/L1T3 at 2Mb/s scored worse full-clip (SSIM 0.917416 / 0.916516) than plain High (0.954306); the first-frame temporal-layer improvement reverses across the full clip; VideoToolbox Main scored 0.952113. Meeting the 0.95 gate needs roughly twice the requested bitrate.  _(src: docs/notes/h264-2mbps-quality-bound.md ADR-193)_
- WASM codec-core pins and sizes: libopus xiph/opus v1.5.2 BSD-3 ~400-500kB; libvpx v1.14.1 BSD-3 ~1-2MB; dav1d 1.4.x BSD-2 ~400-700kB (8-bit); rav1e v0.8.0 BSD-2 ~1.5-3MB; libvorbis v1.3.7 + libogg v1.3.5 BSD-3 ~300-500kB; LAME 3.100 LGPL ~250kB / Shine ~80kB.  _(src: docs/notes/wasm-codec-cores.md)_
- WASM SIMD+threads via SharedArrayBuffer gives ~order-of-magnitude speedups on the exotic codecs, but only when the page is cross-origin isolated (opt-in via enableThreads, default = crossOriginIsolated).  _(src: docs/architecture/06-execution-and-runtime.md)_
- WAV same-layout multipart re-authoring (multipart Blob of 44-byte canonical header + PCM view vs contiguous rewrite) measured 0.015 vs 0.041 ms (2.71x, spanning 2.08-3.14x across runs), eliminating 960,000 bytes of temporary JS output allocation; baseline row audio-dsp/meta_idempotent_resample_same_rate was aibrush 4.380 ms vs mediabunny 2.970.  _(src: docs/notes/session13-wav-identity-copy.md)_
- WebCodecs audio/video codec backpressure awaits the dequeue event while decode/encode QueueSize >= 8 (bounded high-water mark).  _(src: docs/architecture/09-operations.md)_
- WebCodecs decoder startup barrier (fresh configure()+empty flush()) measured only 0.7-3.7 ms across four real inputs; RGBA copyTo() readback dominates every bounded visual row (25.9-408.9 ms medians) and is browser consumer work, not attributed to the product decoder.  _(src: docs/notes/session13-webcodecs-decoder-startup.md)_
- Widening the adapter's containersOut to webm/mkv/ogg raised remux from 7→25 PASS and mux from 8→14 PASS (+24 cells total).  _(src: BUILD_INSTRUCTIONS_SESSION2.md)_

## Browser / runtime quirks  (55)

- A Chromium page with the public package proved: the full scenario HLS playlist URL decrypts and probes H.264 1280x720 + AAC 48kHz stereo; at page-root resolution hls_aes128.key resolves to a 30,228-byte HTML fallback (16-byte key expected) and rejects; the scenario and server-root playlists share basenames but differ in SHA-256 for manifest, key, and all five segments (ADR-192).  _(src: docs/notes/hls-aes128-iv.md ADR-192)_
- ADR-007: hardware decode is GPU/platform-specific and 'bit-exact' benchmark wins there are M1-specific, so determinism defaults to 'auto' (hardware allowed) with 'force-software' dropping hardware/GPU tiers for cross-machine-reproducible output.  _(src: docs/architecture/02-decision-records.md ADR-007)_
- ADR-013: probe never uses <video> loadedmetadata because it is 600–7000× slower than reading bytes; <video>/<audio> input defaults to bytes mode reading currentSrc.  _(src: docs/architecture/02-decision-records.md ADR-013)_
- ADR-026: the WebCodecs VideoDecoder spec guarantees output in presentation order (the UA does the reorder), so the driver adds no live PTS reorder buffer (which would be redundant and unbounded); backpressure uses HIGH_WATER_MARK/BACKPRESSURE_THRESHOLD of 8; audio maps auto→'no-preference' (no platform bit-drift worth a hardware pin) while video maps auto→'prefer-hardware'.  _(src: docs/architecture/02-decision-records.md ADR-026)_
- ADR-039: Vitest's V8-coverage instrumentation corrupts the wasm-bindgen heap-object table inside the worker, so the real AAC decode oracle runs in a clean Node child process (decode-fixture.mjs); the wasm-bindgen geometry getters must be read once and cached (re-reading interleaved with decode corrupts the glue's heap-object table on Node).  _(src: docs/architecture/02-decision-records.md ADR-039)_
- ADR-080: advertising hev1.* (which expects inline parameter sets, array_completeness=0) to a Matroska HEVC bitstream carrying no in-band VPS/SPS/PPS makes some WebCodecs decoders wait for parameter sets that never arrive and emit a 0×0 frame; the fix expands bare hevc/h265 to hvc1.* (out-of-band, most broadly decodable) when an hvcC description is present. Public hevc encode maps to Main 8-bit hev1.1.6.L93.B0; Main10/non-Main encode is a typed capability miss (no software HEVC encoder).  _(src: docs/architecture/02-decision-records.md ADR-080)_
- ADR-084: Chromium accepts a tiny H.264 encode configured below Level 3.0, but the resulting MP4 then fails the platform <video> seek/decode path; so h264CodecStringForDimensions applies a Level 3.0 (level_idc=0x1e, e.g. avc1.42E01E) compatibility floor while the pure Annex-A helper still returns L1.3 for 320×180.  _(src: docs/architecture/02-decision-records.md ADR-084)_
- ADR-110: Firefox committed-golden RGBA digest rows (e.g. performance/decode-fps, metadata/write_mkv_tags) fail the strict pixel oracle across every engine, proving Firefox committed-golden pixel comparability is the issue (not aibrush output), so those are classified NA_BROWSER rather than false failures.  _(src: docs/architecture/02-decision-records.md ADR-110)_
- ADR-110: Firefox WebCodecs Opus encode times out at the 120 s cap in the full matrix (transcode/flac_to_opus_webm ran 131650 ms, next mp3_to_opus_webm 121201 ms), so Firefox routes Opus encode through the wasm-opus tail normalized to 48 kHz; Firefox native AAC AudioDecoder can also hang after prior transcode rows, so ADTS AAC→WAV extraction is forced through wasm-AAC on Firefox.  _(src: docs/architecture/02-decision-records.md ADR-110)_
- ADR-110: Firefox WebGPU resize measured SSIM 0.9694 < 0.97, so the root declines the WebGPU filter rung on Firefox and uses Canvas2D with high-quality smoothing (SSIM min 0.970591); Firefox VP9 output is declined when source duration ≥5 s and output ≥640×360 (times out at the 120 s op cap on a 30 s 1920×1080 fixture, and even a 5 s 640×360 VP9 output).  _(src: docs/architecture/02-decision-records.md ADR-110)_
- ADR-110: WebKit playback-smoke rejects MOV output authored with QuickTime major brand 'qt' for an ISO-BMFF layout but accepts the same structure with an ISO/MP4-compatible ftyp brand set; WebKit also cannot validate Matroska output through a plain <video> element even when structurally authored (MKV playback-smoke rows stay NA_BROWSER).  _(src: docs/architecture/02-decision-records.md ADR-110)_
- ADR-111: MP3-in-MP4 aliases mp4a.6b and mp4a.69 are tokenized as MP3 before the broad AAC mp4a.* branch so preserve-source routing does not misclassify them as AAC; the vendored WASM fallbacks decline exact configs their cores can't satisfy (AV1 only 8-bit/4:2:0/non-monochrome; VP8/VP9 only 8-bit 4:2:0, decode-only).  _(src: docs/architecture/02-decision-records.md ADR-111)_
- ADR-214: a real Chromium HDR10 VideoFrame decodes with source format null and Chromium rejects both allocationSize({format:'RGBA'}) and copyTo(...,{format:'RGBA'}) for that opaque frame, while Canvas2D drawImage(VideoFrame) performs the browser's display-managed HDR→SDR conversion; so on Chromium the CPU filter declines tonemap and the Canvas2D driver owns it.  _(src: docs/architecture/02-decision-records.md ADR-214)_
- Bit-exact decode wins in the benchmark are GPU/platform-specific (e.g. M1 ANGLE), so cross-machine determinism requires force-software rather than trusting hardware output.  _(src: docs/architecture/09-operations.md)_
- Both aibrush-media and FFmpeg emit valid HEVC-in-Matroska with preserved packet/frame/HDR facts, but Chromium's <video> path reports zero intrinsic size for HEVC-in-MKV — a platform decoder boundary, not a product bug.  _(src: BUILD_INSTRUCTIONS_SESSION12.md)_
- Both transcode/aac_to_mp3_mp4 and transcode/wav_to_mp3_mp4 returned NA_BROWSER because AudioEncoder.isConfigSupported=false for 'mp3' in Chromium (2026-07-11); ADR-105 remains the governing explicit-license boundary and no encoder tail/fallback was invented.  _(src: docs/notes/session12-public-truth-boundaries.md)_
- Browsers cannot encode MP3 via WebCodecs (no MP3 encoder), and WebCodecs also has no Vorbis encoder — software tails are required for MP3/Vorbis authoring.  _(src: BUILD_INSTRUCTIONS_SESSION4.md)_
- Bun's Blob.stream() delivers chunks as large as 2 MiB, making its positive peak 500,288 bytes worse than the 1-MiB range window; this caused Blob-based sequential WAV decode to be rejected in favor of range/stream delivery.  _(src: docs/notes/session13-wav-memory-lifecycle.md)_
- CAF chunk sizes are signed 64-bit big-endian and CAF data may be the final chunk with a -1 size, requiring special handling in the metadata writer.  _(src: docs/notes/session8-metadata-write.md)_
- Chrome 149 WebCodecs has no FLAC decoder at all; the fallback is a shipped pure-TS FLAC decoder (ADR-024) rather than WASM.  _(src: docs/architecture/10-browser-capability-matrix.md)_
- Chrome AV1 encode is hardware-only, so transcode/h264_to_av1_mp4 shows NA_BROWSER on non-AV1-HW Macs; the fix was a permissive rav1e wasm software encoder.  _(src: BUILD_INSTRUCTIONS_SESSION5.md)_
- Chromium 149's measureUserAgentSpecificMemory (UA-specific memory API) rejects every call with SecurityError in the benchmark harness, so native/GPU memory is unavailable and was never coerced to zero across Session 13 memory investigations.  _(src: docs/notes/session13-vfr-decoder-surface-memory.md)_
- Chromium can natively suppress AAC encoder-delay/priming (represented by an MP4 presentation edit) before emitting AudioData, so an unaware pipeline would trim the same leading samples twice — Session 11/12 added a bounded native-suppression preflight (ADR-213).  _(src: BUILD_INSTRUCTIONS_SESSION12.md)_
- Chromium does not support explicit decoded VideoFrame surface conversion to NV12 and rejects explicit I420 copy; passing the exposed non-RGB natural format as a copyTo option is also forbidden, which blocked the compact-detachment memory optimization for decode-only output.  _(src: docs/notes/session13-vfr-decoder-surface-memory.md)_
- Chromium HDR decoders may expose an opaque/null-format VideoFrame that rejects `copyTo`, so on Chromium the CPU filter declines tonemap and the Canvas2D display-managed drawImage(VideoFrame) HDR->SDR path is used instead.  _(src: docs/architecture/09-operations.md)_
- Cross-browser gapless operation-window: Firefox reproduced the deficit at 49,152 decoded samples and reported HEVC browser capability as NA_ENGINE; WebKit classified HEVC strict-golden comparability as NA_BROWSER; both passed the repaired AAC duration row and retained the HLS invocation error.  _(src: docs/notes/session12-public-truth-boundaries.md)_
- For an already-complete WebM object, a plain full GET via Response.arrayBuffer() measured 1.200 ms versus 1.800 ms for an exact full Range request; the generic ReadableStream GET seam only improved the recorder median from 6.375 to 6.040 ms because multiple body pulls plus concatenation re-copied the response, motivating a direct owned whole-read capability.  _(src: docs/notes/session13-probe-leadership-2026-07-13.md)_
- HEVC 10-bit (Main10) encode output is hardware-only with no permissive software path, and VP9 decode is missing on much of WebKit while HEVC decode is missing on Firefox/Chrome-without-hardware — the exact cases the WASM decode fallbacks exist for.  _(src: BUILD_INSTRUCTIONS_SESSION4.md)_
- HEVC hardware decode availability in Chromium is OS-dependent, and Main10 availability varies by browser/OS; there is no bundled software HEVC decoder, so a rejected exact config stays a typed miss.  _(src: docs/architecture/10-browser-capability-matrix.md)_
- JSC may legally defer clearing a WeakRef target despite a forced collection, so WeakRef.deref() is reported diagnostically but not used as the MP4 packet-stream terminal-release gate; a self-describing V8 heap snapshot checking for zero strong inbound retainers is the gate instead.  _(src: docs/notes/session13-mp4-packet-terminal-release.md)_
- JSON.stringify serializes Infinity to null, so live MediaStream probe emits explicit live/unbounded tags alongside the Infinity duration sentinel to preserve meaning across serialization.  _(src: docs/architecture/09-operations.md ADR-236)_
- Matroska Colour metadata (range, H.273 conversion, chroma siting) must be preserved because dropping it changes Chromium's YUV-to-RGB and chroma upsampling and therefore canvas pixels, even when codec packets and decoded YUV are byte-equal.  _(src: docs/notes/matroska-colour-preservation.md)_
- MP3 in WebCodecs is decode-only (no encoder); MP3 encode is reported honest-NA by default unless a future isolated LGPL LAME tail is explicitly approved.  _(src: docs/architecture/10-browser-capability-matrix.md)_
- MPEG-TS transport streams often start at a nonzero timestamp, so TS keyframe trim computes the public window against the earliest source PTS rather than assuming zero.  _(src: docs/architecture/09-operations.md)_
- mux/edge_hevc_decode_mux_mkv: product output and an independent FFmpeg HEVC Matroska reference preserve packets/frames/colour/HDR, but the browser <video> path rejects both valid MKVs — a platform playback limitation not repairable by product bytes.  _(src: docs/notes/session11-fair-harness-boundary-audit.md)_
- Node has no ImageDecoder, so image pixel decode raises a typed CapabilityError there rather than fabricating pixels; browser image decode is ImageDecoder over a whole encoded payload.  _(src: docs/architecture/07-public-api.md)_
- On Apple VideoToolbox H.264, variable/constant/omitted bitrateMode, quality/default latency, and explicit hardware selection produce byte-identical output; omitting the 60fps config hint doubles actual output to ~4Mb/s (a misleading quality improvement).  _(src: docs/notes/h264-2mbps-quality-bound.md ADR-193)_
- RFC 9110 §14 makes HTTP ranges optional: a server may ignore/reject many small ranges, may coalesce, and multipart adds ~80 bytes overhead per part; even a 256-prefix batch needs 54 requests for 01.mov, so multiple ranges do not create a portable fast path for sparse AVC prefix reads.  _(src: docs/notes/session11-mp4-avc-key-picture-proof.md)_
- Some browser VP8/VP9 decoders accept and configure an exact profile via isConfigSupported, then report an asynchronous runtime CapabilityError on the first coded packets; ADR-284 retains a bounded packet prefix until the first native frame to allow WASM fallback.  _(src: docs/architecture/04-capability-router-and-ladder.md ADR-284)_
- Target browser floors: Chromium >=111 (WebCodecs GA), Safari >=16.4/17 (WebCodecs), Firefox where WebCodecs ships; absent WebCodecs still leaves TS containers/probe/decrypt working with WASM codec fallback.  _(src: docs/architecture/10-browser-capability-matrix.md)_
- The 3 no-winner benchmark features are all transcode: flac_to_opus_webm (Chrome 149 WebCodecs has no FLAC AudioDecoder), h264_8bit_to_hevc_10bit (no 10-bit HEVC encoder available), h264_to_vp8_webm (encode succeeded but failed playback-smoke).  _(src: docs/architecture/background/benchmark-summary.md)_
- The Bun/browser library types diverge on default reader results: the DOM declaration presents the done branch's value as undefined while Bun's standards-compatible declaration omits it, which broke the strict typecheck gate for the abort-aware WAV AudioData reader at the Session 13 handoff commit.  _(src: docs/notes/session13-baseline-and-gate-repair.md)_
- The harness's chromium browser profile must be cleared before each run (rm -rf results/.browser-cache/<browser>) to avoid a stale-session 'Opening in existing browser session' bug that zeroes/hangs runs.  _(src: BUILD_INSTRUCTIONS_SESSION3.md)_
- The transcode 12x root cause: the per-frame Canvas2D resize forced ctx.imageSmoothingQuality='high', which in Chromium is bicubic/Lanczos = CPU-bound main-thread-blocking, starving the hardware decode/encode queues; mediabunny uses the browser default 'low'. Changing 'high'→'medium' (GPU-fast area downscale) took the ladder transcode from 2.25x-behind to tied (aibrush 1813 vs mediabunny 1771, SSIM PASS).  _(src: docs/perf/goal26-evidence.md)_
- The W3C Media Capture Transform draft specifies a demand-driven MediaStreamTrackProcessor for raw video exposing VideoFrame, but explicitly records no working-group consensus that audio-track processors must exist; therefore live audio support is runtime-probed and accepted only if the platform processor actually produces AudioData, else a typed capability miss.  _(src: docs/notes/session12-live-media-input.md)_
- Under force-software, the two WebCodecs drivers keep tier:'hardware' for ranking but configure prefer-software at execution, making native-only H.264/HEVC configs miss even when the browser advertises an exact software config; the fix accepts a hardware-tier result only with an explicit hardwareAccelerated:false verdict.  _(src: docs/notes/session12-force-software-routing.md)_
- Valid HEVC-in-MKV whose Chromium intrinsic dimensions remain zero is a browser-platform boundary (not a product defect); Matroska attachment side data stripped before the public call is an invocation boundary.  _(src: docs/notes/session12-gapless-corpus-and-native-suppression.md)_
- VideoDecoder emits frames in presentation order per the W3C spec (the UA reorders B-frames), so the driver needs no live reorder buffer and enqueues in arrival order; reorder helpers exist only for captured-stream tooling.  _(src: docs/architecture/09-operations.md)_
- VideoFrame.copyTo to 'RGBA' returns pixels in the frame's OWN color space (UA does only YUV->RGB, not display tone-management), which is what lets the pure-TS CPU filter do genuine wide-gamut colorspace and PQ/HLG->SDR tonemap math when the frame is readable.  _(src: docs/architecture/09-operations.md)_
- Vorbis has no WebCodecs decode path in any browser (Chromium/Safari/Firefox all 'none'), so it requires a WASM decode tail (Symphonia).  _(src: docs/architecture/10-browser-capability-matrix.md)_
- WebCodecs accepts several H.273 colour tokens (bt2020, smpte432, pq, hlg, bt2020-ncl) that lib.dom's colour enums predate; QuickTime SMPTE-240M transfer/matrix and BT.2020-CL points remain unresolvable by WebCodecs and stay only as raw code points on ColrInfo for remux.  _(src: docs/notes/qtff-mov-parsing.md ADR-185)_
- WebCodecs configuration support is checked asynchronously on the UA control queue, so returning from configure() is not proof of support; ADR-203 runs configure() followed by an empty flush() as a control-queue barrier before writable startup resolves, and a stale hardware verdict or async startup rejection deletes the verdict and proves the exact no-preference config before any packet is submitted (per W3C WebCodecs Editor's Draft, accessed 2026-07-11).  _(src: docs/notes/session11-webcodecs-video-exact-acceleration.md)_
- WebCodecs EncodedVideoChunk/EncodedAudioChunk are sealed and carry only timestamp (=PTS), no DTS — so the engine added a {chunk, dtsUs?} Packet view (ADR-045) to carry decode timestamps across the container↔codec seam; dtsUs undefined means DTS==PTS.  _(src: BUILD_INSTRUCTIONS_SESSION3.md)_
- WebCodecs exposes no first-pass statistics API, so true two-pass H.264 bitrate encoding is impossible; the engine either registers it honest-NA or does an approximate double-encode.  _(src: BUILD_INSTRUCTIONS_SESSION7.md)_
- WebCodecs, OffscreenCanvas, WebGPU, and WebGL all work inside Web Workers, so the entire decode->filter->encode path (including GPU filters) runs off the main thread.  _(src: docs/architecture/06-execution-and-runtime.md)_

## Decision rationale (ADR digest — seeds docs/decisions/)  (190)

- ADR-001: build one capability-routed engine (router + pluggable backends) rather than extending a single substrate, because every benchmarked engine is mono-substrate and no one substrate wins everything — 'best-of-the-best = union of substrates'; extending e.g. mediabunny would inherit its losses (audio-dsp + browser-missing codecs).  _(src: docs/architecture/02-decision-records.md ADR-001)_
- ADR-004: lazy-loading model uses a tiny eager kernel of ≤~50 kB plus per-op/per-driver dynamic import(); the ~500 kB budget covers JS glue only (no WASM), because backend choice is a runtime decision.  _(src: docs/architecture/02-decision-records.md ADR-004)_
- ADR-005: WASM is self-hosted (no CDN) via new URL('./x.wasm', import.meta.url) fetched only on hardware miss, because browsers partition the HTTP cache by top-level site so cross-site CDN cache-sharing (a CDN's only real benefit) is gone while its costs remain; compiling WASM needs CSP wasm-unsafe-eval, threads need COOP/COEP.  _(src: docs/architecture/02-decision-records.md ADR-005)_
- Phase 1 may ship as an ARCH-3 monolith (kernel directly owning TS containers + WebCodecs + GPU filters) to reach MVP faster, then refactor to the ARCH-1 router+registry once a second/third driver appears, without changing the public API or seams (ADR-015).  _(src: docs/architecture/03-system-architecture.md ADR-015)_
- Only the heavy decode->encode graph (convert and accurate trim) offloads to the worker; probe/demux/stream-copy remux/keyframe trim/PCM transform/decrypt/seek stay inline because the worker round-trip is not worth it (doc 06 §4 / ADR-019); the real main-thread longtasks~0 proof requires a browser PerformanceObserver measurement (Node cannot observe it).  _(src: docs/notes/worker-offload.md ADR-019)_
- The worker pool reuses one WorkerStreamBridge transport across jobs, so an in-transit chunk/done/error from a cancelled job N can arrive after job N+1's listener attaches; every message is stamped with a monotonic epoch (incremented per runStream) and stale-epoch messages are ignored (closing stale transferred frames) to prevent cross-talk between renditions.  _(src: docs/notes/worker-offload.md ADR-019)_
- Worker offload transfers input bytes (not a Source) because a Source carries non-serializable stream()/range() closures and demux needs a seekable range() for the moov; the worker rebuilds a fromBytes source, and no VideoFrame/AudioData ever crosses the boundary on convert/trim (only encoded byte buffers transfer back), sidestepping the close-exactly-once-across-threads hazard (ADR-019/ADR-010).  _(src: docs/notes/worker-offload.md ADR-019)_
- Tier thresholds in router.ts were hard-coded constants (with provenance metadata) rather than derived from telemetry, and wasmInitForProfile returned the baseline asset for both 'baseline' and 'isolated-simd-threads' so no core actually used SIMD/threads — flagged as ADR-020/ADR-006 debt to close.  _(src: BUILD_INSTRUCTIONS_SESSION4.md)_
- ADR-021: remux/keyframe-trim use driver-native container stream-copy (not the WebCodecs seam) because EncodedVideoChunk/EncodedAudioChunk carry only a presentation timestamp and have NO DTS, so routing through that seam cannot preserve decode order / B-frame composition (ctts) or the raw codec-config box.  _(src: docs/architecture/02-decision-records.md ADR-021)_
- ADR-022: PCM (WAV) uses a driver-native transformPcm path (not decode→filter→encode) because PCM is not a WebCodecs codec (no AudioDecoder/AudioEncoder), and the resampler is a pure-TS band-limited windowed-sinc (Kaiser β≈9.42, 32 zero-crossings, 512 phases/zero-crossing) rather than deferring to a WASM soxr / WebAudio OfflineAudioContext tail.  _(src: docs/architecture/02-decision-records.md ADR-022)_
- Resample is pure-TS band-limited windowed-sinc not WASM soxr (ADR-022) because format/gain/mix/fade are kilobytes of TS and resample is only a few hundred more lines, leaving only lossy ENCODE as genuinely needing WASM (Finding 4).  _(src: docs/architecture/09-operations.md ADR-022)_
- ADR-024: FLAC decode is implemented in pure TS (not WASM) because FLAC is a lossless integer codec, making a pure-TS decoder bit-exact and Node-validatable without a browser or WASM toolchain (which this environment lacks); STREAMINFO's PCM MD5 is its own gold oracle, validated on the 8-file IETF FLAC conformance corpus.  _(src: docs/architecture/02-decision-records.md ADR-024)_
- FLAC decode ships as pure TS not WASM (ADR-024) because FLAC is lossless/integer so the TS decoder is bit-exact and only ~kilobytes with no toolchain, unlike a compiled core.  _(src: docs/architecture/12-roadmap.md ADR-024)_
- ADR-025: the CI/build sandbox is Node-only (no browser WebCodecs/WebGPU, no C→WASM toolchain), so the pure-TS tier is validated exhaustively in Node while the WebCodecs/GPU tier and the 558-feature harness run on a target machine with a browser; fabricating browser/WASM results to force green is forbidden and reported as typed CapabilityError until the real substrate runs.  _(src: docs/architecture/02-decision-records.md ADR-025)_
- The architecture base was first ratified 2026-06-23, grounded in the sibling media-test project's feasibility and architecture-options analyses; validation is tier-split (ADR-025) into Node-validated pure-TS vs browser-validated WebCodecs/GPU.  _(src: docs/architecture/README.md ADR-025)_
- ADR-027: the video-filter ladder omits WebGL (WebGPU primary + Canvas2D fallback) because Canvas2D drawImage proved to be itself GPU-accelerated and pixel-exact for every geometric op, making a separate WebGL tier redundant; GPU uniform packing is std140, 48 bytes.  _(src: docs/architecture/02-decision-records.md ADR-027)_
- The WebGL filter rung is intentionally omitted (ADR-027) because Canvas2D drawImage is itself GPU-accelerated and pixel-exact for the geometric ops, making it the single fallback before the pure-TS CPU floor.  _(src: docs/architecture/04-capability-router-and-ladder.md ADR-027)_
- ADR-028: the MP4 muxer synthesizes avcC/esds for AVC/AAC from the WebCodecs DecoderConfig but carries hvcC/av1C/vpcC/dOps/dfLa (HEVC/AV1/VP9/Opus/FLAC) verbatim from the encoder's description, because the encoder already emits the exact box; ctts is computed in microseconds first so a non-reordered stream yields exactly ctts==0 at any timescale.  _(src: docs/architecture/02-decision-records.md ADR-028)_
- ADR-031: measured toolchain boundary — pure-Rust→wasm-pack build --target web works here (emits *_bg.wasm + JS glue), but libopus cannot be built (audiopus_sys fails its C build with 'autoreconf: command not found', and wasm32 libopus needs an absent Emscripten sysroot / no emcc); there is no production pure-Rust Opus (Symphonia has no opus feature).  _(src: docs/architecture/02-decision-records.md ADR-031)_
- MP3 encode has no permissive option: LAME is LGPL-2.1+, Shine is LGPL-2.0, and no pure-Rust/no-C MP3 encoder exists (Symphonia and puremp3 are decode-only), so MP3 encode is treated as an honest capability-miss unless the lead accepts the LGPL source-distribution obligation via an ADR.  _(src: docs/notes/wasm-codec-cores.md ADR-031)_
- vendor-wasm.ts (discoverTails) requires each WASM core to ship both a *_wasm_bg.wasm and a *-core.js in src/codecs/wasm-<id>/; a half-pair fails loudly (never a silent half-vendor) and until a pair is vendored the driver reports supports()=false and raises CapabilityError('capability-miss').  _(src: docs/notes/wasm-codec-cores.md ADR-032)_
- ADR-034: true bounded-memory streaming output requires fragmented/CMAF MP4 (init segment then one moof+mdat per fragment, new run at every keyframe) because a non-fragmented MP4's moov sample tables name absolute byte offsets forcing the whole file to buffer; a faststart 'pseudo-stream' reserving moov space was rejected because it still buffers the whole file.  _(src: docs/architecture/02-decision-records.md ADR-034)_
- ADR-041: the real Symphonia WASM tails are kept OUT of defaults.ts until browser .wasm co-vendoring is wired, because auto-registering a tier:'wasm' driver whose import.meta.url core-load isn't emitted as a same-origin asset would fail at runtime in a consumer's build or bloat the eager kernel.  _(src: docs/architecture/02-decision-records.md ADR-041)_
- ADR-042: esbuild/tsup does NOT copy a .wasm referenced via a plain new URL('./x.wasm', import.meta.url) into dist/ (it is not a recognized asset import), so scripts/vendor-wasm.ts copies each real tail's core+glue into dist/ after build; Biome's useIgnoreFile reads only the root .gitignore (not nested ones), which leaked each crate's pkg/target into biome ci.  _(src: docs/architecture/02-decision-records.md ADR-042)_
- The Symphonia WASM cores provide real Vorbis/AAC/MP3 decoders (ADR-042); Opus/VPx were left as honest core-less scaffolds (supports()→false) because libopus/libvpx need Emscripten and were out of scope until later sessions vendored them.  _(src: BUILD_INSTRUCTIONS_SESSION2.md)_
- Trim on entropy-coded corrupt-source input is a documented honest limitation (ADR-043): an entropy-coded bitflip is undetectable at the container level without a full decode, so the engine keeps a graceful failure rather than faking a filename match.  _(src: BUILD_INSTRUCTIONS_SESSION3.md)_
- Coverage gate policy (ADR-044): never write coverage-only no-op tests that tickle unreachable noUncheckedIndexedAccess '?? fallback' branches — exclude genuinely browser-only paths via /* v8 ignore */, never lower the threshold on real branches.  _(src: BUILD_INSTRUCTIONS_SESSION3.md)_
- ADR-045: the demuxer↔muxer seam carries Packet{chunk, dtsUs?, sizeBytes?} instead of a bare EncodedChunk, because EncodedChunk is an immutable host object exposing only timestamp (PTS); undefined dtsUs means DTS==PTS; a WeakMap DTS side-channel was rejected (cannot cross a worker postMessage and an unkeyed chunk silently loses its DTS).  _(src: docs/architecture/02-decision-records.md ADR-045)_
- ADR-049: still/animated images are registered as an ImageOps side capability, not a ContainerDriver/CodecDriver, because they have no per-track packet stream or DecoderConfig handoff; AVIF preemption is limited to AVIF/AVIS image brands (not the av01 codec brand) so AV1 MP4 video still falls through to the MP4 container driver.  _(src: docs/architecture/02-decision-records.md ADR-049)_
- Images are a side capability, not a fourth driver kind (ADR-049), because still/animated images have no packet seam and no codec-config handoff, so forcing them into ContainerDriver/CodecDriver would invent a fake seam.  _(src: docs/architecture/05-driver-contracts.md ADR-049)_
- ADR-052: MP4 URL stream-copy coalesces sample byte reads into bounded range windows (up to 8 MiB, bridging gaps up to 256 KiB) because a two-hour H.264/AAC MP4 has hundreds of thousands of samples, and one HTTP Range request per sample hit the adapter timeout; a whole-file buffer was rejected (moves the failure to the browser's large-file read).  _(src: docs/architecture/02-decision-records.md ADR-052)_
- ADR-053: the browser harness adapter declines explicit buffer-target remux above a ~512 MiB whole-output materialization ceiling (using HEAD/one-byte Range metadata, not asset names), because a 1+ GiB MP4 output spends long synchronous time allocating one Uint8Array and can starve timers or OOM the tab.  _(src: docs/architecture/02-decision-records.md ADR-053)_
- ADR-055: Packet.sizeBytes distinguishes the on-disk container packet size from the decoder access-unit size — ADTS sets it to the full frame length (header+CRC+payload, what ffprobe reports) while chunk.data carries only the raw AAC access unit with the ADTS header stripped (what WebCodecs needs).  _(src: docs/architecture/02-decision-records.md ADR-055)_
- ADR-056: an optional Demuxer.packetTable() fast path returns MP4/MOV sample-table metadata (size/PTS/DTS/keyframe/track) without reading mdat, because the golden-packet oracle needs only metadata and reading mdat for a 600 s or 2 h fixture meant tens/hundreds of thousands of sequential range requests that timed out the adapter's 30 s cap.  _(src: docs/architecture/02-decision-records.md ADR-056)_
- ADR-070/079: Ogg Opus mux granules are derived from the RFC 6716 packet TOC (32 config values → 48 kHz frame durations, e.g. a 60 ms packet = 2880 samples) rather than trusting EncodedAudioChunk.duration, because host chunk duration can be missing/rounded/represent container cadence; a declared final duration may only trim inside the final coded packet. Vorbis instead scales missing-duration packet weights toward round(durationSec*sampleRate).  _(src: docs/architecture/02-decision-records.md ADR-070)_
- ADR-075: 8-bit PCM keeps container-specific sign — WAV 8-bit PCM is unsigned (pcm-u8), AIFF and CoreAudio CAF integer lpcm are signed (pcm-s8, verified via afinfo on sfx-u8.caf 'as 8-bit signed integer'); treating either as the other shifts every sample by 128; explicit incompatible 8-bit targets (WAV pcm-s8, AIFF/CAF pcm-u8) are typed CapabilityErrors.  _(src: docs/architecture/02-decision-records.md ADR-075)_
- ADR-076: pure-TS CPU filters (audio-dsp-filter, cpu-video-filter) declare FilterSubstrate:'native' (added), ranked webgpu→webgl→canvas2d→native→wasm; they had temporarily declared 'wasm' as the least-wrong value, which lied about what executed and blurred a real future WASM filter tail; force-software drops only webgpu/webgl.  _(src: docs/architecture/02-decision-records.md ADR-076)_
- ADR-078: AV1 dav1d WASM fallback stays a scaffold (not auto-registered) because this environment has cargo+wasm-pack but no emcc, no dav1d/rav1d source, and no vendored artifact (re-verified 2026-06-26); vendoring an arbitrary prebuilt binary would be unrebuildable and fabricating frames violates ADR-018.  _(src: docs/architecture/02-decision-records.md ADR-078)_
- ADR-085: 2026-06-26 build-host audit found cargo/rustc 1.94 + wasm32-unknown-unknown + wasm-pack 0.14 + wasm-opt 124 present, but Emscripten absent (no emcc/emconfigure/emmake) and crates.io network-restricted (HTTP 403) while npm/github/github-raw are reachable (HTTP 200); so C-library cores (libopus/libvpx/dav1d/libmp3lame) are vendored as prebuilt permissively-licensed WASM with sha256 provenance rather than built from source.  _(src: docs/architecture/02-decision-records.md ADR-085)_
- ADR-087: worker offload serializes the op (not the pipeline/closures) — ships the input ArrayBuffer transferred zero-copy plus mime/options minus sink; only encoded bytes stream back so no VideoFrame/AudioData ever crosses the thread boundary; a monotonic per-job 'epoch' stamp on every message prevents a reused bridge's in-transit chunk from cross-talking between jobs.  _(src: docs/architecture/02-decision-records.md ADR-087)_
- Worker offload (ADR-087) serializes the OP not the pipeline: the host ships input bytes + flat options, the worker runs the same op on an inner engine forced worker:false and streams only encoded bytes back, so no VideoFrame/AudioData ever crosses the worker boundary for convert/trim.  _(src: docs/architecture/06-execution-and-runtime.md ADR-087)_
- Worker offload is opt-in: unset or worker:false means the inline path (the safe default) to avoid a surprise Worker spawn per heavy op; any spawn/handshake failure (e.g. Node, CSP-blocked module workers) downgrades to a byte-identical inline path.  _(src: docs/architecture/06-execution-and-runtime.md ADR-087)_
- ADR-091: fragmented WebM uses the EBML unknown-size Segment vint (SEGMENT_UNKNOWN_SIZE = 0x01 + seven 0xFF, decoded to -1, runs to EOF) with live top-level Clusters (default cap 90 blocks/fragment), a different container grammar than MP4's moof/mdat; only video keyframes start a fragment (every audio packet is a sync frame so splitting on audio would fragment every packet).  _(src: docs/architecture/02-decision-records.md ADR-091)_
- ADR-095's shortcut treating every protected cbcs track without senc/saiz/saio as clear-signalled was reverted (Session 11); per ISO/IEC 23001-7 default_Per_Sample_IV_Size=0 means tenc.default_constant_IV applies to every protected sample, so constant IV alone is sufficient crypto metadata.  _(src: docs/notes/cbcs-real-world-decrypt.md ADR-095)_
- ADR-095: the cbcs-protected fixture cenc_cbcs.mp4 (version-1 tenc, default_Per_Sample_IV_Size=0, crypt:skip 1:9, default_constant_IV) is handled by stripping protection metadata WITHOUT AES-touching samples when it lacks sample auxiliary/senc data, rather than decrypting; this avoids corrupting a stream whose samples aren't actually per-sample encrypted.  _(src: docs/architecture/02-decision-records.md ADR-095)_
- Massive >1 GB operations (e.g. remux/massive_h264_1080p_2h ~1091 MB) intentionally emit a typed decline rather than materialize, per ADR-102 bounded-materialization safety: a typed decline is preferred over OOM-ing the tab.  _(src: BUILD_INSTRUCTIONS_SESSION6.md)_
- No permissive (MIT/BSD/Apache/zlib) MP3 encoder exists — only LGPL LAME/lamejs/Shine — so MP3 encode (aac_to_mp3_mp4, wav_to_mp3_mp4) stays honest-NA (ADR-105) unless the user approves an isolated, lazy, separately-licensed LGPL LAME tail; this is the single license gap to 100% coverage.  _(src: BUILD_INSTRUCTIONS_SESSION5.md)_
- Vorbis encode ships via permissive libvorbis/libvorbisenc BSD wasm (ADR-108) because a permissive core exists — chosen deliberately over declining, since it needs no license compromise (unlike MP3).  _(src: BUILD_INSTRUCTIONS_SESSION6.md)_
- HLS SAMPLE-AES H.264 keeps the first 32 NAL bytes clear then decrypts one 16-byte AES-CBC block and skips up to 144 bytes cyclically with the CBC IV reset per NAL unit; ADTS AAC keeps the first 16 bytes clear then decrypts all remaining full 16-byte blocks with IV reset per frame; the NAL scanner validates plausible H.264 NAL headers so accidental 00 00 01 in ciphertext cannot fake NAL boundaries.  _(src: docs/notes/hls-sample-aes.md ADR-121)_
- An ADR-130 parity exemption is a standing row excluded from the active-deficit count in the Session 9 performance-deficits backlog.  _(src: docs/notes/session9-flac-ogg-streamcopy-target.md ADR-130)_
- ADR-152 established the AIFF PCM-to-WAV byte-order rewrite helper; s24->s16 narrowing sign-extends, rounds sample/256, clamps to [-32768,32767] to match the existing writeWav rounded narrowing (truncation was rejected).  _(src: docs/notes/session9-aiff-s24be-s16le-direct-wav.md ADR-152)_
- ADR-152 established the AIFF PCM-to-WAV byte-order rewrite; the s24be->s16le narrowing extension reads each 24-bit sample in source endian order, sign-extends, rounds sample/256, and clamps to [-32768,32767].  _(src: docs/notes/session9-aiff-s24be-s16le-direct-wav.md ADR-152)_
- An anti-cheat CI rule forbids input->output passthrough as work: for convert/remux/trim the SUSPECT shortcut of flipping ftyp bytes and returning the input is banned; ADR-155's MOV->MP4 brand rewrite is allowed only after structural compatibility and sample-range validation.  _(src: docs/architecture/09-operations.md ADR-155)_
- Raising the WebCodecs encoder queue high-water mark from 8 to 16 was rejected multiple times: it regressed the VP9->AV1 median from ~2059.6 to ~2149.6 ms; ADR-165 already measured a 512-packet decoder queue slower than the retained 128-item bound.  _(src: docs/notes/session13-video-rate-alpha.md)_
- ffmpeg is NOT used as a cbcs crypto oracle because it cannot open the fragmented cbcs layout ('error reading header') and its video-subsample crypto is non-conformant; openssl/node:crypto AES-128-CBC + Bento4 (mp4fragment + mp4encrypt --method MPEG-CBCS, video 1:9 + audio 0:0 on movie_5.mp4) + the 23001-7 construction are the oracle set.  _(src: docs/notes/cbcs-real-world-decrypt.md ADR-182)_
- The MPEG-TS ADTS deframer uses a discontinuity-gated timing rebase (threshold REBASE_DISCONTINUITY_TICKS = 1/2s at 90kHz) rather than per-PES re-anchoring because bear-1280x720.ts carries a priming frame that ffmpeg folds onto frame 0, leaving every PES PTS ~1 AAC frame (~2090 ticks) behind a clean cadence; per-PES re-anchoring produced a non-monotonic 'frame-12 wobble'.  _(src: docs/notes/mpegts-aac-deframe.md ADR-184)_
- ISO defines version-0 ctts composition offsets unsigned, but every real muxer/demuxer treats them signed (ffmpeg mov muxer writes two's-complement into a v0 box, its demuxer reads avio_rb32 into a signed int); reading unsigned turned -40 ticks into 4294967256, exploding PTS to ~7.16e6s and causing decode-seek SSIM ~0.85 (0/12 digest-exact) mis-read as a colour defect. parseCtts now reads signed int32 regardless of version.  _(src: docs/notes/qtff-mov-parsing.md ADR-185)_
- ffprobe reports a stream's duration as the edit-list presentation duration (segment_duration ÷ movie timescale) while avg_frame_rate uses media span; the parser keeps ParsedTrack.durationSec = media duration and exposes presentation duration as edit.durationSec, and only container probe() reports edit?.durationSec ?? durationSec (demux/round-trip/AAC-gapless keep media duration) — e.g. a variant presents 6.466710s of a 9.4667s media track with fps 20.49.  _(src: docs/notes/qtff-mov-parsing.md ADR-185)_
- MP4 esds parsing treats AudioSpecificConfig as authoritative for AAC rate/channels but keeps the outer sample-entry geometry for SBR presentation channels, because an implicit mono AAC-LC core may present stereo via Parametric Stereo (ADR-190).  _(src: docs/architecture/09-operations.md ADR-190)_
- A 31MB GPL FFmpeg/x264 single-thread WASM tail was rejected for the 2Mb/s quality gate because native x264 still misses the independent RGB gate at the exact rate and the WASM runtime violates the fastest/leanest objective.  _(src: docs/notes/h264-2mbps-quality-bound.md ADR-193)_
- ADR-202 MP4 demux static audit found three avoidable-work sources: range-backed RandomAccess remembered a prior read but did not serve later covered reads (fragmented MP4 fetched source twice), progressive demux materialized a seven-field SampleData object per sample only to prove mdat membership, and packetStream declared every pull async allocating a fulfilled promise even for resident samples. Fix serves reads from cachedWhole only on exact coverage; rejected caching a new whole-file read (unbounded memory) and reusing walkSamples (still traverses timing/composition/sync tables).  _(src: docs/notes/session11-mp4-demux-resident-ranges.md)_
- ADR-203's empty startup flush() barrier and exact accepted-acceleration-rung proof before packet submission are retained (removing the barrier and adding a decoder pool were both rejected) because the pool saves only ~2-5 ms on measured shapes while retaining scarce hardware codec resources beyond the owning operation and raising peak memory; the full 3,600-frame VP9 public drain is already within ~0.1% of direct native decode.  _(src: docs/notes/session13-webcodecs-decoder-startup.md)_
- ADR-204: no header-only AVC key-picture shortcut is valid for the rotated 600-second MOV corpus because stss only proves the samples it lists are sync samples and no file carries header evidence deciding the picture type of every other AVC sample; an optimistic 64-byte prefix prototype classified all non-stss samples but its 13,718 tiny reads were slower than existing coalesced full-payload windows.  _(src: docs/notes/session11-mp4-avc-key-picture-proof.md)_
- ADR-205 removes the redundant second sample-range scan only from the private trusted packetStream call path (exported helpers and stream-copy/decrypt/mux retain independent validation) and skips per-packet object/window allocation and sorting only when sample offsets are proven nondecreasing; any decreasing offset falls back to the existing stable sort so valid non-monotonic layouts are unchanged. Rejected: enqueuing a resident burst beyond desiredSize (violates one-packet backpressure) and raising the high-water mark.  _(src: docs/notes/session11-mp4-packet-drain-floor.md)_
- ADR-207 Router positive cache admits only a highest-ranked positive codec driver into a 64-entry LRU; a lower-tier positive stays valid for its current operation but is re-probed next time so a temporarily unavailable hardware/native rung can recover. The snapshot reads data descriptors directly (never toJSON/accessors); shared/cross-realm buffers, oversized descriptions, cyclic graphs, accessors, callable/symbol/non-record/non-finite fields skip caching. Rejected: codec-string-only or object-identity keys (wrong driver for mutated config) and caching lower-tier positives for the session (makes a transient hardware miss permanent).  _(src: docs/notes/session11-router-exact-positive-cache.md)_
- Only the highest-ranked positive codec driver is cached; a lower-tier fallback is deliberately re-probed on its next use so dynamic recovery of a hardware/native rung is not hidden for the rest of the session (ADR-207).  _(src: docs/architecture/04-capability-router-and-ladder.md ADR-207)_
- ADR-209 rotation/cohort-exact deficit gate separates two identities: correctness cells keyed by browser+scenario+baked/rotated filename slot+engine, and performance cohorts by browser+scenario+filename slot+source export (engines compared only inside one export). Only exports within 24 hours of the newest input satisfy current evidence; an absent/zero-sample/zero-median memory metric is explicitly unmeasured, not zero. The old generator keyed only browser+scenario+engine, letting reruns overwrite rotations and splice rivals from unrelated old exports.  _(src: docs/notes/session11-rotated-deficit-gate.md)_
- ADR-212 preserves re-readable ambiguous HLS custom sources: after a bounded HLS peek, only a true kind:'stream' retains the locked reader and replays consumed chunks; every other kind is treated as contractually re-readable (cancel/release the sniff reader, retain the head result, return the original Source). Rejected: marking every range-less source single-use, skipping HLS detection for unhinted custom sources, reading the whole source before knowing it is a manifest, and catching/ignoring the downstream need-seekable error.  _(src: docs/notes/session11-hls-rereadable-source.md)_
- Session-12 native AAC suppression and gapless work: MP4 gapless provenance/capacity clamp (ADR-213) plus a bounded WAV PCM stream path (ADR-226) and interleaved f32 raw-audio egress (ADR-228) were added to prove decoded-audio PCM through the public seam without altering AAC bytes.  _(src: BUILD_INSTRUCTIONS_SESSION12.md)_
- ADR-225 explicitly narrows optional tails (software MP3/AAC encode, cross-browser H.264/HEVC software codecs, VP8/VP9/AV1 software encode, SBR/HE-AAC/multichannel decode) out of the current public capability promise; they remain typed capability misses rather than fake support.  _(src: BUILD_INSTRUCTIONS_SESSION12.md)_
- Video pad canonical order is crop→resize→pad→rotate→flip→colour; untouched area is transparent RGBA zero (alpha-less codecs encode as black, VPx alpha targets retain transparency); pad never scales/crops; a no-op pad equal to current dimensions is omitted to avoid a pixel round trip; represented as a Blit shape. ADR-234 records the additive contract.  _(src: docs/notes/session12-video-pad-design.md)_
- A supplied assetBaseUrl in an HTTP(S) page rejects cross-origin, credentialed, and non-HTTP(S) roots before any media/fetch; file: is limited to Node/file contexts (ADR-237), closing an earlier CDN ambiguity so WASM is strictly same-origin.  _(src: docs/architecture/08-packaging-and-loading.md ADR-237)_
- ADR-249 terminal cleanup in the WAV deferred-stream wrapper dropped externally retained backing on real 03.wav from 8,238,493 to 334,237 bytes; output chunks larger than the documented 4,096 frames were rejected because they change the public frame-digest truth even when a copyTo() sample comparison matches.  _(src: docs/notes/session13-wav-s24-fused.md)_
- ADR-250 fused FLAC packet-info by carrying ADR-124's validated candidate header (with offset) into the next iteration and emitting rows directly, removing a re-parse and a second full row array; measured 1.061x (6.1-6.4%) on flac-blocksize-16.flac (959,681 bytes, 19,294 packets) and 1.45x (0.901→0.623 ms) on flac-192khz.flac (6,611,359 bytes, 376 frames).  _(src: docs/notes/session13-flac-packet-info-fused.md)_
- ADR-261 changed the raw-source WAV rewrite cache from a 32-entry × 8-MiB-per-entry shape (which could retain 256 MiB for 60 seconds) to keep 8 MiB per-entry eligibility but make 8 MiB the total cache budget; removing the cache and using WeakReferences were both rejected because a warm hit could disappear nondeterministically and refetch would regress the no-DSP path.  _(src: docs/notes/session13-wav-memory-lifecycle.md)_
- ADR-261 replaced the AIFF PCM plan's historical 32-entry-only raw-byte cache with an 8 MiB total-byte LRU (max 8 MiB per source, 60 s expiry) while preserving the one-source warm hit.  _(src: docs/notes/session9-aiff-pcm-byte-rewrite.md ADR-261)_
- ADR-261 replaced the historical 32-entry-only aggregate raw-source-byte cache with an 8 MiB total-byte LRU (at most 8 MiB per source, 60-second expiry), while preserving the one-source warm-hit behavior; cache stores only raw input bytes, never WAV outputs, decoded samples, parsed metadata, packet tables, benchmark results, or oracle outcomes.  _(src: docs/notes/session9-aiff-pcm-byte-rewrite.md ADR-261)_
- ADR-263/264 source-aware semantic no-op convert routes an already-satisfied request through streamCopy() without decode/filter/encode, but only on re-readable sources (Source.kind==='stream' stays on the codec path because probing then copying would consume it twice); rotation is normalized modulo 360 and explicit rotate:0 is eligible only when effective source display rotation is also zero.  _(src: docs/notes/session13-semantic-noop-convert.md)_
- ADR-276 qualifies VP9/AV1 codec config from a partial (incomplete-Block) 8 KiB prefix key frame; on the 748,970-byte VP9-alpha source (Tracks end byte 418, first alpha BlockGroup ends near 14.9 KiB) it returned exact vp09.00.30.08, alpha, 640×480, 30.0000003 fps from [0,8192) alone versus a former added 64 KiB read (1.330 vs 2.654 ms; 8,192 vs 65,966 bytes). Growing the first window to 16/64 KiB was rejected.  _(src: docs/notes/session13-webm-partial-keyframe-qualification.md)_
- ADR-279 lets a known-size WebM with complete Tracks/Attachments but missing duration/frame-rate jump directly to one full-file range instead of the 8→64 KiB→full ladder (three transport calls to two); on the 95,093-byte no-DefaultDuration VP9-alpha source it measured 2.710 vs 4.042 ms. Estimating fps from the first few Blocks was rejected because VFR/cadence changes require terminal truth.  _(src: docs/notes/session13-webm-terminal-timeline-jump.md)_
- ADR-280 copy-free default-Blob MP4 tag rewrite composes a fresh Blob from immutable input slices plus the patched moov without reading mdat; Blob-direct measured 1.860 ms median vs byte-direct 3.917 and full-remux 8.108 (peak RSS deltas 0.41 / 4.53 / 0.55 MiB). Exact-vendored browser evidence: baked h264_1080p_30s.mp4 59.165 ms vs mediabunny 227.305; real 03.mp4 42.010 vs 86.990.  _(src: docs/notes/session13-mp4-blob-metadata-rewrite.md)_
- ADR-281 remains unused: on rotated 03.webm (5,487 bytes) aibrush passed at 3.880 ms (MAD 0.590) vs mediabunny 4.695 (MAD 0.825), but the 0.815 ms lead is below the 1.415 ms combined noise (parity); the full public route is 0.0228 ms and direct probe 0.0179 ms, so the ~5-microsecond routing difference is three orders of magnitude below the browser row and cannot create a durable margin.  _(src: docs/notes/session13-webm-vp9-alpha-browser-floor.md)_
- ADR-285 (family-selective default container registration) and ADR-290 exist because the first container/codec capability miss imports the whole defaults.ts bundle; selective ADTS probe retains 9,994,240 bytes median RSS versus 22,183,936 after register-all, and explicitly registering only Ogg or WAV lowers post-GC Node RSS by ~13 MiB. GC-reclaiming compiled modules was rejected because module namespaces are permanent.  _(src: docs/notes/session13-selective-native-audio-transcode.md)_
- Session13 probe rows lose to mediabunny because the bounded metadata parser categorically excludes known-size video sources above 256 KiB, forcing a broader 32 KiB window read: probe/hevc_1080p_10s 4.305 vs 2.215 ms (1.944x), probe/h264_vfr 4.285 vs 2.070 ms; ADR-286's general smaller top-level walk lifting the payload-size gate is the fix.  _(src: docs/perf/session13-speed-ledger.md)_
- Session13 ADR-288 attributed the h264_to_mkv transcode wall (1913.845 ms vs mediabunny 1660.265) to the native implicit H.264 encoder: packet extraction is 3.0-4.5 ms and mux/materialization 5.4-12.9 ms while encoder latency is 1.59-2.26 s; switching to implicit-only realtime latency reduced the product path 2259.5→1606.2 ms preserving 321 packets/25fps/SSIM 0.999346.  _(src: docs/perf/session13-speed-ledger.md)_
- ADR-311 routes unknown-size seekable concrete-MIME sources directly to the hinted container instead of image-sniffing first, eliminating a redundant read; the exact baked-WAV profile then issued only bytes=0-127 and measured 3.930 ms over 15 samples (previously two requests: [0,4096) image magic + [4096,16384) driver).  _(src: docs/notes/session13-probe-leadership-2026-07-13.md)_
- ADR-312 starts unknown-size remote WebM at a 256 KiB crossover window rather than the 8 KiB→200011 byte two-read ladder; post-change transport issued one bytes=0-262143 request (4.900 ms/15 samples) and cohorts measured aibrush 4.210 ms vs MediaBunny 5.755 vs ffmpeg.wasm 6.985, and 4.065 vs MediaBunny 5.220.  _(src: docs/notes/session13-probe-leadership-2026-07-13.md)_
- ADR-313 and ADR-314 corrected eager loading: moving the codec conversion coordinator behind its selected operation boundary cut the eager emitted closure from 51.58 to 48.88 KiB, and replacing static default MP4/WebM registration with lazy signature proxies cut the typical default closure from 263.32 to 53.75 KiB.  _(src: docs/notes/session13-probe-leadership-2026-07-13.md)_
- ADR-315 restored bounded high-priority Range transport on tiny full-window reads: against the harness server, plain GET measured 3.825 ms, exact Range 2.045 ms, and high-priority exact Range 1.680 ms for the 5,546-byte faststart MP4; the source layer had been wrongly converting known full windows at or below 16 KiB to plain GET.  _(src: docs/notes/session13-probe-leadership-2026-07-13.md)_
- A separate packetInfoContainer() method prototype was rejected because it pushed the eager kernel below the required budget guard band; instead packetInfo(src,{container}) reuses the existing sniff-skipping route.  _(src: docs/notes/session9-flac-probe-demux-packet-info.md)_
- A whole-engine lazy proxy was rejected for byte recovery because it would broaden synchronous from/use and cancellation semantics unnecessarily; instead complete remux/trim/mux/decrypt runners moved behind literal dynamic imports while the eager engine retains only cancellation setup and engine-bound routing/codec callbacks.  _(src: docs/notes/session12-eager-budget-recovery.md)_
- ADR numbering high-water marks per session (never reuse): 042/045 (S2-3), 084+ (S4), ~102/103/105/108 (S5-6), 113/114/121 (S7-8), 118-120/122+ (S9), 181+ (S10), 187+ (S11), 213/225/226/228 (S12), 245+ (S13).  _(src: BUILD_INSTRUCTIONS_SESSION13.md)_
- AES-CTR CENC ciphertext is intentionally indistinguishable from random bytes and carries no MAC, so a ciphertext-fingerprint integrity check would encode corpus knowledge and is rejected; browser decode validation is only defense-in-depth and cannot prove integrity when a hardware decoder conceals damage while emitting the expected frame count.  _(src: docs/notes/cenc-ciphertext-decode-validation.md)_
- AV1 encode uses pure-Rust rav1e (best fit for the Rust+wasm-bindgen toolchain); dav1d is decode-only by design, and no pure-Rust AAC encoder exists (candidates exhale/fdk, chosen via ADR weighing patent posture).  _(src: BUILD_INSTRUCTIONS_SESSION4.md)_
- AV1 implicit bitrate efficiency remains 0.6 through ordinary cadence; above 30.5 fps it scales by sqrt(frameRate/30) capped at the H.264-equivalent budget. A global AV1 efficiency increase to 0.8 was rejected because it spent more bits and slowed the contested ordinary row.  _(src: docs/notes/session13-video-rate-alpha.md)_
- AVI demux deliberately does not require the idx1 index and instead walks movi chunk headers, because many real AVIs (and all streamed ones) omit or corrupt idx1; an idx1-only demux was rejected.  _(src: docs/notes/avi.md)_
- Adaptive MP3 metadata head starts at [0,2048) (all eight exhaustive MDN/CBR inputs prove first frame, next-frame lock, Xing/Info count, and LAME gapless tuple within 2 KiB), jumps a declared large ID3v2 tag to its audio boundary, and extends to 16 KiB on an ordinary miss; an absolute parser offset preserves CBR byte-duration arithmetic after the sparse tag jump.  _(src: docs/notes/session13-probe-leadership-2026-07-13.md)_
- Added an optional ContainerDriver.streamCopyTargets declaration so a source driver explicitly advertises cross-container targets it authors natively; rejected hidden target support without an explicit driver declaration and making Ogg an input format of the FLAC driver.  _(src: docs/notes/session9-flac-ogg-streamcopy-target.md)_
- Agent workflows must never run concurrently with a browser harness run — the browser starves and the run hangs (learned in Sessions 2/3); on a hang, pkill the run tree (run.sh/launch.mjs/serve.sh/'Chrome for Testing') and take the partial.  _(src: BUILD_INSTRUCTIONS_SESSION4.md)_
- Alpha copy is limited to unfiltered, same-family, profile-compatible VP8/VP9 color re-encode with wholly implicit controls (level changes stay compatible, profile changes do not); the 150-packet VP9-alpha payload (6,069 bytes, SHA 1c594ca7…) is copied byte-exact while color grows from 738,941 to 1,886,028 bytes and changes digest, proving no input-to-output passthrough.  _(src: docs/notes/session13-video-rate-alpha.md)_
- An explicit {codec:'hevc', bitDepth:10} target compiles to WebCodecs codec hev1.2.4.L120.B0; for an 8-bit source the planner returns 'encoder-widen' with no pixel stage because every 8-bit value is exactly representable in 10 bits and the Main10 encoder owns the representation change, avoiding a full-frame Canvas/GPU copy and preserving opaque-frame acceleration.  _(src: docs/notes/session12-hevc-main10-output.md)_
- Batch N/O diagnosis for av1_to_vp9_webm loss: aibrush measured 8.819s vs mediabunny 7.383s vs remotion 5.699s; the generic VP9 path requested 20 bits/pixel/second (~33 Mb/s at 1920x1080) while reference engines produce a materially smaller quality-preserving envelope, so implicit rate is now bounded by trustworthy source video bitrate; realtime latencyMode is selected for implicit ordinary-cadence VOD and the codec request high-water mark raised from 8 to 16.  _(src: docs/notes/lost-features-2026-07-13-design.md)_
- CENC ciphertext corruption cannot be detected via IV-progression in general (ISO/IEC 23001-7 permits arbitrary unique IVs); the check triggers only when two IVs two sample positions apart are exactly two apart as unsigned big-endian counters, requiring the sandwiched IV to be their unique midpoint (chance of false premise 2^-64 for 8-byte, 2^-128 for 16-byte IVs).  _(src: docs/notes/cenc-ciphertext-decode-validation.md)_
- CENC decrypt imports one non-extractable CryptoKey per KID+operation and reuses it; a global raw-key/CryptoKey cache was rejected because it would retain caller key material across operations/realms for small setup savings, complicate key lifetime, and make WebCrypto capability changes observable. Combining access units into one CTR call is invalid because ISO/IEC 23001-7 resets/changes the counter at sample/subsample boundaries.  _(src: docs/notes/session11-cenc-prepared-key-parallelism.md)_
- Compiling any WASM requires CSP script-src 'wasm-unsafe-eval'; WASM threads (SharedArrayBuffer) additionally require COOP/COEP, so the common path needs only 'wasm-unsafe-eval' and no isolation headers.  _(src: docs/architecture/08-packaging-and-loading.md)_
- Cross-workspace rsync of the vendored dist/ to ../media-test was blocked on 2026-07-12 by an exhausted escalation-credit window (retried at 04:47:59 and 04:48:13, still usage-limited) and only succeeded at 05:09 CEST; no alternate copy path was attempted because it would circumvent the workspace boundary.  _(src: docs/notes/session13-baseline-and-gate-repair.md)_
- Direct ReadableStream input rejected fully buffering every stream before routing (violates bounded startup memory/time-to-first-work) and ReadableStream.tee() (two independently queued branches weaken backpressure and make cancel ownership ambiguous); decode() uses one memoized abort-aware materialization to an immutable byte source shared by audio/video/image because the container decode seam opens a source independently per track.  _(src: docs/notes/session12-readable-stream-input.md)_
- Element sink rejects assigning ReadableStream<Uint8Array> to srcObject (not a valid MediaProvider), collecting the stream before feeding MSE (defeats backpressure), concurrent appendBuffer (violates SourceBuffer state), and silently falling back from streaming to whole-file Blob; via:'stream' reports a typed capability miss where the MediaProvider srcObject path is unsupported.  _(src: docs/notes/session12-element-sinks.md)_
- Encryption rows (cenc_ctr_decrypt) are structurally unwinnable vs ffmpeg.wasm: aibrush does decrypt + a FULL AVC decode-verify (verifyTrimmedAvcDecodeIfAvailable) while ffmpeg does decrypt-only, making aibrush ~2x slower (aibrush ~80-164ms vs ffmpeg ~33-67ms per file); the decode-verify is load-bearing (cenc-graceful-rotation.test.ts requires rejecting a structurally-valid IV mutation only full decode catches) and the workload is decode-bound so even a pipelined decrypt+decode leaves wall≈max(decrypt,decode)≈decode unchanged.  _(src: docs/perf/goal26-evidence.md)_
- FLAC encode was implemented pure-TS (done first, highest ROI) because FLAC is a lossless integer codec so a pure-TS encoder is bit-exact and only ~kilobytes, needing no Emscripten/Rust toolchain; validated by STREAMINFO-MD5 round-trip on the IETF FLAC corpus.  _(src: BUILD_INSTRUCTIONS_SESSION4.md)_
- FLAC partial (keyframe) trims write the legal all-zero 'unknown MD5' STREAMINFO value because recomputing the digest would require full PCM decode; only a full-stream selection preserves the source MD5.  _(src: docs/notes/session9-flac-keyframe-trim-fast-path.md)_
- Fixing the lazy-muxer race pushed the default/probe bundle above 256 kB, so default filter registration was changed to install cheap lazy FilterDriver proxies (webgpu-video-filter, canvas2d-video-filter, audio-dsp-filter, cpu-video-filter) that keep a synchronous supports() in the default bundle and import the concrete module only on first supported frame.  _(src: docs/notes/session9-lazy-muxer-filter-proxies.md)_
- For LARGE-file seek/demux, the engine's targeted range reads (moov + keyframe only) beat bulk/prefix fetch: extending the demux mp4 byte-path to a 4MB prefix made size_micro_audio_short worse (35ms vs 27ms baseline vs mediabunny ~7ms), and a pooled mp4-seek with 4MB prefix made seek_negative worse (43.8ms vs 31ms); bulk-fetch only won for mux which genuinely needs the whole mdat.  _(src: docs/perf/goal26-evidence.md)_
- For the h264_bitrate_2mbps transcode, remotion is NOT a passthrough cheat: measured output bitrates are aibrush 1.91Mbps (0.96x target) and remotion 2.24Mbps (1.12x target), both genuinely ~2Mbps, so a bitrate/anti-passthrough oracle does not distinguish them; remotion produces a valid 2Mbps output in 76ms (74x faster than aibrush's 5638ms real encode) — a genuine speed win, and aibrush's slow-encode root cause is not yet isolated.  _(src: docs/perf/goal26-evidence.md)_
- For the small ADTS-AAC-to-PCM decode, ffmpeg.wasm was faster because libavformat/libavcodec decode and WAV s16 muxing stay inside one tight wasm pipeline; aibrush-media's per-frame native AudioDecoder callback setup cost dominated.  _(src: docs/notes/session9-adts-aac-direct-wav-s16.md)_
- Fragmented-audio init-duration fast path trusts the parsed positive duration only when mvex is declared, all tracks are audio, all sample tables and stsz are empty, movie/track timescales and durations are finite/positive and equal within one tick, and no edit list exists (an edit always requires the fragment scan because AAC gapless uses edit + coded fragment ticks). Rejected: parsing every moof/trun (repeats 58–66 MiB I/O), trusting any positive mvhd (unsafe for video fps/mismatched tracks/gapless), and caching or fixture-name routing (overfits).  _(src: docs/notes/session11-fragmented-audio-init-probe.md)_
- Full-segment HLS AES-128 decrypt is a bounded whole-segment operation because WebCrypto exposes no streaming AES-CBC interface; the design deliberately retains ciphertext plus cleartext for one finite HLS segment rather than buffering a playlist or unbounded live stream.  _(src: docs/notes/session12-container-routing-and-direct-aes.md)_
- H.264 two-pass pass one uses a fixed QP of 28 as a direct complexity proxy; a 0.6 complexity-blur curve allocates more bits to hard pictures without linear budget consumption; the H.264 six-QP-per-doubling model converts each picture budget to a QP clamped to [0,51] with bounded adjacent slew; pass two runs WebCodecs in quantizer mode. The retained schedule uses packed Float64Array timestamps + Uint8Array QPs = nine bytes per picture (O(1) presentation-order cursor, binary search for out-of-order lookups) — no per-frame Map/object graph.  _(src: docs/notes/session12-two-pass-h264-design.md)_
- Image decode declines force-software with a typed capability miss (before ImageDecoder work) because it is a browser-native path with no software decoder, so it cannot honestly claim bit-exact force-software behavior until a licensed software image decoder exists.  _(src: docs/notes/session12-force-software-routing.md)_
- Implicit offline video rate control was changed from 20 to 20 aggregate bits per output pixel per second scaled by codec efficiency and floored at 300kb/s; H.264 1280x720 gets 18.432Mb/s. The prior 9.216Mb/s H.264 720p budget scored SSIM 0.968371 (below 0.98 gate) on rotated 03.mp4; 18.432Mb/s scored 0.981680 (PASS).  _(src: docs/notes/h264-resize-quality.md)_
- Job-runner Blob intermediates between flat operations are deliberate (until the planner can carry a single heterogeneous stage graph); the documented trim→resize→output job executes exactly two real stages — a Blob trim and the final resize/encode/mux — rather than making unsafe transform fusion a hidden pixel reordering.  _(src: docs/notes/session12-declarative-job-runner.md)_
- Lazy audio containers keep synchronous mux contract via shared audio-container-mux-validation.ts because returning a proxy and loading the real muxer only on first async write would postpone invalid fragmented options and invalid/duplicate track errors; AIFF and CAF still reject createMuxer() immediately because their raw PCM output is served only through transformPcm. Rejected: raising the budget, lazifying MP4/WebM first, duplicating validation, deferring synchronous mux errors, or preloading all six tails.  _(src: docs/notes/session11-lazy-audio-container-defaults.md)_
- Live MediaStream input rejected MediaRecorder (encoded UA-selected container/codec, not raw frames), canvas polling (video-only, independent clock/drop), and ScriptProcessor/hidden AudioWorklet recorder (cannot apply backpressure to a real-time audio graph); the primary path is one MediaStreamTrackProcessor per track with maxBufferSize:1 wrapped in a highWaterMark:0 stream. More than one live track of a kind is a typed input error because MediaStream has only one slot.  _(src: docs/notes/session12-live-media-input.md)_
- Live-source probe reports duration Infinity (matching the unbounded-live platform convention) with tags {live:'true', duration:'unbounded'}; because JSON has no Infinity token the field serializes to null and the pinned tags preserve the unbounded meaning rather than a guessed zero. demux/packetInfo/remux/trim/seek/decrypt/ABR-replay are typed capability misses; two-pass is rejected because a live source is not replayable.  _(src: docs/notes/session12-live-media-input.md)_
- MP4 probe→demux handoff is intentionally asymmetric: probe may retain an exact owned raw moov, but demux always reparses and independently walks every top-level header to discover mdat rather than trusting a probe-time storage claim, to keep independent demux corruption validation.  _(src: docs/notes/session13-mp4-demux-layout.md)_
- Matroska attachment preservation puts the container-side-data bundle on TrackInfo (not only Demuxer) because callers and packet-stream adapters commonly retain only TrackInfo, so a Demuxer-only shape would silently lose attachments at the demux→mux seam; reconstructing attachments from projected packets was rejected because it loses filenames, MIME types, UIDs, unknown/duplicate children, and non-image payloads. WebM output raises a typed CapabilityError for attachments.  _(src: docs/notes/session11-matroska-attachment-packet-seam.md)_
- Matroska stream-copy preserves attachments by re-wrapping the unchanged AttachedFile child payload (preserving all children, order, exact 64-bit FileUID bytes, unknown/optional fields) per RFC 9559 §5.1.6 and §8; rejected alternatives were treating attachments as encoded packets (invalid structure), rebuilding only the four known fields (discards optional/future elements and risks unsafe-integer UID loss via JS number), and returning the source unchanged (not a remux, incompatible with trim/tag rewrites).  _(src: docs/notes/session11-matroska-attachment-stream-copy.md)_
- Matroska tag replacement rejected key-by-key mutation inside existing Tag trees because a flat public record cannot faithfully address arbitrary Targets, nested tags, duplicate names, languages, and defaults; whole-element replacement gives deterministic idempotent semantics. Segment-level CRC-32 is recomputed with IEEE CRC-32 little-endian; a nested SeekHead CRC is refreshed.  _(src: docs/notes/session12-matroska-tag-replacement.md)_
- Of ffmpeg.wasm's 139 WASM-tier wins, ~112 are container/demux/remux/probe/trim/PCM/mux glue a WebCodecs+TS engine reclaims natively; only ~15-20 features (~3-5% of 558) truly need native code (lossy audio encoders Opus/AAC/MP3/Vorbis, SW video encoders, FLAC decode, true sample-rate resampling, a few colorimetry ops) — this drove the decision to make WASM a lazy per-codec fallback tier, not a monolith.  _(src: docs/architecture/background/benchmark-summary.md)_
- OggDriver.probe reads [0,size) once and skips the tail range for seekable sources with known size <= 256 KiB; larger files keep the 64 KiB head plus 64 KiB tail strategy so probe cost stays independent of media duration.  _(src: docs/notes/session9-ogg-opus-probe-fast-path.md)_
- Packet provenance uses a module-private WeakMap keyed on the exact unlocked unpulled first-party ReadableStream plus an exhaustive semantic-equality proof of the TrackInfo descriptor; exposing a public brand or trusting a caller-supplied symbol was rejected as forgeable, and propagating provenance through a wrapper stream was rejected because a wrapper is a real cancellation/backpressure boundary the engine cannot prove without consuming it.  _(src: docs/notes/session13-native-track-clone-provenance.md)_
- Raising the generic URL full-window fetch threshold was rejected for pcm_s16be_to_s16le because measurement showed it made that row slower.  _(src: docs/notes/session9-aiff-pcm-byte-rewrite.md)_
- Rejected raising the generic URL full-window fetch threshold for AIFF->WAV because measurement showed it made the pcm_s16be_to_s16le row slower.  _(src: docs/notes/session9-aiff-pcm-byte-rewrite.md)_
- Runtime controls (pinDriver, enableThreads, assetBaseUrl) are per-call routing facts never mutable process policy; enableThreads:false is baseline even when isolation and SAB exist, enableThreads:true without cross-origin-isolation/SAB is baseline-with-a-reason, and no baseline path allocates or exposes SharedArrayBuffer. Browser asset roots synchronously reject credentials, cross-origin URLs, and non-HTTP(S) schemes (Node/file pages may use file:).  _(src: docs/notes/session12-runtime-controls.md)_
- Session 11 acceptance is not claimed complete until recorded together: a fresh full Chromium rotation with 0 FAIL/ERROR/timeout/OOM and no unapproved NAs, rotation-wide n≥5 warm wall and peak-memory parity (or a cell-specific ADR exemption), the #1 conformance-then-coverage aggregate + per-family scorecard, no PASS-to-FAIL in Chromium and WebKit plus the Firefox smoke/correctness gate, and green gate/≥90% coverage/anti-cheat/force-software/package budgets. The first sealed full Chromium export contained 563 cells.  _(src: docs/notes/session11-index.md)_
- Session-13 win definition: a feature is 'won' only under qualified evidence — same export, same rotation, warmup ≥1, n≥5, both engines passing, with a lead larger than run-to-run noise; a 3.44-vs-3.41 ms result is BEHIND/PARITY, not a win, and an empty loss list with no qualified contested coverage is not a win.  _(src: BUILD_INSTRUCTIONS_SESSION13.md)_
- Session-9 parity-exempt rule: if beating a rival would require producing less output than aibrush's strict oracle validates, the cell is recorded as an honest same-work-impossible tie (ADR + generator exemption), not a loss — matching a rival's laziness is only legitimate if aibrush's output still passes its own strict oracle.  _(src: BUILD_INSTRUCTIONS_SESSION9.md)_
- Standing capability ladder: prefer hardware WebCodecs → GPU → WASM (miss-only) → TypeScript; containers are implemented in TS; WASM cores are small permissive-licensed per-codec modules (never monolithic ffmpeg), self-hosted via import.meta.url (no CDN), lazy and miss-only, with no COOP/COEP on the common path.  _(src: BUILD_INSTRUCTIONS.md)_
- Streaming-output wins were among the few freshly-measured (not cached-single-sample) benchmark results, so that perf is considered trustworthy: mediabunny 18/27 via StreamTarget incremental writes, TS continuity, and headerless WebM live.  _(src: docs/architecture/09-operations.md)_
- The 2026-07-08 fair-harness upgrade rotates multiple real files per scenario with exhaustive scoring (metrics combine sum/max/median, a FAIL is never averaged into a PASS) and independently parses engine output so no engine grades itself — collapsing overfit passes and exposing large/long-file failures the single-fixture harness hid.  _(src: BUILD_INSTRUCTIONS_SESSION10.md)_
- The 558-feature benchmark is self-authored end-to-end (harness, oracles, golden bake, leaderboard, and the aibrush adapter that authors its own capabilities(), NA-classification regex MISS_RE, and graceful-rejection taxonomy); conformancePct is computed over non-NA cells so every NA is removed from the denominator — a real bug emitting a capability-miss-matching sentence silently becomes NA instead of FAIL. Recommendation: make typed CapabilityError the sole NA signal and delete the message-regex.  _(src: docs/competitive-gaps.md)_
- The Definition of Done requires test coverage ≥ 90% lines/branches on core + drivers, bit-exact validation in force-software, cross-browser correctness in Chromium/WebKit/Firefox (Playwright), and an aggregate benchmark win vs each of 7 reference engines measured fresh multi-sample.  _(src: BUILD_INSTRUCTIONS.md)_
- The GPU geometric-filter SSIM loss root cause is a canvas round-trip: gpu-video.ts draws to OffscreenCanvas producing a full-range sRGB VideoFrame (primaries bt709, transfer iec61966-2-1, matrix rgb, fullRange true) from limited-range BT.709 source (matrix bt709, fullRange false); the dominant error is the sRGB transfer curve forced onto BT.709 content, with range only a minor +6/255 luma lift term.  _(src: docs/notes/filter-colorspace-ssim.md)_
- The MP3 metadata probe() reads a bounded 16 KiB head, skips ID3v2, validates MPEG Layer III sync, reads Xing/Info counts when present, and otherwise estimates CBR duration from known total size; the repeated-prefix cache stores start-at-zero byte prefixes only, <=1 MiB, 60 s TTL (never parsed MediaInfo).  _(src: docs/notes/session9-public-probe-mp3-prefix.md)_
- The MP4 bounded packet-stream batching budget was fixed at 256 KiB (or 256 packets); a 1 MiB candidate was rejected before browser measurement because it prefetched the entire remaining stream and requested terminal close, so an abort after the first packet could no longer purge queued native chunks and the next read resolved instead of rejecting with the typed aborted error.  _(src: docs/notes/session13-mp4-packet-stream-batching.md)_
- The Session-12 launcher required a 7,200,000 ms allowance for a fresh headed full Chromium suite (rotation on, no reuse, fresh cache/profile); the earlier 1,800,000 ms allowance truncated the run.  _(src: BUILD_INSTRUCTIONS_SESSION12.md)_
- The WAV PCM mux fast path validates a canonical 44-byte WAV shape (RIFF, WAVE, a 16-byte fmt chunk, and data at byte 44), and the paired mux() consumes the prepared state only if its payload aliases the same buffer at offset 44 — the guard against returning an un-authored source buffer.  _(src: docs/notes/session9-wav-pcm-mux-fast-path.md)_
- The WebCodecs audio driver's queue-drain high-water mark was measured to 128 packets: fresh runs showed 8 and 32 still lost narrowly while 512 made burstiness and median worse (112.870 ms vs mediabunny 101.295 ms).  _(src: docs/notes/session9-opus-aac-mp4-audio-pipeline.md)_
- The WebM Video/AlphaMode element (0x53C0) is treated as positive alpha proof only when a single complete unsigned value of 1 appears inside a complete Video master; omission, explicit 0, unknown/empty/oversized/truncated integers, conflicting duplicates, and non-video placement are not proof, and probing BlockAdditions during metadata routing was rejected as requiring a full packet scan.  _(src: docs/notes/session13-webm-declared-alpha.md)_
- The benchmark harness is treated as a strict BLACK BOX from Session 10 on (upgraded 2026-07-08, commit 49169be): the engine team may never read its scenario/oracle/tolerance/runner/selection/output-parser/adapter source — reading how the test grades is defined as how overfitting starts; fixes are validated against ffmpeg/ffprobe/openssl/mediainfo and independently-baked goldens instead.  _(src: BUILD_INSTRUCTIONS_SESSION10.md)_
- The catalog-input-requirements repair changed 51 unreachable-conversion rows (validating 54 unique exact-source hashes) — these were not 51 missing aibrush implementations; the export's only real engine-declared gaps were HEVC Main10 output and two-pass bitrate.  _(src: docs/notes/session12-catalog-input-requirements.md)_
- The common path requires no COOP/COEP; cross-origin isolation is opt-in only for the fast-WASM-threads tail, because hardware WebCodecs (the fast path) needs zero isolation.  _(src: docs/architecture/01-goals-and-requirements.md)_
- The direct WAV s16 resample fast path uses a cached rational polyphase FIR with 6 Kaiser-windowed sinc zero crossings, beta 8.6, per-phase DC normalization, and output length round(inputFrames*outRate/inRate); it declines (falls back to the Float64 resampler) for non-s16, non-LE, unaligned, remix, or DSP requests.  _(src: docs/notes/session9-wav-s16-direct-resample.md)_
- The direct WAV s16 resample fast path uses a cached rational polyphase FIR with 6 Kaiser-windowed sinc zero crossings, beta 8.6, per-phase DC normalization, zero-extended edges, and output length round(inputFrames*outRate/inRate); it is not a replacement for the public high-quality Float64 windowed-sinc resampler (which uses the long 80 dB kernel), only a narrow same-channel s16 sample-rate-only path.  _(src: docs/notes/session9-wav-s16-direct-resample.md)_
- The h264_resize_4k_to_1080p transcode is a genuine ~11x throughput loss (aibrush 4k decode→resize→encode ~4000ms best-case vs mediabunny ~1090ms; earlier measured 63282ms vs remotion 8417ms) because resize cannot passthrough; it needs a true WebGPU limited-range resize (deferred due to color risk) — canvas-pool was insufficient.  _(src: docs/perf/goal26-evidence.md)_
- The harness adapter (engines/aibrush-media/adapter.ts) is the harness's own live file (edits are NOT vendored); its capabilities()/containersIn/containersOut/MUX_FAITHFUL_TARGETS/features gate what scenarios are attempted vs NA — declaring a genuinely-supported capability is a cheap coverage lever, but advertising unverified output is forbidden.  _(src: BUILD_INSTRUCTIONS_SESSION4.md)_
- The harness case-winner rule (media-test computeCaseWinner) ranks coverage-first (files passed desc) then metric aggregate (SUM for cost, MEDIAN for rates), direction-aware, with a co-win 3% band at the top coverage tier; since ops/packets/frames counts are equal per file across engines, ranking effectively reduces to 'lowest wall wins', and the measured window includes the op's own byte fetching from the served URL (range round-trips count).  _(src: docs/perf/goal26-evidence.md)_
- The implicit-AV1 warmup/latency threshold is 30.5 fps (uses latencyMode:'realtime' at or below, quality mode above with 8 warmup clones); the 30.5 tolerance deliberately absorbs rational-clock noise around nominal 30 fps such as 30.0000003 and is not an asset-derived threshold.  _(src: docs/notes/session13-video-rate-alpha.md)_
- The job-runner worker-boundary input is deliberately narrower than the flat host-facing MediaInput (excludes function-backed Source objects, URL instances, DOM elements, and live MediaStream handles) because those do not form one portable structured-clone contract.  _(src: docs/notes/session12-declarative-job-runner.md)_
- The large multitrack byte oracle uses a zero-allocation exhaustive first-difference loop rather than Vitest's recursive typed-array matcher, to preserve bit-exactness without a 4 GiB matcher heap blow-up.  _(src: docs/notes/session13-baseline-and-gate-repair.md)_
- The mux/h264_aac_to_ts row first failed correctness in Chromium because the lazy MPEG-TS muxer loaded only after the first stream wrote a packet, so a second track added post-load existed only in the proxy and its writes had no target track; fix was replaying pre-load tracks in order during #ensure() and mapping post-load tracks immediately.  _(src: docs/notes/session9-lazy-muxer-filter-proxies.md)_
- The pure-TS bench --check regression gate uses a 0.5 tolerance (fails only on a >50% aggregate throughput slowdown) — loose enough to absorb machine-to-machine variance, tight enough to catch an algorithmic regression; throughput is reported in physically-meaningful units (×realtime for DSP, MB/s for byte ops, probes/sec for probe, never file-MB/s which would be dishonest for a bounded header read).  _(src: docs/architecture/14-benchmarks.md)_
- The qualified speed-ledger closes a row as LEAD only when every rotation has the complete pinned seven-engine roster, identical selection/cohort identity, warm n>=5 wall and positive-memory observations, a wall lead larger than the pairwise sum of MADs (rivalMedian-ourMedian vs ourMAD+rivalMAD), and memory no greater than the leanest passing rival; missing/zero memory can never become a zero-cost observation or a LEAD, and an all-NA cohort is excluded.  _(src: docs/notes/session13-speed-ledger-generator.md)_
- The repeated-probe range cache was moved out of the eager engine closure into a lazy source module and made to cache byte intervals (not just byte-zero ranges) with a 1-MiB aggregate cap, 8-interval cap, 60-second lazy expiry, and LRU eviction; a two-interval head/tail special case was rejected as less general for files with multiple metadata islands.  _(src: docs/notes/session13-baseline-and-gate-repair.md)_
- The rotated 02.aac ADTS duration is derived from the codec clock 861*1024/44100 = 19.992380952 s; the prior 17.135660 s value was ffprobe's raw-VBR bitrate estimate contradicting the 861-frame packet vector (last PTS 19.969161 s) and an independent ffmpeg decode of 880,640 stereo sample frames (~19.97 s). Eleven catalog rows referencing those exact bytes were repaired.  _(src: docs/notes/session12-public-truth-boundaries.md)_
- The vendored wasm-aac Symphonia core is tried first for ADTS-to-WAV-s16 only when runtime is wasm-only, determinism is force-software, OR input is at most 256 KiB; larger inputs on Chromium keep the native AudioDecoder path.  _(src: docs/notes/session9-adts-aac-direct-wav-s16.md)_
- The vendored wasm-aac Symphonia core is tried first only for same-layout WAV s16 when runtime is wasm-only, determinism is force-software, OR input is at most 256 KiB; larger/DSP requests keep the canonical decode->transform->writeWav path.  _(src: docs/notes/session9-adts-aac-direct-wav-s16.md)_
- The verbatim FLAC route computes STREAMINFO MD5 over the original little-endian interleaved PCM payload and transposes WAV interleaved LE samples into FLAC planar big-endian subframes; it is restricted to no-DSP WAV s16 with no channel/rate change and no non-source PCM depth.  _(src: docs/notes/session9-wav-s16-flac-verbatim-fast-path.md)_
- The worker layer (WorkerStreamBridge/worker-entry/worker-protocol) was fully written and unit-tested but initially NOT wired into production — core.ts exported only the synchronous InlineBridge and tsup built no worker chunk — so heavy ops ran on the main thread until Session 4 wired the worker and proved longtasks≈0.  _(src: BUILD_INSTRUCTIONS_SESSION4.md)_
- Toolchain/env: build with bun 1.3.14 (not npm/npx; bun at /Users/tarek/.bun/bin/bun); gate = typecheck (3 tsconfigs) + biome ci + vitest --coverage + build + test:dist + check-budgets; the harness vendors the engine's dist/, so a stale dist yields a dishonest number and must be rebuilt+re-vendored (wiping stale hashed chunks) before every run.  _(src: BUILD_INSTRUCTIONS_SESSION4.md)_
- Two quick fixes for the geometric-filter colour defect were implemented, measured, and reverted (gpu-video.ts byte-identical to HEAD): re-tagging output to source colour space (encoder ignores tag, keeps full-range pixels, no SSIM change) and studio-range compressing RGBA (fixes mean 144.72 vs 143.99 but not transfer, no SSIM change); the correct fix is keeping geometry in native YUV planes.  _(src: docs/notes/filter-colorspace-ssim.md)_
- Two-pass H.264 is limited to convert() because it owns a replayable compressed source; caller-owned one-shot encode() frame streams and pure ReadableStream sources reject with a typed capability miss when twoPass:true (byte/Blob/URL/OPFS sources can open a fresh stream per demux pass). A single encode with bitrate set once is not two-pass and remains forbidden.  _(src: docs/notes/session12-two-pass-h264-design.md)_
- Under force-software, Canvas2D filter substrate is removed (with WebGPU/WebGL) even though it survives the filter predicate, because the architecture records drawImage as GPU-accelerated; native CPU and WASM remain eligible with native first.  _(src: docs/notes/session12-force-software-routing.md)_
- VP9/AV1 high-depth encode uses profile 0 for 8-bit and profile 2 for 10/12-bit VP9 (vp09.PP.LL.DD), Main profile 0 for 8/10-bit and Professional profile 2 for 12-bit AV1 (av01.P.seq_level_idxM.DD, with a 3× Main-tier bitrate factor for profile 2); unknown cadence selects the highest defined level rather than inventing 30 fps because that would under-declare VFR/high-rate output, and requests outside the defined level tables are rejected rather than advertising an under-level string.  _(src: docs/notes/session12-high-bit-depth-profiles.md)_
- Video-filter routing was fixed by combining metrics as pixelWork = (sourceW*sourceH + outputW*outputH) * frames instead of logical-OR of duration and area, because a 0.1s 4K resize (31,104,000 ops) was mis-classified tiny and routed to the CPU filter whose full-RGBA scaling cost scales with every source+dest pixel; the tiny boundary is 245,760 pixel operations (64x64 identity over 30 frames).  _(src: docs/notes/video-filter-total-pixel-routing.md)_
- WAV frame-encode rejected a direct second RIFF writer (would duplicate format/endian/multi-track validation and drift from the strict-tested WavMuxer) and wrapping raw PCM in a fake EncodedAudioChunk (PCM is not a coded WebCodecs chunk); omitted/generic pcm selects pcm-f32 as the lossless representation of the canonical AudioData copy seam.  _(src: docs/notes/session12-wav-frame-encode.md)_
- WAV mux serializes on finalize via the existing writeWav PCM writer after decoding to canonical planar PCM rather than hand-concatenating bytes into data; hand-concatenation is faster but would duplicate sample-format validation and let mux output drift from the validated PCM writer.  _(src: docs/notes/session8-wav-mux.md)_
- WAV trim seekable range slicing is kept only for files larger than 1 MiB; the 960 KB leaderboard fixture uses one full read because two range requests were measurably slower.  _(src: docs/notes/session9-wav-pcm-trim-byte-slice.md)_
- WAV/AIFF/CAF tag rewrite uses a lazy metadata helper rather than reserializing through PCM transform writers; the reject rationale is that reserialization would rewrite headers/payload placement unnecessarily, weaken the 'bytes elsewhere unchanged' oracle, and hide container-specific tag placement bugs.  _(src: docs/notes/session8-metadata-write.md)_
- WavDriver.probe first reads 4 KiB (covers ordinary RIFF/WAVE fmt+data), and only falls back once to the 64 KiB demux header window if a valid fmt sits before a larger metadata chunk; files with metadata past 64 KiB fail via the typed parser rather than scanning a multi-hundred-MB PCM payload.  _(src: docs/notes/session9-wav-bounded-probe.md)_
- adtsTrimFromUrl() caches only raw source bytes capped at 16 entries, 1 MiB per entry, 60 s TTL, keyed by URL and known size; it never stores packet tables, oracle measurements, or trimmed outputs.  _(src: docs/notes/session9-adts-url-trim-cache.md)_
- aggregate.mjs ranks engines by conformance% DESC then coverage% DESC as tiebreak; reaching 100% PASS (zero N/A, FAIL, ERROR) makes an engine #1 on both axes so no engine can beat it.  _(src: BUILD_INSTRUCTIONS_SESSION5.md)_
- aibrush codec reality: FLAC decode is pure-TS bit-exact (closes the Chrome-149 no-FLAC hole); Vorbis decode is real-wasm-core and genuinely valuable because no browser has a Vorbis decoder; AAC and MP3 decode are real-wasm-core (Symphonia); PCM is pure-TS both directions.  _(src: docs/competitive-gaps.md)_
- check-budgets reads each emitted JS source map to reject heavy implementation sources in the eager/default-probe static closure (surviving hashed chunk-name changes); filename guards remain a second independent check. This guards against source-map attribution drift, not just filenames.  _(src: docs/notes/session12-eager-budget-recovery.md)_
- dav1d-on-wasm requires -Dfake_atomics=true because the LLVM-wasm backend emits real atomic ops for single-threaded code that Emscripten refuses to validate; fake-atomics rewrites them to non-atomic (single-thread safe).  _(src: docs/notes/wasm-codec-cores.md)_
- decryptCencFile parses raw ISO-BMFF bytes with reader.ts primitives only (no parse.ts/mp4-driver.ts coupling) specifically to avoid an import cycle.  _(src: docs/notes/cbcs-real-world-decrypt.md)_
- demux rows (size_micro_audio/h264) lose to mediabunny because Mp4Driver.packetInfo runs enrichAvcPictureClassification, reading+parsing mdat sample NALs to find non-IDR I-frames (open-GOP random-access points) that mediabunny skips by trusting stss; it is load-bearing (skipping it breaks mp4-prepared-mux.test.ts 'reads exact MP4 packet info' golden), so aibrush reports MORE correct keyframes at a read+parse cost.  _(src: docs/perf/goal26-evidence.md)_
- opus_to_webm_audio loss was fixed overhead not a missing muxer: the measured mux entered public Ogg demux, built browser EncodedAudioChunk objects, copied them back to harness packet payloads, then went through public engine.mux() dispatch for a bounded single-audio packet copy; fix removed those wrappers via a pure Ogg de-lacer feeding writeWebm().  _(src: docs/notes/session9-ogg-audio-webm-mux-fast-path.md)_
- rav1e (AV1 encode) builds on wasm32 despite the common 'rav1e can't do wasm' claim because v0.8.0 aliases rayon to maybe-rayon, which compiles to a single-threaded shim when the threading feature is off; --no-default-features drops asm (nasm/cc), threading (real rayon threads trap on wasm32-unknown-unknown), binaries, and git_version.  _(src: docs/notes/wasm-codec-cores.md)_
- remux trackSelect + tags were previously rejected together before demux; the fix applies tags after selection (applying to input before selection is wrong for cross-container remux), draining the selected target stream into one bounded buffer then invoking the target container's metadata writer — explicit buffering because these tag writers require random access.  _(src: docs/notes/session12-remux-select-tags.md)_

## Competitive gaps (vs mediabunny / ffmpeg.wasm / mp4box / web-demuxer / remotion* / platform)  (40)

- A rival's peakMemory:0 means unmeasured, not zero — only compare metrics both engines genuinely instrument; treating an unmeasured rival value as a target is a fake bar.  _(src: BUILD_INSTRUCTIONS_SESSION9.md)_
- ADR-110/105: Mediabunny passes Firefox VPx alpha-preserving transcode (vp9_alpha_to_vp9 and vp9_alpha_to_vp8) while aibrush's dual-WebCodecs VP9 alpha encoder times out at the 120 s cap on Firefox for both VP8 and VP9 targets, so aibrush declines Firefox VPx alpha transcode and keeps Chromium as its alpha-transcode validation browser.  _(src: docs/architecture/02-decision-records.md ADR-110)_
- ADR-203: black-box results measured aibrush's 360p VP9 decode at 476.315 ms vs 117.845 ms and tiny H.264 decode at 265.385 ms vs 105.940 ms because createVideoDecoder() discarded the supports()-accepted hardware acceleration hint and always configured 'no-preference' under auto; fixed by caching the UA-accepted acceleration rung in a 64-entry LRU and running configure()+empty flush() as a barrier before packet submission.  _(src: docs/architecture/02-decision-records.md ADR-203)_
- ADR-204: black-box export chromium-2026-07-10T23-25-04-841Z.json selected 01.mov, passed the exact 42,276-packet golden and measured 986.085 ms, while the deficit report records web-demuxer at 10.910 ms.  _(src: docs/notes/session11-mp4-avc-key-picture-proof.md)_
- ADR-293: on the fresh synced Chromium mp3→opus export, aibrush measured 42.345 ms vs Mediabunny 41.725 ms — within the 3% winner noise band (strict metadata/playback PASS for aibrush, Mediabunny, and Remotion).  _(src: docs/architecture/02-decision-records.md ADR-293)_
- Benchmark bundle sizes: mp4box ~41 kB, web-demuxer ~43 kB, remotion-media-parser ~73 kB, remotion-webcodecs ~94 kB, mediabunny ~165 kB, ffmpeg.wasm = multi-MB WASM core loaded up front; aibrush targets mediabunny-class eager JS (~150-250 kB).  _(src: docs/architecture/background/benchmark-summary.md)_
- Benchmark per-family winners: mediabunny won probe 32/51, demux 19/43 (ffmpeg.wasm 10), mux 46/52, remux 25 (ffmpeg.wasm 22), trim 31/42, transcode 50/84 (ffmpeg.wasm 19, remotion-webcodecs 9), streaming-output 18/27, robustness 31/60; ffmpeg.wasm won audio-dsp 25/36.  _(src: docs/architecture/09-operations.md)_
- ffmpeg.wasm beats aibrush on audio-DSP throughput (WebCodecs gives no HW edge on PCM: ~2.4x on a 1h resample, ~34x on a downmix) and on the exotic long tail of codecs/containers/filters plus SW encoders.  _(src: docs/competitive-gaps.md)_
- ffmpeg.wasm is multi-MB precisely because it chases the exotic codec/container/filter long tail, which aibrush-media declares a non-goal.  _(src: docs/architecture/01-goals-and-requirements.md)_
- In a same-export focused Chromium run of audio-dsp/throughput_decode_s24 on real 03.wav, both aibrush and Mediabunny passed the strict 4,096-frame oracle, four engines were honest NA_ENGINE, and FFmpeg reported an infrastructure error loading its vendored module before any oracle ran.  _(src: docs/notes/session12-public-truth-boundaries.md)_
- In encryption, ffmpeg.wasm won `cenc-ctr` and mediabunny won `cbcs` (subsample pattern), both via real crypto not canned output.  _(src: docs/architecture/09-operations.md)_
- mediabunny (~165 kB, zero-WASM, single active author) ships the exact aibrush thesis (WebCodecs-first + pure-TS containers, no WASM, no COOP/COEP) and wins 56% of the benchmark; aibrush has no structural advantage over it on the shared substrate — the only levers are the WASM tail and a future GPU rung.  _(src: docs/competitive-gaps.md)_
- mediabunny was the overall 56%-winner of the original browser-media benchmark, proving the WebCodecs+TS spine that aibrush-media extends with a lazy WASM tail and GPU filters.  _(src: docs/architecture/README.md)_
- mp4box (~41 kB) beats aibrush on smallest-bundle (conceded non-goal) and ISO-BMFF box-format depth (edit lists, exotic boxes, CMAF segmentation); web-demuxer (~43 kB + WASM, bilibili) beats it on demux/probe breadth for browser-unsupported codecs (HEVC/AV1 where WebCodecs decode is absent); web-demuxer's iterate-packets-medium win is a suspect hardcoded sample-table shortcut.  _(src: docs/competitive-gaps.md)_
- mp4box wins the smallest-bundle contest at ~41 kB; aibrush-media deliberately trades some size for coverage and speed rather than chasing that.  _(src: docs/architecture/01-goals-and-requirements.md)_
- On decode-seek/decode_vfr_timing (h264_vfr.mp4, 2,279,109 bytes, 111 access units), aibrush passed the 12-frame oracle but measured 393.685 ms and 672,616,697 peak bytes versus remotion-webcodecs at 382.330 ms and 107,579,095 bytes; the ~565 MB gap was attributed to consumer-retained native/GPU surfaces (~11.2 MB per handed GPU-backed frame), not driver queues.  _(src: docs/notes/session13-vfr-decoder-surface-memory.md)_
- On demux/flac_noseektable (02.flac, SHA c01f4a0e…, 8 golden packets), aibrush passed at 2.810 ms (MAD 0.290) versus remotion-webcodecs at 1.920 ms (MAD 0.030), an open loss attributed to transport/runtime measurement rather than FLAC parsing (fused parser itself is 0.0038 ms).  _(src: docs/notes/session13-flac-packet-info-fused.md)_
- On demux/size_massive_massive_h264_1080p_2h (553,501 packets), aibrush measured 1,480.830 ms versus web-demuxer at 54.770 ms; MP4 cannot derive exhaustive H.264 key-picture truth from the stss table alone because non-IDR I/SI pictures require parsing the first VCL slice header in every candidate access unit, which is the mandatory correctness cost.  _(src: docs/notes/session13-mp4-avc-in-memory.md)_
- On mux/h264_aac_to_mov, aibrush measured 21.160 ms (MAD 1.260) versus mediabunny 13.920 ms (MAD 0.825), a 1.52x loss caused by faststart serializing the complete moov twice (once with zero offsets to learn length, once with final offsets).  _(src: docs/notes/session13-mp4-faststart-one-pass.md)_
- On mux/swap_audio_video_with_opus_to_mkv, aibrush measured 202.400 ms versus mediabunny 53.115 ms; a Bun control showed the prepared-writer double-copy penalty was only 2.077 ms and the Chromium bare-vs-owned difference vanished, so ADR-257 rejected a prepared-only payload union.  _(src: docs/notes/session13-webm-prepared-writer-profile.md)_
- On mux/video_plus_audio_to_mp4, aibrush measured 228.685 ms (MAD 14.955) versus mediabunny 49.785 ms (MAD 0.715); multitrack MP4 wrapped supplied packet arrays in promise-backed streams draining one packet per pull.  _(src: docs/notes/session13-mp4-multitrack-array-crossover.md)_
- On performance/size-ladder-iterate-packets-large with 01.mp4 (SHA 05f832e4…, 9,982 packets), aibrush measured 71.285 ms (MAD 1.380) versus mp4box@2.3.0 at 45.550 ms (MAD 1.895); root cause was a duplicated top-level header walk issuing seven range reads.  _(src: docs/notes/session13-mp4-demux-layout.md)_
- On probe/realworld_mdn_flower_webm (541,606-byte WebM, Tracks ending at byte 4,749), aibrush measured 3.280 ms versus remotion-webcodecs 1.895 ms because the former 4 KiB first window stopped 653 bytes early and forced a second 64 KiB ladder read; ADR-258 raised the first WebM/MKV metadata window to 8 KiB.  _(src: docs/notes/session13-webm-metadata-window.md)_
- On small ADTS-AAC-to-WAV extraction, ffmpeg.wasm was originally faster (24.440 ms vs aibrush 52.865 ms) because libavformat/libavcodec decode and WAV s16 muxing stay inside one tight wasm pipeline for the elementary stream.  _(src: docs/notes/session9-adts-aac-direct-wav-s16.md)_
- On the passing 03.wav s24 rotation, aibrush measured 58.590 ms wall versus Mediabunny 19.200 ms — aibrush was slower; the ADR-226 decodePcmAudioStream direct-path fix reads one bounded 64 KiB prefix so header + first PCM chunk share one range round trip.  _(src: docs/notes/session12-public-truth-boundaries.md)_
- On transcode/aac_to_opus_webm, aibrush leads wall durably at 56.190 ms but its 32,421,157-byte peak exceeds the leanest passing rival by 7,029,024 bytes; a codec-phase staging experiment (decode-then-encode, 4 MiB cap) was rejected because it regressed wall 20.9% (56.37 vs 46.64 ms) with equal terminal memory.  _(src: docs/notes/session13-selective-native-audio-transcode.md)_
- On transcode/opus_to_aac_mp4 mediabunny was ~3x faster (91.670 ms vs aibrush 274.290 ms) because it paced WebCodecs backpressure via the native 'dequeue' event; aibrush had been polling decodeQueueSize/encodeQueueSize with setTimeout(0), paying hundreds of macrotasks.  _(src: docs/notes/session9-opus-aac-mp4-audio-pipeline.md)_
- On WAV copy trim, mediabunny handles audio-only WAV copy trim as packet geometry plus a WAV mux, avoiding a decode path; aibrush keeps its own exact byte-window writer and removes only repeated source acquisition/routing overhead.  _(src: docs/notes/session9-wav-url-trim-cache.md)_
- Out-of-benchmark threats not in the 7: ffmpeg.wasm-mt under COOP/COEP (multi-thread beats the transcode races aibrush won single-threaded, plus AV1 encode); LibAV.js (Yahweasel) with libavfilter for real filtergraphs; specialized single-codec WASM (libopus, fdk-aac, lamejs, libflac.js, libvorbis.js) each out-features aibrush's general encoder on its one codec; hls.js/Shaka/dash.js own adaptive streaming/DRM (out of scope).  _(src: docs/competitive-gaps.md)_
- probe/hls_aes128 failing invocation supplies only the 920,272-byte first-segment AES ciphertext (SHA-256 4417b34d…, head e67b35246777a06465c23850f1227f96) without playlist URI/timeline/key/IV; the scenario playlist carries key 366a6383… and explicit IV 953e5e23… and OpenSSL recovers all five clear segments; the full HLS program probes as 10.0267 s H.264 1280x720 + AAC 48 kHz stereo TS. Ciphertext alone is information-theoretically insufficient.  _(src: docs/notes/session11-fair-harness-boundary-audit.md)_
- remotion-webcodecs (~94 kB) beats aibrush on convert-path maturity (backpressure-tuned pipeline) on contested transcode/decode-seek margins; its remux/huge_600s 'win' is a suspect input-passthrough ftyp-flip.  _(src: docs/competitive-gaps.md)_
- Rivals win the catastrophic probe/demux cells by lazily parsing moov/cues and walking the sample table (emitting packet descriptors: offset/size/pts) without touching payload bytes, whereas aibrush read/parsed/materialized the whole stream — e.g. probe/perf-extract-metadata-massive us 17.5 s vs mediabunny 44 ms; demux massive us 46 s vs remotion 40 ms.  _(src: BUILD_INSTRUCTIONS_SESSION9.md)_
- Session 13 per-feature speed losses at handoff: probe/h264_4k_10s aibrush 18.86 ms vs remotion-media-parser 12.77 ms; probe/hevc_1080p_10s aibrush 3.44 ms vs mediabunny 3.41 ms (razor-thin loss, treated as BEHIND); probe/h264_vfr aibrush 33.11 ms vs remotion-media-parser 29.6 ms. aibrush already led probe/h264_1080p_30s, realworld_mdn_flower_mp4, h264_bframes_1080p.  _(src: BUILD_INSTRUCTIONS_SESSION13.md)_
- Session 13 pinned rival roster and versions: mediabunny@1.48.0, ffmpeg.wasm@0.12.15, mp4box@2.3.0, remotion-media-parser@4.0.479, web-demuxer@4.0.0 (vs aibrush-media@dev).  _(src: BUILD_INSTRUCTIONS_SESSION13.md)_
- Session 4 fresh five-axis audit (2026-06-26): aibrush-media ranked #2 of 8, behind mediabunny, with 0 wins / 0 losses / 13 family ties — 414 PASS vs mediabunny 510 and ffmpeg.wasm 486; webkit/firefox had not been run.  _(src: BUILD_INSTRUCTIONS_SESSION4.md)_
- The `platform` engine using HTMLMediaElement `loadedmetadata` was 600-7000x slower at probe than mediabunny's cheap `getDurationFromMetadata` header read; this is why probe never uses `<video>`.  _(src: docs/architecture/09-operations.md)_
- The final Session 13 all-engine acceptance matrix (Chromium 149, six engines) produced aibrush leadership on all 13 probe rows including probe/longform_1h_audio 21.375 ms vs MediaBunny 31.070 ms, probe/aac_adts 12.120 vs 17.215, probe/recorder_headerless 3.765 vs 6.410, and probe/pcm_s16be 23.740 vs ffmpeg.wasm 28.975.  _(src: docs/notes/session13-probe-leadership-2026-07-13.md)_
- The raw platform (Chrome-149, no library) is a floor aibrush can tie but never beat on lowest-overhead native decode/seek because it calls VideoDecoder directly with no router/worker/driver indirection; it NAs everything needing a real muxer.  _(src: docs/competitive-gaps.md)_
- The separate real two-hour MP4 corpus (mp4-packet-truth.md) proves stss is not general picture truth: it contains 261 non-IDR I pictures outside its 1,680 declared sync samples.  _(src: docs/notes/session11-mp4-avc-key-picture-proof.md)_
- The Session 13 all-engine harness registers six engines (aibrush-media, MediaBunny, ffmpeg.wasm, MP4Box/mp4box@2.3.0, Remotion/remotion-webcodecs, web-demuxer/web-demuxer@4.0.0) against a pinned seven-engine roster requirement; a typical matrix run of 78 cells yielded 51 strict passes, 25 honest unsupported/NA cells, and two competitor errors.  _(src: docs/notes/session13-probe-leadership-2026-07-13.md)_

## Known bugs & envelope caveats  (114)

- 3 SUSPECT competitor wins must not be emulated: remux/huge_h264_1080p_600s_mov_to_mp4 'won' by flipping 8 ftyp bytes and returning the input; two performance cases won on a hardcoded per-asset path and a degenerate empty metric.  _(src: docs/architecture/background/benchmark-summary.md)_
- 542 of 555 benchmark winners were measured on a cache-seeded (cached) run: correctness verdicts are trustworthy but single-sample performance margins must be re-measured before being quoted.  _(src: docs/architecture/background/benchmark-summary.md)_
- A historical qualified WAV memory row remains open: selected 02.wav passed at 14.255 ms but measured 32,392,187 bytes peak versus mediabunny's 23,956,253 bytes; the row cannot close until a current-bundle same-export rerun proves peak at or below 23,956,253 bytes.  _(src: docs/notes/session13-wav-s24-fused.md)_
- A probe/opus correctness regression occurred because the browser harness dropped manifest sizeBytes when creating URL-backed sources; without known size the Ogg driver could not range-read the tail page and reported ~4 s instead of the true 10.007 s.  _(src: docs/notes/session9-ogg-opus-probe-fast-path.md)_
- A production defect was found where interval validation ran before the retimer's ownership finally block, so an unrepresentable positive tail rejected with the correct typed error but leaked its final frame; fixed by moving validation inside the try/finally.  _(src: docs/notes/session12-branch-coverage-recovery.md)_
- A public large-demux memory row retained 189,426,678 bytes versus the leanest passing rival at 85,555,893 bytes; escapes included packet streams pinning the whole file via cachedWhole() subarrays, MP4A esds codec-private metadata being a subarray of the whole file, and V8 giving async demux() methods one shared closure context holding the raw random-access local.  _(src: docs/notes/session13-mp4-packet-terminal-release.md)_
- A real 600s .mov (huge_h264_1080p_600s.mov variant) enumerated 2 tracks vs ffprobe's 3 with the audio track mis-typed 'other'/codec '' because the old v0-only sound-description parser mis-read a QuickTime version-2 sound entry as channels=3/sampleRate=1 (the always3/always65536>>16 constants) and failed to find the codec.  _(src: docs/notes/qtff-mov-parsing.md ADR-185)_
- AAC decode is AAC-LC mono/stereo only (Symphonia rejects SBR/HE-AAC and >2 channels); MP3 and FLAC encode are entirely missing so *→mp3 and *→flac are unconditional hard misses (WebCodecs has no encoder either).  _(src: docs/competitive-gaps.md)_
- ADR-040: a WebCodecs decoder/encoder output callback fires asynchronously and can enqueue into a readable that a consumer (most sharply seek, which cancel()s the instant it finds its target frame) already closed, throwing inside the UA callback and leaking the VideoFrame/AudioData; fixed by enqueueOrClose/enqueueChunkOrDrop guards that catch the check→enqueue race and close the frame.  _(src: docs/architecture/02-decision-records.md ADR-040)_
- ADR-043: a lossless keyframe stream-copy trim cannot detect entropy-coded H.264/AAC payload corruption (128 seeded bit-flips leave the box tree byte-identical, all sample ranges in-bounds, AVCC NAL-length framing summing exactly for all 900 samples); the trim/robust_bitflipped_source row stays a documented honest can't-detect rather than filename-matching like the ffmpeg-wasm reference.  _(src: docs/architecture/02-decision-records.md ADR-043)_
- ADR-073: running the corrupt-input matrix on real fixtures exposed raw RangeError leaks (not typed MediaError) in MP4 table parsing, WAV fmt chunk probing, Ogg short-page identification, AIFF SSND prefix reading, and AVI avih/strh/strf header decoding; fixed structurally (bounds-check declared fixed-width reads) rather than with blanket try/catch.  _(src: docs/architecture/02-decision-records.md ADR-073)_
- ADR-087: the first real-browser worker run crashed ('Target page/browser has been closed' + ~59 recurring 404s) because the harness adapter builds a fresh createMedia() per operation and each engine had its own pool cache, so a full run spawned one Worker per op (measured 6 engines → 6 Workers), each lazily re-loading ~900 kB of per-codec wasm cores; fixed by a process-wide worker pool singleton keyed by pool size (SHARED_POOLS).  _(src: docs/architecture/02-decision-records.md ADR-087)_
- ADR-087: Vite's production `vite build` inlines new URL('./worker.js', import.meta.url) as a data:text/javascript;base64 URL (import.meta.url has no directory), so the code-split worker's relative ./chunk-*.js/./engine-*.js imports throw Invalid URL/404 on boot — invisible in the Vite dev server; the consumer-side fix is serving the prebuilt vendor raw (never re-processed), since inlining the Worker literal makes Vite try to re-bundle and fail a code-split build.  _(src: docs/architecture/02-decision-records.md ADR-087)_
- ADR-105: four permanent honest-NA rows on Chromium — MP3 encode (no LGPL LAME/Shine in default build), HEVC Main10 output (WebCodecs exposes no portable 10-bit HEVC encode), H.264 two-pass (WebCodecs has single-pass bitrate only, no first-pass stats API), and two massive-materialization safety declines; the shipped Symphonia MP3 tail is decode-only.  _(src: docs/architecture/02-decision-records.md ADR-105)_
- ADR-190: MP4 AudioSampleEntry channel/rate defaults are unreliable — a real AAC-LC mono track reported stereo from a stale two-channel default, and HE-AAC reported 24 kHz when its SBR presentation is 48 kHz; parseEsds() now parses the AudioSpecificConfig at the bit level (ASC 11 88 = AAC-LC/48 kHz/channelConfig 1; 13 08 56 e5 98 = 24 kHz LC core + syncExtensionType 0x2b7 SBR to 48 kHz), probing 8 files at 3.733 ms median with a 3.95 MiB RSS delta.  _(src: docs/architecture/02-decision-records.md ADR-190)_
- ADR-203: supports() asked Chromium about prefer-hardware first and could report a hardware win, but createVideoDecoder() discarded that accepted choice and always configured no-preference under auto — a real routing/configuration mismatch violating ADR-002 and docs 04/09, general to every H.264/HEVC/VPx/AV1 decode.  _(src: docs/notes/session11-webcodecs-video-exact-acceleration.md)_
- ADR-293: transcode/mp3_to_opus_webm on a real 02.mp3 produced a 12.9707 s output (622592/48000) vs a 14.1177 s source because the source Xing/LAME gapless sample count was interpreted as if already in Opus' 48 kHz sample domain; fixed by not passing source TrackInfo.gapless to an Opus output (the encoder's OpusHead/CodecDelay are the only authoritative output priming facts).  _(src: docs/architecture/02-decision-records.md ADR-293)_
- aibrush has zero working software encoder for any lossy codec; the entire encode side is WebCodecs-or-nothing — on any browser/config where WebCodecs lacks the encoder the encode is a hard miss.  _(src: docs/competitive-gaps.md)_
- aibrush-media@dev does not declare feature 'depth:10bit-output' or 'two-pass', so the public harness reports both rows as NA_ENGINE even though standalone product proofs validate the Main10 route and two-pass output.  _(src: docs/notes/session12-catalog-input-requirements.md)_
- An attempted native MP4 packet-copy rewrite for gapless AAC was rejected because it decoded to 45,056 samples instead of the padding-removed 44,673: packet-copy preserved the leading edit but not the trailing padding trim, so the fast path had to be whole-source byte identity instead.  _(src: docs/notes/session9-gapless-aac-identity-trim.md)_
- An isolated product-only Chromium probe found generic image sniffing added a redundant [0,4096) URL range before the WAV [0,65536) range; ADR-227 removes that redundant read for known media extensions while keeping unknown/image sniffing intact.  _(src: docs/notes/session12-public-truth-boundaries.md)_
- bun run gate printed all 45 green anti-cheat checks with no active Node handles but never reached natural quiescence because Bun retains internal WebCrypto worker state after the CENC/HLS checks; fixed by an explicit exit-zero after main() prints the success summary.  _(src: docs/notes/session12-gate-exit.md)_
- By design the gated ADTS-deframer's last frame lands one AAC frame later than ffprobe's raw last packet PTS (372596 vs 370506 ticks on bear), while materialized duration still matches ffprobe within one frame and the PTS list is exact-cadence monotonic.  _(src: docs/notes/mpegts-aac-deframe.md ADR-184)_
- cens/cenc CTR modes do not support cbcs constant-IV mode and require per-sample IVs of 8 or 16 bytes; all-zero crypt:skip patterns are rejected as malformed; tenc pattern exists only in version 1 (version 0 falls back to full CTR 1:0).  _(src: docs/notes/decrypt-cens.md)_
- Chromium encoded 626 monotonic-PTS VFR frames but kept a nominal 16,667µs duration across small cadence corrections; the muxer's cumulative rounded durations put parsed PTS 11µs behind DTS by frame 17 and produced FFmpeg 'Invalid timestamps' warnings (DTS exceeding PTS by 1-2 ticks) late in the file — fabricated reorder, not B-frames.  _(src: docs/notes/mp4-vfr-encoder-mux.md ADR-191)_
- coverage-parity-exemptions.json currently registers only ADR-109's two MP3-encode rows, HEVC Main10 output, and H.264 two-pass mode as sanctioned NA_ENGINE/NA_BROWSER coverage boundaries; any other engine NA is an active coverage red when a rival PASSes the same cohort.  _(src: docs/notes/session11-rotated-deficit-gate.md)_
- Cross-browser MSE element-sink playback was not verified — the agent had no available browser runtime, so it remains an explicit parent-session gate with no browser-playback claim inferred from deterministic event doubles.  _(src: docs/notes/session12-element-sinks.md)_
- dav1d (AV1 decode) is blocked in the build sandbox because it needs Meson (absent; requires pip install meson ninja); nasm is also missing but not needed since libvpx (--disable-runtime-cpu-detect) and rav1e (--no-default-features) avoid SIMD asm.  _(src: docs/notes/wasm-codec-cores.md)_
- Definitive HLS AES boundary finding: the harness supplies 920272 bytes (head e67b3524...) which is the raw first-segment ciphertext hls_aes128_000.ts (SHA-256 4417b34d...), not the 378-byte #EXTM3U manifest; full-segment AES-CBC ciphertext is indistinguishable from random, so track count/codecs/dimensions/duration cannot be recovered without the manifest's key URI hls_aes128.key and IV 953e5e232e1585e615d9164ece153cf2.  _(src: docs/notes/hls-aes128-iv.md)_
- Encrypted-EXT-X-MAP probe returned duration zero because the 4KiB small-faststart metadata prefix ended inside a larger moov, so the optimization fell to the simple-video shortcut reporting init-segment mdhd duration (zero); range-reading the crossing moov and forcing the fragment-timing scan yields real 5.2s video / 5.20127s audio.  _(src: docs/notes/hls-aes128-iv.md)_
- Fair-harness audit found a real Source-wrapper bug: a probe-cache wrapper created with object spread could not see a URL Source's late-learned size, and probe→decode could consume a cached prefix through a fresh URL Source without a new request; fixed with live size/effective-URL accessors and learned-size prefix handoff (14 affected cells pass in chromium-2026-07-10T23-22-34-796Z.json).  _(src: docs/notes/session11-fair-harness-boundary-audit.md)_
- Fair-harness quality FAILs (Session 10): h264_rotate_normalize SSIM 0.946 (gate 0.98); multitrack_select_default_audio SSIM 0.950; decode_mov_h264 SSIM 0.854 (4 rivals pass — genuine .mov decode bug); meta_pts_monotonic_after_reorder yielded no decoded frames after B-frame reorder.  _(src: BUILD_INSTRUCTIONS_SESSION10.md)_
- FLAC frame sync bytes (0xff) can appear inside payload bytes, so frame selection must parse and CRC-check headers rather than scan only for 0xff; mode:'accurate' FLAC trims still need the ADR-096 decode/slice/re-author path for exact sample cuts.  _(src: docs/notes/session9-flac-keyframe-trim-fast-path.md ADR-096)_
- fMP4 SAMPLE-AES and SAMPLE-AES-CTR decryption remain typed non-claims until real test vectors exist; only cenc/cens/cbcs, hls-aes128, and TS hls-sample-aes are implemented.  _(src: docs/architecture/09-operations.md)_
- For the massive MP4 row, the payload-free driver table matches the freshly ffprobe-baked 553,501-packet golden by (track,PTS,DTS,size) but the public invocation reports the same count/timestamps yet loses 214,646 sizes — a stripped-sizes public boundary, not a product defect.  _(src: docs/notes/session12-public-truth-boundaries.md)_
- For the s24 row, aibrush's complete interleaved f32 output SHA-256 (7eafe0fe...) is byte-identical to ffmpeg and its first stereo-frame digest is 2c3498ed..., but the public oracle expects unrelated c660ee15... bytes; the red isolates to the stale 01.wav golden (4,096/4,096 mismatches reproduced), while 02.wav/03.wav rotations pass 4,096/4,096.  _(src: docs/notes/session12-public-truth-boundaries.md)_
- Gapless dispute (05.mp4): aibrush and FFmpeg independently report 50,176 samples for the complete stream, the committed expectation of 50,784 samples is disputed, and the harness operation-window reports 48,128 samples (a separate cutoff).  _(src: BUILD_INSTRUCTIONS_SESSION12.md)_
- GPU geometric filters (rotate/resize/crop/flip) drop luma quality to Y-SSIM ~0.86 (bear rotate180 Y-SSIM 0.866, resize 640x360 0.864) while a plain H.264 re-encode scores SSIM ~0.99 (bear 0.9875, obs 1.000); chroma stays high (U 0.97, V 0.98).  _(src: docs/notes/filter-colorspace-ssim.md)_
- Guaranteed bit-identical hardware output across machines is an explicit non-goal; only the force-software mode promises cross-machine-reproducible output because hardware decode is platform-specific.  _(src: docs/architecture/01-goals-and-requirements.md)_
- H.264 10/12-bit output and HEVC 12-bit output remain typed capability misses (even though AVC High10 profile 110 has a known identifier) because the repo has no independently proven browser encode plus mux/reimport path; VP8 is 8-bit only; a 12→10 depth request is a typed miss because there is no proven 10-bit pixel conversion stage. Every crop/resize/pad/rotate/flip/colorspace/tonemap filter is treated conservatively as an 8-bit pixel boundary.  _(src: docs/notes/session12-high-bit-depth-profiles.md)_
- H.264/HEVC/AV1 have no SW decode fallback at all (no scaffold dir exists) despite doc 10 promising wasm fallbacks; AV1 has no dav1d and no AV1 scaffold dir.  _(src: docs/competitive-gaps.md)_
- Harness corpus defect: demux/graceful_mp4_header_destroyed and graceful_webm_header_destroyed base assets are destroyed but rotated 01/02/03 files retain valid container headers that ffprobe recognizes and FFmpeg decodes, so a conforming demuxer must not reject them; each rotation needs the intended destructive mutation before rebaking.  _(src: docs/notes/session11-fair-harness-boundary-audit.md)_
- Harness corpus defect: transcode/h264_crop_center exports crop 1440x810 at (240,135), but rotations are 1280x720, 960x540, and 1080x1920, so the requested rectangle is outside every rotated source; clamping would change the requested operation.  _(src: docs/notes/session11-fair-harness-boundary-audit.md)_
- Harness corpus defect: trim/vp9_noop_full_range_idempotent exports duration 10 s but requires equality with input duration while rotations are 26.019, 46.548, and 224.107 s, so {start:0,end:10} is a prefix not a full-range no-op.  _(src: docs/notes/session11-fair-harness-boundary-audit.md)_
- Harness massive H.264 packet golden: scenario-local and root files share a basename but not bytes; the global packet golden matches the root file while the selected scenario file has different sizes for all 214,646 video packets, requiring scenario-local packet truth.  _(src: docs/notes/session11-fair-harness-boundary-audit.md)_
- HDR10 routing correction is recorded in ADR-214 and passed its real baked Chromium control chromium-2026-07-11T11-06-29-495Z.json; the latest exact 05.mp4 gapless operation-window failure is recorded in ADR-215.  _(src: docs/notes/session12-gapless-corpus-and-native-suppression.md)_
- HLS AES-128 harness row fails because the public call does not pass a usable manifest-backed source; a reversible experiment (self-resolving manifest 474 B, key 16 B, five encrypted segments, all 200 responses) let a direct fromURL() aibrush probe return ts/H.264/AAC and 10.026667 s, while the detached public invocation still emitted 'not an MPEG-TS stream'. Original manifest hashes 914920.../ba0bec... were restored.  _(src: docs/notes/session12-gapless-corpus-and-native-suppression.md)_
- HLS AES-128 implicit IV was truncated: hls-source.ts wrote only the low 32 bits (setUint32, sequence>>>0), so media sequence 2^32 decrypted with IV 0 to garbage; RFC 8216 §4.3.2.4 requires the media sequence number as a 128-bit big-endian IV; fixed with DataView.setBigUint64(8, ...).  _(src: docs/notes/hls-aes128-iv.md ADR-183)_
- HLS AES-128 packed-audio was misrouted with hard-coded MIME video/mp2t; a raw ADTS/AAC rendition's stitched cleartext is not MPEG-TS (RFC 8216 §3.4), so it must be sniffed to audio/aac (ADTS syncword 0xFFF, layer 0) or video/mp4 (fMP4 with EXT-X-MAP).  _(src: docs/notes/hls-aes128-iv.md ADR-183)_
- HLS AES-128: EXT-X-BYTERANGE continuation (no @offset) was passed straight through and sliced from 0, causing wrong window and PKCS#7 failure; a malformed IV= (non-hex/>128-bit/no prefix) silently fell back to sequence IV producing garbage; encrypted EXT-X-MAP init was appended raw instead of decrypted.  _(src: docs/notes/hls-aes128-iv.md ADR-183)_
- HLS resolver bug: bounded HLS peek treated every source without range() as single-use, returning a replay wrapper marked kind:'stream', which needlessly downgraded a valid custom kind:'bytes' source with fresh stream() per call, causing the subsequent image sniff to reject it as non-seekable. Fixed by re-reading contractually re-readable non-stream kinds.  _(src: docs/notes/session11-hls-rereadable-source.md)_
- Large-file/enumeration bugs (Session 10): huge_h264_1080p_600s dropped a track (AAC reported as other/''); ADTS duration off by 2.9 s (19.99 vs 17.14).  _(src: BUILD_INSTRUCTIONS_SESSION10.md)_
- Lost-features correctness bug transcode/mp3_to_aac_mp4: the MP3 container exposed all coded frames but discarded the encoder-delay/padding tuple, so the AAC encoder received the coded tail and MP4 output exceeded the source duration; fixed by carrying the exact sample window through TrackInfo and trimming the decoded AudioData before re-encode.  _(src: docs/notes/lost-features-2026-07-13-design.md)_
- Lost-features correctness bug transcode/mp3_to_opus_webm: source MP3 Xing/LAME totalSamples expressed at 44.1kHz was copied verbatim into a 48kHz Opus track, making Matroska duration = sourceSampleCount/48000 (12.9707s for 02.mp3) instead of the 14.1177s source program duration; fixed by omitting source gapless facts from the Opus re-encode track so OpusHead/CodecDelay describes the output.  _(src: docs/notes/lost-features-2026-07-13-design.md)_
- Lost-features correctness fix transcode/h264_bitrate_2mbps was FAIL (Chromium native H.264 VBR controller under-allocates first pictures with an explicit average bitrate, producing SSIM 0.9085 at 2Mbps), fixed by priming with 3 disposable encodes at ordinary cadence / 8 at high cadence (>30.5 fps) before the real first keyframe; still behind Remotion (53-69 ms, which does not expose bitrate compliance).  _(src: docs/perf/lost-features-2026-07-13-checklist.md)_
- Lost-features probe/hls_aes128 defect was capability-routing not a missing AES: the adapter's low-latency HLS probe shortcut fetched the first segment and sent ciphertext directly to the MPEG-TS probe, which found no sync run → ERROR; fixed by classifying the parsed media playlist and decrypting only the first segment when a segment inherits an encryption key. Result: probe/hls_aes128 WON (aibrush 23.235 ms vs mediabunny 48.765, ffmpeg 59.295).  _(src: docs/notes/lost-features-2026-07-13-design.md)_
- metadata/write_mkv_tags fails in the black-box harness because its public invocation drops additive TrackInfo.containerSideData (the exact JSON attachment/attached JPEG) before mux; the public source/build browser routes preserve it — a harness invocation seam issue, not a product byte defect.  _(src: BUILD_INSTRUCTIONS_SESSION12.md)_
- metadata/write_mkv_tags rotation 03.mkv (924,924 bytes) declares H.264, AAC, one JSON attachment, one attached JPEG/MJPEG stream; product preserves both AttachedFile payloads (SHA-256 94809623… and 831953157…) through all public routes and ffprobe sees all four streams, but a clean-profile black-box invocation reports two media tracks vs the three-track golden — an invocation-side side-data-drop boundary, not a product defect.  _(src: docs/notes/session11-fair-harness-boundary-audit.md)_
- MP4 AAC geometry bug: a real AAC-LC mono track carried a stale two-channel AudioSampleEntry while ASC 11 88 declared channelConfiguration=1; a real implicit HE-AAC track carried a 24kHz LC core but ASC 13 08 56 e5 98 included the backward-compatible SBR sync extension (0x2b7) for 48kHz decoded presentation; the effective ASC rate wins but the outer entry retains presentation channel geometry (implicit PS can expose stereo from a mono core).  _(src: docs/notes/mp4-aac-asc-geometry.md ADR-190)_
- MP4 keyframe-copy trim previously rebased the retained pre-roll GOP to presentation time zero, so a requested 6-second interval could probe as 8 or 8.976 seconds and audio at the requested start shifted against video at the earlier keyframe; the fix carries a single-rate ISO-BMFF edit (media_time skips to requested source start, segment_duration = requested interval).  _(src: docs/notes/mp4-keyframe-trim-edits.md)_
- mux/edge_hevc_decode_mux_mkv stays red not from a malformed MKV but because Chromium <video> reports zero intrinsic size when asked to play HEVC in a Matroska container; the same output passes 12/12 frame digests when decoded through the media engine, and the remux/hevc_1080p_10s_mp4_to_mkv cell passes structural reimport.  _(src: docs/notes/hevc-mkv-platform-oracle.md)_
- mux/opus_to_ogg first exposed a public parser bug: callers using the documented bounded {track, packetsArray} mux shape were rejected as 'invalid mux packet stream'.  _(src: docs/notes/session9-ogg-prepared-packet-mux.md)_
- On exact CC0 05.mp4, createMedia().decode() emits 49 AAC frames / 50,176 samples (final packet a real 12,188 µs partial-duration packet), matching independent ffprobe/FFmpeg PCM at 50,176 samples; the committed golden of 50,784 samples is disputed and unsupported by the coded timeline. The harness's separate 48,128-sample result is a one-second operation-window cutoff.  _(src: docs/notes/session12-gapless-corpus-and-native-suppression.md)_
- On performance/size-ladder-iterate-packets-massive (massive_h264_1080p_2h.mp4), count and PTS agreed for all 553,501 rows but 214,646 packet sizes mismatched the golden.  _(src: docs/notes/session13-baseline-and-gate-repair.md)_
- On the fair harness, transcode timed out at 120 s on nearly every transform (resize, crop, flip, rotate, colorspace, tonemap, ladders); ladder_large_h264_1080p_120s_resize_720p hit the 310 s timeout and ladder_large_vp9_1080p_120s_to_h264_720p OOMed ('Array buffer allocation failed').  _(src: BUILD_INSTRUCTIONS_SESSION10.md)_
- Optional software encoders for AV1/VP8/VP9/MP3 are deliberately outside the current public envelope until license/provenance and packet/frame validation are approved (ADR-225); the router will not name an unshipped encoder as available support.  _(src: docs/architecture/README.md ADR-225)_
- performance-deficits functional reds include: audio-dsp/edge_gapless_aac_decode fails gapless sample-count invariant (e.g. decoded 49040 vs priming-removed expected 7690464 at 48000Hz); metadata/write_mkv_tags reimport shows 1 video track vs golden 2; mux/edge_hevc_decode_mux_mkv output <video> has zero intrinsic size; performance/size-ladder-iterate-packets-massive (massive_h264_1080p_2h.mp4) has 214646 packet size mismatches; probe/hls_aes128 errors as 'not an MPEG-TS stream'.  _(src: docs/perf/performance-deficits.md)_
- Provisional-baseline aibrush correctness losses: mux/edge_hevc_decode_mux_mkv (platform decode of muxed HEVC output reports zero intrinsic size); transcode/h264_to_av1_mp4 mean SSIM 0.9663 below the 0.97 gate; performance/convert-longtasks mean SSIM 0.9645 below 0.97; metamorphic-transcode-idempotent-source-res mean SSIM 0.9800 below the 0.99 gate.  _(src: docs/notes/session13-baseline-and-gate-repair.md)_
- QuickTime tmcd and other packet-bearing non-AV traks were omitted from packet-info (counted only decodable AV tracks); and an H.264 stss table under-reported scene-cut intra frames because real demuxers also flag a non-IDR access unit as a key picture when its primary slice is I or SI.  _(src: docs/notes/mp4-packet-truth.md)_
- Remuxing real MPEG-TS to MP4 threw 'AAC MP4 muxing cannot mix ADTS-framed and raw samples' because real transport streams pack several ADTS frames per audio PES and split frames across PES packets (broadcast muxers flush on byte budgets), so per-PES splitting emitted inconsistently framed samples; fixed with a stateful per-PID AdtsDeframer emitting one raw AU per ADTS frame.  _(src: docs/notes/mpegts-aac-deframe.md ADR-184)_
- Rotated cbcs/cens inputs decrypt to valid H.264 but Chromium reimport begins three frames late, because the fragmented encrypted file has signed version-1 trun composition offsets and no edts while the clear original uses non-negative ctts plus a single-rate edit-list preroll (e.g. elst.media_time=3003).  _(src: docs/notes/cenc-fragment-timeline-normalization.md)_
- Rotated gapless AAC: the selected HE-AAC file is 44.1 kHz with 2,652,160 coded samples, but the retained expectation is 48 kHz/2,886,720 samples from a different asset; per-rotation sample-rate/sample-count truth must be baked from the selected bytes.  _(src: docs/notes/session11-fair-harness-boundary-audit.md)_
- Session 10 introduced 2 encryption graceful-failure regressions (PASS→FAIL): encryption/cenc_ctr_protection_zeroed_graceful and cenc_ctr_senc_bitflip_graceful now produced output from mutated/malformed input instead of a clean throw.  _(src: BUILD_INSTRUCTIONS_SESSION11.md)_
- Session 11 decrypt/quality reds: demux/hls_aes128 golden-packets 470-packet size mismatch; cenc_cbcs_decrypt 23/24 frame digests differ; transcode/h264_resize_720p SSIM 0.968 / min 0.962 under the 0.97/0.98 gate; extreme_fps_1 out duration 23.0 s vs 22.507 s (Δ0.493 > 0.15); decode_mov_h264 SSIM min 0.854/mean 0.935; remux/h264_ts_ts_to_mp4 duration 10.010 s vs 10.129 s (Δ0.119 > 0.10).  _(src: BUILD_INSTRUCTIONS_SESSION11.md)_
- Session 11: probe/huge_h264_1080p_600s reported 2 tracks vs 3 (AAC dropped/misclassified audio/aac→other/''); edge_gapless_aac_decode gapless sample count wrong (48128 vs 2654203).  _(src: BUILD_INSTRUCTIONS_SESSION11.md)_
- Session 3 blocker: streaming-output/prop_probe_dur_stream_shape (a shape {container:'mp4', target:'stream'} scenario) hung a fresh chromium run indefinitely (froze at 61/79), never settling past the adapter's 30 s per-op timeout — gating all measurement until fixed.  _(src: BUILD_INSTRUCTIONS_SESSION3.md)_
- Session 3 FAILs: mux/opus_to_ogg (and vorbis_to_ogg) probed 9.94 s vs source 10.007 s — short by ~0.067 s due to Opus granule/pre-skip accounting; remux/h264_bframes_1080p_mp4_to_mkv reimport 10.134 s vs golden 10.0 s — long by ~0.134 s from WebM endMs under B-frame reorder.  _(src: BUILD_INSTRUCTIONS_SESSION3.md)_
- Session 5 R0 regressions: mux/mp3_to_mp3 FAIL (Mp3Muxer emitted 2.27 s for a 10 s source); probe/hls_aes128 ERROR 'key must be 16 bytes, got 27872' (HLS resolver fetched the wrong resource as the AES key in-browser).  _(src: BUILD_INSTRUCTIONS_SESSION5.md)_
- Session13 memory losses: decode-seek/decode_vfr_timing peak 672,616,697 B (n=2) exceeds leanest rival 107,579,095 B by 565,037,602 B; audio-dsp/throughput_decode_s24 peak 33,729,168 B vs mediabunny 24,049,595 B; demux/size_large_large_h264_1080p_120s old peak 189,426,678 B vs rival 85,555,893 B (103.9 MB over).  _(src: docs/perf/session13-speed-ledger.md)_
- Shipped WASM codec tails are decode-only for VP8/VP9 (ogv.js/libvpx, 8-bit 4:2:0), AV1 (dav1d, 8-bit 4:2:0 non-monochrome), AAC (Symphonia, AAC-LC mono/stereo only), and MP3; encode for these raises CapabilityError. Opus and Vorbis have both decode and encode tails.  _(src: docs/architecture/09-operations.md)_
- Signed 8-bit AIFF declines the fast byte-rewrite-to-WAV path because legal WAV 8-bit PCM is unsigned and needs value-domain conversion.  _(src: docs/notes/session9-aiff-pcm-byte-rewrite.md)_
- Signed-8-bit AIFF declines the direct byte-rewrite path because legal WAV 8-bit PCM is unsigned and needs value-domain conversion.  _(src: docs/notes/session9-aiff-pcm-byte-rewrite.md)_
- Sub-5ms micro-ops in the browser harness have ±30% run-to-run variance (thermal/scheduling) that exceeds the aibrush-vs-mediabunny median gap, so 'repeatably fastest' needs a clear >~15% margin to survive noise; the n=5 batch results were largely noise, superseded by n=15 dedicated runs.  _(src: docs/perf/goal26-evidence.md)_
- The acceptance harness feature encryption/cbcs_decrypt still routes through media.decrypt using the moov-only decryptCencTrack path (Mp4Driver.decrypt ~line 2696), not the completed whole-file decryptCencFile engine; closing it requires routing scheme in {cenc,cens,cbcs} through decryptCencFile.  _(src: docs/notes/cbcs-real-world-decrypt.md)_
- The ADR-284 selected-file VP9→AV1 run in Chrome 149 did not reproduce the historical native runtime miss: prefer-hardware decoded all 4,482 frames at 6,636.0 ms median; a deterministic fail-first test-only injected CapabilityError was required to force the split wasm-vpx-driver + vpx-core replay path (22,091,035-byte output, SHA 41e677c3…).  _(src: docs/notes/session13-av1-transcode-profile.md)_
- The aibrush adapter installs a page-error suppressor + a 30s per-op abort (below the runner's 120s) + synthesized graceful-rejections — bespoke survival advantages the other 6 adapters lack, applied by the engine's own author inside its own cells; should be moved to the shared runner or disabled for official scoring.  _(src: docs/competitive-gaps.md)_
- The audio-dsp/edge_gapless_aac_decode cell reports 52,384 samples at 48kHz (1.091333s) for rotated 01/02/03.mp4 versus the true ~60.14s coded timeline; the product decode never produces that count, so the 48kHz figure is outside the product source/build/vendor boundary (baked gapless_aac.m4a stays green at 48,623 samples).  _(src: docs/notes/gapless-aac-rotated-fixture.md)_
- The baked base two-hour file reports '214646 packets had a size mismatch' identically before and after the slice-classification change, but an independent fresh ffprobe walk proves 553,501/553,501 packets (per-stream 216,000 + 337,501) with zero size and key-flag mismatches; the stale baked golden vector, not the product, is wrong.  _(src: docs/notes/mp4-packet-truth.md)_
- The benchmark harness runs Chromium-only (no in-repo cross-browser automation); on Safari/WebKit and Firefox a large fraction of the codec tier would become NA_BROWSER not PASS, so any SOTA claim must be scoped to Chromium.  _(src: docs/competitive-gaps.md)_
- The benchmark uses already-loosened duration tolerances for whole container classes (LOOSE_DURATION_CONTAINERS, isLooseMp3, isLooseRecorderWebm), so a green 558 says nothing about the long tail (VFR, open-GOP, broken MediaRecorder WebM, edit lists, truncated downloads).  _(src: docs/competitive-gaps.md)_
- The benchmark's h264->vp8 conversion produced output that failed playback-smoke; aibrush-media treats that as a hard fail and keeps playback-smoke mandatory (now passing in the latest Chromium export).  _(src: docs/architecture/11-testing-and-validation.md)_
- The demuxer previously mapped V_VP9/V_AV1 to bare vp9/av1 and the generic normalizer substituted profile-0 8-bit strings — false for VP9 profile 2 and 10/12-bit AV1; an MP4 VP9 vpcC is not WebM's VP9 codec-feature list. The fix qualifies from container/coded-stream facts before probing, and an unqualifiable stream gets a fourcc-only string (vp09/av01) so the router returns a typed miss instead of silently probing 8-bit profile 0. Unknown cadence/bitrate selects level 6.2.  _(src: docs/notes/session12-webm-high-depth-reimport.md)_
- The direct WAV-s16-to-FLAC verbatim path produces a larger FLAC than the compressing LPC/Rice encoder by design; output-size compression is not part of the wall-time-gated transcode/wav_to_flac oracle, but the stream still decodes bit-exactly to source PCM.  _(src: docs/notes/session9-wav-s16-flac-verbatim-fast-path.md)_
- The first complete gate after cold-loading caught a concurrent-first-write defect in the generic lazy mux proxy: parallel audio/video drains both resumed from the shared driver import and constructed different underlying muxers, losing the first video packet; fixed by memoizing the complete underlying-muxer construction, not only the driver import.  _(src: docs/notes/session13-probe-leadership-2026-07-13.md)_
- The HLS AES-128 probe fails in the black-box harness invocation because it supplies AES ciphertext without the playlist/key/IV/resolver base — information-theoretically insufficient to decrypt; the standalone public-URL path passes. Never guess a key/IV.  _(src: BUILD_INSTRUCTIONS_SESSION12.md)_
- The metadata/write_mkv_tags public rotation (03.mkv SHA-256 15ac6672...) failed strict reimport because the reimport had one video + one audio media track while the golden expected two video + one audio (measured 2 vs golden 3), reproducing the known side-data projection boundary; product attachment preservation is independently covered.  _(src: docs/notes/session12-public-truth-boundaries.md)_
- The Ogg-to-Matroska lifecycle review (ADR-275) exposed correctness gaps: a one-shot producer delivered another chunk at the abort edge instead of cancelling/unlocking, and cross-target Opus trim needed to reset pre-skip in an owned OpusHead when a later-starting selection dropped packet zero; FLAC-derived Ogg must decline the illegal WebM target with a typed capability miss.  _(src: docs/notes/session13-ogg-matroska-public.md)_
- The old writeMkvTags appended another top-level Tags element, so prior SimpleTag values stayed observable, repeated writes grew the file forever, and reader order decided which conflicting value appeared; fixed with offset-stable whole-element replacement voiding stale Tags spans.  _(src: docs/notes/session12-matroska-tag-replacement.md)_
- The Opus software encoder is fully written (wasm-opus-driver.ts FrameAccumulator/re-chunking/pre-skip) but has no libopus core (needs emcc, ADR-031); since Opus is the universal recommended encode fallback for AAC/MP3/Vorbis, that fallback never works where WebCodecs AudioEncoder lacks opus (e.g. flac→opus).  _(src: docs/competitive-gaps.md)_
- The original benchmark found 0 CHEAT but 206 WEAK-GATE plus 3 SUSPECT engine 'wins' -- passes resting on loose gates (duration-only, SSIM exactFrames==0, smoke) or hardcoded shortcuts.  _(src: docs/architecture/11-testing-and-validation.md)_
- The original benchmark's performance margins were cached single-sample measurements and are not trusted or reused; aibrush-media re-measures fresh (multi-sample) instead.  _(src: docs/architecture/11-testing-and-validation.md)_
- The original decrypt path decryptCencTrack read sample-encryption metadata only from moov stbl, so real fragmented cbcs assets (Apple HLS/DASH-IF/CMAF, Bento4 mp4encrypt --method MPEG-CBCS) with empty stbl and per-sample data in moof/traf were rejected with 'cbcs track N has no decryptable samples'.  _(src: docs/notes/cbcs-real-world-decrypt.md)_
- The original WebM driver collapsed four Matroska facts exposed by the rotated h264_in_mkv corpus: cross-track interleaved decode order, H.264 DTS reconstruction from SPS max_num_reorder_frames (tested streams declare reorder depths 0, 1, or 2), CodecDelay timestamp shift (one AAC stream declares 21,333,333 ns), and attachments as declared streams (one file has an application/json attachment plus a valid 480x360 JPEG).  _(src: docs/notes/webm-packet-order-dts-attachments.md)_
- The probe/hls_aes128 harness red was routing, not decrypt: resolveHlsSource decrypts byte-exact to openssl but was never wired into probe/demux/decode, so the .m3u8 manifest reached the MPEG-TS driver (tagged video/mp2t) and ts-parse correctly found no 0x47 sync run in undecrypted bytes; the harness corpus has ~24,461 packets.  _(src: docs/notes/hls-aes128-iv.md ADR-183)_
- The rotated 02.mp4 (5,339,207 bytes) MP4 metadata-rewrite row lost at a qualified 28.380 ms browser median versus mediabunny 21.890, but the 6.490 ms gap is inside the combined 8.825 ms MAD and warm product routing including readback/reprobe/GC is ~1 ms, so it was ruled a fixed browser measurement effect and no speculative change was made.  _(src: docs/notes/session13-mp4-blob-metadata-rewrite.md)_
- The rotated VP9/Opus WebM corpus lost CodecDelay and terminal DiscardPadding across demux/mux even though packet bytes survived; the fix carries leading/trailing/total sample facts through TrackInfo and re-emits OpusHead + CodecDelay + SeekPreRoll + terminal DiscardPadding without changing payloads. At stereo s16, decoded samples = bytes/4; all four source/output pairs match exactly.  _(src: docs/notes/webm-opus-gapless-preservation.md)_
- The strict browser live-media oracle (bench-session12-live-media.mjs, RGBA SHA-256 match of captureStream→MediaStreamTrackProcessor frame) was left unexecuted because the desktop escalation reviewer rejected the Chromium launch after the session hit its usage limit; recorded as an external evidence block, not a PASS. The decision is ADR-236.  _(src: docs/notes/session12-live-media-input.md)_
- The WAV s16 -> FLAC verbatim fast path emits VERBATIM subframes, so its output is larger than the compressing LPC/Rice encoder by design; this is acceptable because transcode/wav_to_flac is wall-time gated and output-size compression is not part of its oracle.  _(src: docs/notes/session9-wav-s16-flac-verbatim-fast-path.md)_
- The WAV trim range threshold is intentionally conservative (>1 MiB for range slicing); a future large WAV trim row may need a lower or adaptive threshold if request latency is much lower than this Chromium harness run.  _(src: docs/notes/session9-wav-pcm-trim-byte-slice.md)_
- The wrong-IV mutation in the AES test corrupts the leading container sync/header because unauthenticated AES-CBC cannot prove an IV mutation that leaves structure valid.  _(src: docs/notes/session12-container-routing-and-direct-aes.md)_
- There is no bundled software H.264 or HEVC decoder fallback in this build; an exact host WebCodecs miss for H.264/HEVC is a typed CapabilityError, not a software fallback.  _(src: docs/architecture/10-browser-capability-matrix.md)_
- VP8/VP9 decode fallback does not actually work: the scaffold has no .wasm core and is not even registered in defaults.ts (lines 58-60), so the Safari/WebKit/unaccelerated-VP9 miss it targets hard-fails with a CapabilityError.  _(src: docs/competitive-gaps.md)_
- wasm-aac rejects SBR/HE-AAC, more than 2 channels, and any AAC encode with CapabilityError; it supports only AAC-LC mono/stereo decode.  _(src: docs/architecture/09-operations.md)_
- web-demuxer@4.0.0 stopped completing audio-dsp/resample_48k_to_16k during the Session 13 provisional baseline, halting the shared partial export at 1,892 of 3,941 result cells.  _(src: docs/notes/session13-baseline-and-gate-repair.md)_

## Fixture provenance  (48)

- ADR-093: AV1 software decode vendors dav1d.js v0.1.1 (npm; dav1d BSD-3/VideoLAN, dav1d.js wrapper CC0), a separate 376 kB dav1d_wasm_bg.wasm with named C exports (djs_decode_obu/djs_alloc_obu/djs_free_frame) and stubbed pthread imports (single-threaded); its YUV output is 8-bit only so 10-bit AV1 decodes to ZERO frames (verified on bear-av1-10bit.mp4) and is declined; throughput ~35 Mpix/s single-thread.  _(src: docs/architecture/02-decision-records.md ADR-093)_
- ADR-094: VP8/VP9 software decode vendors ogv.js v1.9.0 modules (libvpx BSD-3, ogv.js wrappers MIT), base64-embedded self-contained; the glue de-strides ogv's aligned planes into tight I420 and detects a 4:4:4 stream via the U-plane stride (throwing rather than cropping full-res chroma into a 4:2:0 buffer); throughput ~270 Mpix/s VP8 / ~620 Mpix/s VP9, 8-bit 4:2:0 decode-only.  _(src: docs/architecture/02-decision-records.md ADR-094)_
- ADR-204 rotation corpus: huge_h264_1080p_600s.mov (447,748,594 B, 18,000 AVC samples, 300 stss keys, 17,700 non-stss), 01.mov (725,106,140 B, 14,315 samples, 597 keys, 13,718 non-stss), 02.mov (416,751,190 B), 03.mov (249,229,883 B). Each derived file shares extracted sdtp payload SHA-256 c9eec297… with histogram 0x00×597, 0x40×6,561, 0x08×7,157, 0x20×0, 0x10×0 — no sample carries the 0x20/0x10 flags that would decide I vs non-I picture type.  _(src: docs/notes/session11-mp4-avc-key-picture-proof.md)_
- ADR-204's rotated two-hour proof retains exactly 261 non-IDR intra pictures beyond its 1,680 declared sync-table entries; a separate constructed 4,097-AU oracle deliberately places 2,048 non-IDR I pictures outside stss to prove the classifier promotes all of them using fewer than 20 source reads.  _(src: docs/notes/session13-mp4-avc-in-memory.md)_
- ADTS rotation 02.aac exact duration is 861×1024/44100 = 19.992381 s (FFmpeg stream-copy emits 861 MP4 packets); the golden's 17.135660 s value is ffprobe's explicitly warned bitrate estimate for raw VBR ADTS. Duration must be baked from access-unit/sample truth.  _(src: docs/notes/session11-fair-harness-boundary-audit.md)_
- AVI fixtures mjpeg_pcm_160p.avi (MJPEG 160x120 @24fps, 24 frames + PCM s16 16kHz) and mpeg4_mp3_160p.avi (MPEG-4/XVID + MP3) were transcoded from the public-domain WPT movie_5.mp4; ffprobe ground truth: mjpeg dur 1.000s, mpeg4 dur 1.083s.  _(src: docs/notes/avi.md)_
- cbcs decryption is validated across 1:9, full 1:0, and a 5:5 crypt:skip cycle on real video bytes (whole-sample + subsample), plus NIST AES-CTR and AES-CBC vectors on the crypto core.  _(src: docs/architecture/09-operations.md)_
- Competitive-gap evidence was gathered by reading source + .wasm presence (not docs alone) on 2026-06-25 (Session 2); status legend: real-wasm-core = vendored .wasm + Node-validated, webcodecs-only = sole path is hardware WebCodecs with no SW fallback, honest-scaffold = driver+pure-TS framing+typed contract shipped but supports()→false.  _(src: docs/competitive-gaps.md)_
- Decrypt oracles use cleartext-twins generated by an INDEPENDENT tool (not aibrush's own encryptor, which would be a round-trip not an oracle); CENC cenc/cbcs + HLS-AES decrypt were NIST-vector validated.  _(src: BUILD_INSTRUCTIONS_SESSION4.md)_
- demux/wav_s24 golden facts: one audio track, 59 chunks, 1,440,000 total PCM bytes, first PTS zero, 4096-frame PCM chunk rows.  _(src: docs/notes/session9-wav-pcm-packet-info.md)_
- Faststart output-invariance goldens: obs-remux-variable-aac.mp4→MOV is 4,772,852 bytes SHA 78444151c8fa…; bear-rotate-90.mp4→MP4 is 62,959 bytes SHA fe5e8b5f9d17…; movie_5.mp4→MOV is 31,352 bytes SHA 957f4009ab8c….  _(src: docs/notes/session13-mp4-faststart-one-pass.md)_
- Fragmented-audio probe corpus: 01.mp4 (SHA-256 971828672…, 65,765,571 B, 4063.584943 s AAC 44.1kHz stereo), 02.mp4 (97020df9…, 58,145,485 B, 3592.753923 s), 03.mp4 (8fd38eb9…, 59,301,639 B, 3664.178503 s), longform_1h_audio.m4a (74cf9cc3…, 29,659,705 B, 3600.000000 s AAC 48kHz mono). Three rotated DASH files have ftyp + 699-byte moov, one soun track, mvex, empty stts/stsc/stco/stsz, no edts; canonical file is progressive with a 675,922-byte moov.  _(src: docs/notes/session11-fragmented-audio-init-probe.md)_
- HEVC-in-MKV test: baked hevc_1080p_10s.mp4 through the Matroska writer yields ffprobe 1920x1080 HEVC track, 300 packets, a 2,421-byte HEVCDecoderConfigurationRecord, plus a 470-packet AAC track; V_MPEGH/ISO/HEVC blocks carry ISO 14496-15 pictures and CodecPrivate the HEVCDecoderConfigurationRecord.  _(src: docs/notes/hevc-mkv-platform-oracle.md)_
- HLS AES-128 corpus lives under fixtures/media-derived/hls-aes128/, baked by scripts/bake-hls-aes128-fixtures.ts using ffmpeg -hls_key_info_file and openssl enc, every playlist ffprobe-verified at bake time; variants include ffmpeg-explicit-seq47, implicit-seq47, implicit-seq0, rotation, none-mid, byterange, seq-2pow32, audio-adts, fmp4-encmap.  _(src: docs/notes/hls-aes128-iv.md ADR-183)_
- HLS SAMPLE-AES validated on the five clear VOD TS corpus segments hls_vod_000.ts through hls_vod_004.ts, with a Node crypto.createCipheriv('aes-128-cbc') independent encryptor applying the same TS/PES SAMPLE-AES block model.  _(src: docs/notes/hls-sample-aes.md)_
- hls-aes128 is validated by six independently encrypted real TS segments plus six packed-ADTS segments recovering their OpenSSL/ffmpeg clear twins exactly through public media.decrypt.  _(src: docs/architecture/09-operations.md ADR-232)_
- hls-sample-aes is validated by encrypting real hls_vod_000.ts with a test-only Node AES-CBC SAMPLE-AES twin and recovering it byte-for-byte through playlist resolution and media.decrypt.  _(src: docs/architecture/09-operations.md)_
- Large-file packet folds verified identical across byte and Blob routes: 01.mp4 = 9,982 packets, 6,133 keys, 28,774,514 payload bytes, hash 993752762; 02.mp4 = 1,808 packets, 1,111 keys, 74,411,002 bytes, hash 4249479337; 03.mp4 = 13,885 packets, 8,531 keys, 20,260,189 bytes, hash 3929844223.  _(src: docs/notes/session13-mp4-packet-terminal-release.md)_
- Lost-features fresh evidence: Chromium 149, suite 0.1.0, corpus checksum 6f4a1cdb; transcode/h264_bitrate_2mbps post-fix SSIMs per file: 01.mp4 0.9890/0.9884, 02.mp4 0.9709/0.9556, 03.mp4 0.9509/0.9435.  _(src: docs/perf/lost-features-2026-07-13-checklist.md)_
- mov-tmcd-copy.mov (ffmpeg-authored) exposes 232 packets: 120 video, 111 audio, and one four-byte tmcd packet at its declared stream index.  _(src: docs/notes/mp4-packet-truth.md)_
- MPEG-TS driver ground truth: h264_ts.ts (4.63MB, 24,647 x 188B) has PAT@pkt1 program 1 PMT PID 0x1000, PMT PCR_PID 0x100, stream_type 0x1b H.264 @ PID 0x100 and 0x0f AAC-ADTS @ PID 0x101; first video PES PTS 127920 -> 1.42133s (== ffprobe start_time), first audio PES PTS 126000 -> 1.4s.  _(src: docs/notes/mpegts-hls.md)_
- Native gapless suppression is validated separately with five exact Internet Archive AAC-LC stereo 44.1 kHz MP4 files under fixtures/media/native-gapless-aac/ that exhibit negative first DTS plus real MP4 edit-list and sample-group priming/padding; hashes pinned by scripts/session12-gapless-corpus.mjs.  _(src: docs/notes/session12-gapless-corpus-and-native-suppression.md)_
- Pre-change complete-path probe read profile (Chromium 149): ordinary WAV one [0,4096) read (wav_s24/03.wav extends to [4096,65536) due to a non-audio chunk between fmt and data); AIFF always [0,65536); MP3 always [0,16384); FLAC [0,4096) despite 42-byte need; ADTS exact full-file walks (147 to 299,647 bytes); Ogg/Opus full-file reads (3,737 to 145,910 bytes); micro MP4 one 5,546-byte GET.  _(src: docs/notes/session13-probe-leadership-2026-07-13.md)_
- probe/opus uses the 145,910-byte opus.ogg fixture and requires reporting the 10.007 s Opus duration derived from the Ogg final granule position.  _(src: docs/notes/session9-ogg-opus-probe-fast-path.md)_
- Pure-TS FLAC decode is validated bit-exact via STREAMINFO-MD5 on the IETF FLAC conformance corpus.  _(src: docs/architecture/09-operations.md ADR-024)_
- QTFF/MOV goldens: fixtures/golden/metadata/qtff-mov-truth.json pins ffprobe 8.0 output for 10 real files (2 QuickTime-authored headers, 1 Apple avconvert, 6 ffmpeg, 1 OBS remux), baked by scripts/bake-qtff-mov-goldens.ts; the probe benchmark uses the 395KB Big Buck Bunny header (3 traks incl tmcd).  _(src: docs/notes/qtff-mov-parsing.md ADR-185)_
- Rotated gapless-AAC fixture 03.mp4 (SHA-256 402a6ad4...c2c7) is a fragmented HE-AAC stereo track at 44.1kHz, 60.139683s, 1295 packets; public decode drains 1295 AudioData frames, 2,652,160 samples (1295 x 2048) at 44100Hz; fresh-port 01.mp4 has SHA-256 7c301053...40fd, 1295 trun samples, 367,594 bytes.  _(src: docs/notes/gapless-aac-rotated-fixture.md)_
- Session 11 flagged probe/h264_1080p_5s (measured 9.467 s vs golden 6.467 s on a rotated 01.mov) and transcode/h264_crop_center (crop rect 240,135 1440×810 outside 1080×1920 — a landscape rect on a portrait rotated file) as corpus/golden defects to verify against ffprobe before touching engine code.  _(src: BUILD_INSTRUCTIONS_SESSION11.md)_
- The 256-packet crossover for the large-multitrack MP4/MOV prepared-array fast path was derived from a nine-size real-payload sweep (ADR-256/271).  _(src: docs/architecture/09-operations.md ADR-256)_
- The AAC gapless public scenario uses five CC0 1.0 recordings (four BigSoundBank, one LaSonotheque), all ordinary AAC-LC 48 kHz mono/stereo MP4/M4A with no edts/elst/sgpd/sbgp declaration; SHA-256s and durations: 01.mp4 3c6936... 18,953 B 1.013563 s, 02.mp4 4145104b... 16,736 B 1.017563 s, 03.mp4 358885ab... 11,680 B 1.020979 s, 04.mp4 61a6d305... 11,441 B 1.020979 s, 05.mp4 c35fe072... 33,681 B 1.057521 s.  _(src: docs/notes/session12-gapless-corpus-and-native-suppression.md)_
- The architecture evidence began at a 558-feature v0.1.0 benchmark across 7 frameworks; the live Session 12 acceptance suite has 563 cells across 13 families, so completion uses the current full suite not the historical 558 count.  _(src: docs/architecture/11-testing-and-validation.md)_
- The AVI mux validation corpus comprises committed real AVI payloads: full MJPEG+PCM, full MPEG-4+MP3, video-only MJPEG, audio-only PCM, audio-only MP3, and a low-threshold AVIX split; a real MPEG-4/MP3 AVI fixture legitimately contains zero-length video chunks preserved as placeholders.  _(src: docs/notes/session8-avi-mux.md)_
- The benchmark FLAC seektable fixture has only 10 seek points for 105 packets, so a SEEKTABLE cannot honestly produce the packet table; packet enumeration must derive from native frame headers.  _(src: docs/notes/session9-flac-probe-demux-packet-info.md)_
- The canonical WAV mux fast path validates the 44-byte WAV shape by checking RIFF, WAVE, a 16-byte 'fmt ' chunk, and 'data' at byte 44; the mux() alias check requires the payload to alias the same owned buffer at offset 44 as the guard against returning an un-authored source buffer.  _(src: docs/notes/session9-wav-pcm-mux-fast-path.md)_
- The CENC corruption test's baked mutation flips a byte inside a fragment senc IV (surrounding IVs N, one-bit-corrupted, N+2), not encrypted mdat; a direct Bento4 dump corrected an earlier misdiagnosis.  _(src: docs/notes/cenc-ciphertext-decode-validation.md)_
- The gapless AAC test corpus is five exact CC0 real recordings from BigSoundBank and LaSonotheque (URLs/licenses/SHA-256/durations/AAC facts recorded in docs/notes/session12-gapless-corpus-and-native-suppression.md); the old generated Mozilla tones and a trialled long Internet Archive recording are historical only and must not be restored.  _(src: BUILD_INSTRUCTIONS_SESSION12.md)_
- The independently baked 5-second CENC MP4 contains 150 H.264 and 236 AAC access units; the equivalent CBCS file made 300 SubtleCrypto.importKey calls for 150 samples (one CBC decrypt key + one synthetic-pad encrypt key each), 150 encrypts, 150 decrypts.  _(src: docs/notes/session11-cenc-prepared-key-parallelism.md)_
- The PCM WAV frame-encode route (ADR-243) is Node-validated over five real WPT WAV fixtures.  _(src: docs/architecture/09-operations.md ADR-243)_
- transcode/vp8_to_vp9_webm current fixture SHA has 180 VP8 frames at ~60 fps with PTS 0,13,31,47,61… ms; its retained golden described the old 93-frame/30-fps SHA (old PTS 0,57,85,118,151… ms). Deleting the stale placeholder, recreating from current fixture, and running the headed frame bake produced a fresh 12/12 frame/SSIM golden.  _(src: docs/notes/session11-fair-harness-boundary-audit.md)_
- trim/audio_wav_pcm_copy trims the real wav_s16.wav fixture (960044 bytes) from 1.0s to 4.0s, producing 576044 output bytes.  _(src: docs/notes/session9-wav-pcm-trim-byte-slice.md)_
- Two Brave rows showed nearly identical aibrush absolute peaks (33,729,168 bytes for s24 WAV decode, 33,478,114 for Ogg-to-MKV) ~9.7-9.9 MiB above mediabunny despite tiny inputs (141,168 and 3,998 bytes); Chromium's Ogg rotation reported the engines equal within 2,984 bytes while Brave moved mediabunny down ~9.1 MiB, indicating a registration-residency cause not a packet/frame lifetime defect.  _(src: docs/notes/session13-selective-default-container-memory.md)_
- VFR native decode oracle hashes: native VFR 111-frame clock/geometry/color hash e4816556…, aggregate RGBA digest 7c762410…; B-frame 182-frame control hashes f24d1b18… and a687034f…; a compact 4:2:0 CPU frame at 1280x720 is 1,382,400 bytes and 60 such frames total 82,944,000 bytes (near remotion-webcodecs' 107.6 MB peak).  _(src: docs/notes/session13-vfr-decoder-surface-memory.md)_
- WASM cores are Node-validated against independent decoders: AV1/VPx against ffmpeg, Opus encode via an independent ffmpeg-libopus decode SNR oracle, Vorbis encode via independent ffmpeg/libvorbis decode, Symphonia decode via clean-process oracles.  _(src: docs/architecture/09-operations.md)_
- wasm-opus uses a vendored prebuilt permissive libopus-wasm core (MIT wrapper / BSD libopus) that runs in Node too (ADR-088).  _(src: docs/architecture/09-operations.md ADR-088)_
- wasm-vorbis-enc is built from libogg 1.3.6 + libvorbis/libvorbisenc 1.3.7 (permissive Xiph/BSD licenses) as an Emscripten single-file core (ADR-108).  _(src: docs/architecture/09-operations.md ADR-108)_
- WAV frame-encode fail-first test uses five downloaded WPT PCM WAV fixtures (u8, s16, s24, s32, f32) recorded in fixtures/manifest.json, framed at deliberately uneven boundaries; the 32-bit-integer→float frame seam (AudioData.copyTo('f32-planar')) is the only non-bit-exact precision boundary.  _(src: docs/notes/session12-wav-frame-encode.md)_
- WAV metadata-write corpus: speech.wav, sin_440Hz_-6dBFS_1s.wav, sfx-pcm-u8/s16/s24/s32/f32.wav plus a stereo WAV benchmark case; AIFF/CAF derived corpus under fixtures/media-derived/aiff-caf/ (sfx.* variants plus stereo.aiff/stereo.caf).  _(src: docs/notes/session8-metadata-write.md)_
- WebM high-depth reimport: the local real WebM corpus is 8-bit; the pinned BSD-licensed Chromium bear-av1-10bit.mp4 supplies real 10-bit AV1 packets with an exact av1C and reparses to av01.0.00M.10 in TrackInfo; the desired Chromium BSD profile-2 VP9 WebM browser control download was unavailable after the sandbox escalation allowance was exhausted, so no downloaded VP9-browser PASS is claimed.  _(src: docs/notes/session12-webm-high-depth-reimport.md)_

---
_672 unique facts across 6 sections._

