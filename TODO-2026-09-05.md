# `@aibrush/media` — next-session goal list (2026-09-05)

One item per media-test cell where `aibrush-media` is not PASS, or PASSes but is slower than a passing
competitor, in `../media-test/results/cache-chromium-1788562444700.json` (Chromium 152, generated
2026-09-04). Work them one by one, top to bottom within each section. Method and traps:
`REVIEW-2026-09-01.md` ("Instruction status", execution log) and the memory notes
`media-test-cell-run-recipe` / `cell-loss-triage-order`.

## Rules

1. **Library changes only.** Never edit `media-test` oracles, goldens, scenarios, `src/core/runner.ts` or `src/core/measure.ts` (REQUIREMENTS §10). The adapter (`media-test/src/engines/aibrush-media/`) changes only to remove a workaround or call a new public API; every remaining workaround is a recorded library gap.
2. **One cell at a time:** `bash scripts/run.sh --browser chromium --engine aibrush-media,mediabunny,mp4box,remotion --scenario <id> --pillar <pillar> --no-reuse` from `media-test/`, read `results/raw/`, fix the library, `bun run build && (cd ../media-test && bun run sync-vendor)` (run sync twice if the first exits 1), re-run the same cell. Robustness rows are n=1 per run: take three runs before believing a margin under 20%.
3. **Fix the library, never the reference.** If an oracle or golden is wrong, write it down under the item and move on.
4. **Done = re-measured numbers written into the item's Result column and into the review's execution log.** The cache numbers below predate the 2026-09-03 fixes for some rows; the first action on every item is a fresh measurement.
5. **Never attempt** `git add`/`git commit`, applying `/tmp/media-test-memory-window.patch`, or deleting tests (classifier-denied); they are listed under "User runs".
6. Before finishing: `bun run typecheck && bun run docs:check && bun run check-budgets`, `bunx vitest run --minWorkers=1 --maxWorkers=3` (start state: 375 files / 5963 tests green), and `bun test tests/engine-aibrush-media-*.test.ts` in `media-test` (164 pass).

## A. Not PASS (17 cells)

| # | Cell | Status | Cache reason | Where to look | Result |
|---|---|---|---|---|---|
| A1 | `decode-seek/decode_rotated_display_matrix` | FAIL | EVIDENCE_VERDICT_FAIL: sufficient=true; applied=ssim-psnr | rotation applied physically and logically, or display matrix mis-decoded (`parse.ts parseTkhd`, decode rotation in `decode-runner.ts`/`cpu-video.ts`); one rotation authority (REQUIREMENTS §5.4) | |
| A2 | `demux/mislabeled_h264` | FAIL | EVIDENCE_VERDICT_FAIL: sufficient=true; applied=golden-metadata,golden-packets | container measured `webm` vs golden `mp4`: the magic-over-hint rule (`engine.ts #sniffContainer`) only covers `bytes` sources; trace how this input reaches the engine and extend definite-magic routing to it | |
| A3 | `performance/size-ladder-demux-peak-memory-huge` | ERROR (invalidated) | aibrush-media not initialized | adapter `init()`/runtime ordering for large assets: "aibrush-media not initialized" means a `#driverCore()`/`#mp4PacketInfoRuntime()` call before its lazy import; reproduce with `--pillar performance` on one row | |
| A4 | `performance/size-ladder-demux-peak-memory-large` | ERROR (invalidated) | aibrush-media not initialized | adapter `init()`/runtime ordering for large assets: "aibrush-media not initialized" means a `#driverCore()`/`#mp4PacketInfoRuntime()` call before its lazy import; reproduce with `--pillar performance` on one row | |
| A5 | `performance/size-ladder-iterate-packets-huge` | ERROR (invalidated) | aibrush-media not initialized | adapter `init()`/runtime ordering for large assets: "aibrush-media not initialized" means a `#driverCore()`/`#mp4PacketInfoRuntime()` call before its lazy import; reproduce with `--pillar performance` on one row | |
| A6 | `performance/size-ladder-iterate-packets-large` | ERROR (invalidated) | aibrush-media not initialized | adapter `init()`/runtime ordering for large assets: "aibrush-media not initialized" means a `#driverCore()`/`#mp4PacketInfoRuntime()` call before its lazy import; reproduce with `--pillar performance` on one row | |
| A7 | `performance/size-ladder-iterate-packets-massive` | ERROR (invalidated) | aibrush-media not initialized | adapter `init()`/runtime ordering for large assets: "aibrush-media not initialized" means a `#driverCore()`/`#mp4PacketInfoRuntime()` call before its lazy import; reproduce with `--pillar performance` on one row | |
| A8 | `probe/h264_1080p_30s` | FAIL | EVIDENCE_VERDICT_FAIL: sufficient=true; applied=golden-metadata | diff `MediaInfo` against `fixtures/golden/h264_1080p_30s.mp4.meta.json` field by field (`defaultDisposition`, `language`, `encryptionScheme`, fps) | |
| A9 | `remux/massive_h264_1080p_2h_mp4_to_mkv` | NA_ENGINE | [AIBRUSH_FINITE_REMUX_OUTPUT_SIZE_UNSUPPORTED] finite remux publication for 1144401376 declared source bytes exceeds the | finite remux above the 1 GiB complete-payload retention ceiling: stream the MKV to a seekable/OPFS sink instead of retaining the payload (REQUIREMENTS §5.1, §8.4) | |
| A10 | `transcode/aac_to_mp3_mp4` | FAIL | EVIDENCE_VERDICT_FAIL: sufficient=true; applied=playback-smoke,property-invariant | MP3 encoder delay/padding not written or not honored in the MP4 timeline (`wasm-mp3-enc`, `mux-trackinfo.ts` gapless authoring, `outputGaplessForAudioEncoder`) | |
| A11 | `transcode/h264_8bit_to_hevc_10bit` | NA_BROWSER (invalidated) | [WEB_CODECS_CONFIG_UNSUPPORTED] VideoEncoder.isConfigSupported returned supported:false for the concrete config | 10-bit HEVC encode unsupported by this browser: declare it in capability preflight with a documented fallback (software route) or an explicit unsupported result | |
| A12 | `transcode/h264_rotate_normalize` | ERROR (invalidated) | EVIDENCE_ORACLE_ERROR: sufficient=false; applied=playback-smoke,ssim-psnr | oracle error on rotated content; expected to clear with the rotation authority fix, otherwise investigate the output the oracle could not read | |
| A13 | `transcode/hdr10_to_sdr_tonemap` | FAIL | [OPERATION_TIMEOUT] functional operation timeout: operation exceeded timeout of 120000ms | tone-map route exceeds 120 s: measure per-frame cost, ensure the WebGPU path is selected, make the CPU fallback vectorised; retire the adapter's `tryBrowserCanvasHdrTonemapTranscode` workaround | |
| A14 | `transcode/wav_to_flac` | FAIL | EVIDENCE_VERDICT_FAIL: sufficient=true; applied=property-invariant | FLAC output sample count vs STREAMINFO declaration (priming/padding through `flac-convert-plan.ts`, `audio-stream-plan.ts`) | |
| A15 | `transcode/wav_to_mp3_mp4` | FAIL | EVIDENCE_VERDICT_FAIL: sufficient=true; applied=playback-smoke,property-invariant | MP3 encoder delay/padding not written or not honored in the MP4 timeline (`wasm-mp3-enc`, `mux-trackinfo.ts` gapless authoring, `outputGaplessForAudioEncoder`) | |
| A16 | `trim/audio_aac_adts_copy` | NA_ENGINE (invalidated) | [AIBRUSH_AUDIO_PRESENTATION_TIMING_UNSUPPORTED] aibrush-media@dev.trim: raw ADTS carries whole AAC access units with no  | exact ADTS AAC copy trim declared unsupported: implement decode-repair of the boundary access unit + copy, or record the decision (REQUIREMENTS §5.7) | |
| A17 | `trim/h264_rotated_keyframe_aligned` | ERROR (invalidated) | EVIDENCE_ORACLE_ERROR: sufficient=false; applied=playback-smoke,trim-boundaries | oracle error on rotated content; expected to clear with the rotation authority fix, otherwise investigate the output the oracle could not read | |

## B. PASS but slower than a passing competitor (55 cells, worst ratio first)

| # | Cell | Ours (ms) | Best competitor (ms) | Ratio | Where to look | Result |
|---|---|---:|---|---:|---|---|
| B1 | `demux/graceful_truncated_h264` | 31.96 | mp4box 8.56 | ×3.73 | malformed MP4 rejection cost: MP4Box rejects in ~8 ms; measure where our reject path spends time (whole-file copy in the adapter, full driver load before the strict probe declines, error translation) | |
| B2 | `robustness/fuzz_truncated_h264_asset_demux` | 28.38 | mp4box 8.42 | ×3.37 | malformed MP4 rejection cost: MP4Box rejects in ~8 ms; measure where our reject path spends time (whole-file copy in the adapter, full driver load before the strict probe declines, error translation) | |
| B3 | `decode-seek/decode_tiny_dims_2x2_h264` | 5.47 | mediabunny 1.71 | ×3.20 | tiny decode/probe rows: decoder session + first-frame cost; check the live-decoder acceleration cache and whether the seek/decode path opens a fresh `VideoDecoder` per call | |
| B4 | `decode-seek/decode_tiny_dims_1x1` | 4.73 | mediabunny 1.55 | ×3.05 | tiny decode/probe rows: decoder session + first-frame cost; check the live-decoder acceleration cache and whether the seek/decode path opens a fresh `VideoDecoder` per call | |
| B5 | `probe/truncated-header-graceful` | 116.11 | mp4box 43.36 | ×2.68 | cold-realm probe: chunk count/size on the root probe path (esbuild chunk waterfall, `defaults` registration, lazy MP4 probe window); compare fetch count against MP4Box's single file | |
| B6 | `robustness/edge_zero_length_probe` | 19.65 | mp4box 7.40 | ×2.65 | malformed MP4 rejection cost: MP4Box rejects in ~8 ms; measure where our reject path spends time (whole-file copy in the adapter, full driver load before the strict probe declines, error translation) | |
| B7 | `robustness/fuzz_mp4_header_truncated_demux` | 22.94 | mp4box 9.38 | ×2.45 | malformed MP4 rejection cost: MP4Box rejects in ~8 ms; measure where our reject path spends time (whole-file copy in the adapter, full driver load before the strict probe declines, error translation) | |
| B8 | `robustness/edge_audio_only_micro_probe` | 31.56 | mp4box 13.14 | ×2.40 | cold-realm probe: chunk count/size on the root probe path (esbuild chunk waterfall, `defaults` registration, lazy MP4 probe window); compare fetch count against MP4Box's single file | |
| B9 | `robustness/edge_video_only_probe` | 26.94 | mp4box 11.33 | ×2.38 | cold-realm probe: chunk count/size on the root probe path (esbuild chunk waterfall, `defaults` registration, lazy MP4 probe window); compare fetch count against MP4Box's single file | |
| B10 | `probe/tiny_h264_360p_2s` | 0.40 | mediabunny 0.17 | ×2.32 | tiny decode/probe rows: decoder session + first-frame cost; check the live-decoder acceleration cache and whether the seek/decode path opens a fresh `VideoDecoder` per call | |
| B11 | `robustness/edge_audio_only_probe` | 26.75 | mp4box 12.02 | ×2.23 | cold-realm probe: chunk count/size on the root probe path (esbuild chunk waterfall, `defaults` registration, lazy MP4 probe window); compare fetch count against MP4Box's single file | |
| B12 | `robustness/edge_extreme_fps_240_probe` | 25.19 | mp4box 11.46 | ×2.20 | cold-realm probe: chunk count/size on the root probe path (esbuild chunk waterfall, `defaults` registration, lazy MP4 probe window); compare fetch count against MP4Box's single file | |
| B13 | `robustness/edge_dims_2x2_h264_probe` | 25.12 | mp4box 11.43 | ×2.20 | cold-realm probe: chunk count/size on the root probe path (esbuild chunk waterfall, `defaults` registration, lazy MP4 probe window); compare fetch count against MP4Box's single file | |
| B14 | `robustness/edge_vfr_probe` | 27.48 | mp4box 12.93 | ×2.13 | cold-realm probe: chunk count/size on the root probe path (esbuild chunk waterfall, `defaults` registration, lazy MP4 probe window); compare fetch count against MP4Box's single file | |
| B15 | `robustness/edge_video_only_micro_probe` | 21.50 | mp4box 10.32 | ×2.08 | cold-realm probe: chunk count/size on the root probe path (esbuild chunk waterfall, `defaults` registration, lazy MP4 probe window); compare fetch count against MP4Box's single file | |
| B16 | `robustness/edge_gapless_priming_probe` | 24.00 | mp4box 11.56 | ×2.08 | cold-realm probe: chunk count/size on the root probe path (esbuild chunk waterfall, `defaults` registration, lazy MP4 probe window); compare fetch count against MP4Box's single file | |
| B17 | `probe/mp3_cbr_notoc` | 0.14 | mediabunny 0.08 | ×1.75 | small margin or cold-realm cost: re-measure with phase marks before changing anything; fix only what a mark attributes to the library | |
| B18 | `robustness/fuzz_mp4_bitflip_probe` | 37.56 | mp4box 21.79 | ×1.72 | malformed MP4 rejection cost: MP4Box rejects in ~8 ms; measure where our reject path spends time (whole-file copy in the adapter, full driver load before the strict probe declines, error translation) | |
| B19 | `probe/micro_h264_1frame` | 0.32 | mediabunny 0.19 | ×1.68 | tiny decode/probe rows: decoder session + first-frame cost; check the live-decoder acceleration cache and whether the seek/decode path opens a fresh `VideoDecoder` per call | |
| B20 | `metadata/neg_garbled_ilst_mp4_probe` | 35.99 | mp4box 22.05 | ×1.63 | re-measure with phase marks; attribute before changing | |
| B21 | `robustness/edge_extreme_fps_1_probe` | 25.26 | mp4box 15.70 | ×1.61 | cold-realm probe: chunk count/size on the root probe path (esbuild chunk waterfall, `defaults` registration, lazy MP4 probe window); compare fetch count against MP4Box's single file | |
| B22 | `robustness/edge_dims_1x1_decode` | 46.07 | remotion 28.75 | ×1.60 | tiny decode/probe rows: decoder session + first-frame cost; check the live-decoder acceleration cache and whether the seek/decode path opens a fresh `VideoDecoder` per call | |
| B23 | `demux/graceful_mp4_header_destroyed` | 86.24 | mp4box 55.33 | ×1.56 | malformed MP4 rejection cost: MP4Box rejects in ~8 ms; measure where our reject path spends time (whole-file copy in the adapter, full driver load before the strict probe declines, error translation) | |
| B24 | `robustness/fuzz_encrypted_mp4_ciphertext_decode` | 33.69 | mediabunny 23.61 | ×1.43 | small margin or cold-realm cost: re-measure with phase marks before changing anything; fix only what a mark attributes to the library | |
| B25 | `robustness/fuzz_adts_aac_bitflip_probe` | 31.06 | remotion 21.85 | ×1.42 | cold-realm probe: chunk count/size on the root probe path (esbuild chunk waterfall, `defaults` registration, lazy MP4 probe window); compare fetch count against MP4Box's single file | |
| B26 | `probe/mislabeled_h264_content` | 0.49 | mediabunny 0.35 | ×1.40 | small margin or cold-realm cost: re-measure with phase marks before changing anything; fix only what a mark attributes to the library | |
| B27 | `performance/convert-webm-resize-320x180` | 2080.49 | remotion 1504.83 | ×1.38 | encode throughput vs Remotion: compare encoder config (bitrate mode, latency mode, hardware preference) and frame hand-off copies in `codec-convert-runner.ts`/`encodeVideoStream` | |
| B28 | `robustness/fuzz_flac_bitflip_probe` | 23.91 | remotion 17.46 | ×1.37 | cold-realm probe: chunk count/size on the root probe path (esbuild chunk waterfall, `defaults` registration, lazy MP4 probe window); compare fetch count against MP4Box's single file | |
| B29 | `demux/graceful_zero_length` | 10.75 | mp4box 8.04 | ×1.34 | malformed MP4 rejection cost: MP4Box rejects in ~8 ms; measure where our reject path spends time (whole-file copy in the adapter, full driver load before the strict probe declines, error translation) | |
| B30 | `mux/neg_h264_into_ogg_illegal` | 79.18 | mediabunny 61.21 | ×1.29 | robustness negatives (n=1): re-measure first — several were fixed on 2026-09-03 (root runtime, no oracle-side parse); the cache predates those runs | |
| B31 | `metadata/neg_garbled_id3_mp3_probe` | 27.12 | remotion 21.20 | ×1.28 | small margin or cold-realm cost: re-measure with phase marks before changing anything; fix only what a mark attributes to the library | |
| B32 | `robustness/prop_gapless_sample_count_priming` | 42.17 | mediabunny 33.14 | ×1.27 | small margin or cold-realm cost: re-measure with phase marks before changing anything; fix only what a mark attributes to the library | |
| B33 | `robustness/fuzz_mp3_header_truncated_probe` | 22.52 | remotion 17.71 | ×1.27 | cold-realm probe: chunk count/size on the root probe path (esbuild chunk waterfall, `defaults` registration, lazy MP4 probe window); compare fetch count against MP4Box's single file | |
| B34 | `remux/neg_zeroed_mp4_to_mkv` | 158.85 | mediabunny 126.75 | ×1.25 | robustness negatives (n=1): re-measure first — several were fixed on 2026-09-03 (root runtime, no oracle-side parse); the cache predates those runs | |
| B35 | `robustness/edge_seek_negative` | 73.41 | mediabunny 59.26 | ×1.24 | seek: MP4 has no `seekPackets` (whole-file demux); add sample-table random access mirroring `webmSeekFrames`, keep the warm decoder pool | |
| B36 | `robustness/edge_seek_past_eof` | 98.68 | mediabunny 80.36 | ×1.23 | seek: MP4 has no `seekPackets` (whole-file demux); add sample-table random access mirroring `webmSeekFrames`, keep the warm decoder pool | |
| B37 | `robustness/edge_flac_without_seektable_probe` | 24.24 | remotion 20.14 | ×1.20 | cold-realm probe: chunk count/size on the root probe path (esbuild chunk waterfall, `defaults` registration, lazy MP4 probe window); compare fetch count against MP4Box's single file | |
| B38 | `robustness/fuzz_webm_header_truncated_demux` | 24.39 | remotion 20.34 | ×1.20 | small margin or cold-realm cost: re-measure with phase marks before changing anything; fix only what a mark attributes to the library | |
| B39 | `robustness/fuzz_webm_bitflip_probe` | 30.37 | remotion 25.79 | ×1.18 | cold-realm probe: chunk count/size on the root probe path (esbuild chunk waterfall, `defaults` registration, lazy MP4 probe window); compare fetch count against MP4Box's single file | |
| B40 | `decode-seek/decode_extreme_fps_1` | 27.45 | mediabunny 23.59 | ×1.16 | tiny decode/probe rows: decoder session + first-frame cost; check the live-decoder acceleration cache and whether the seek/decode path opens a fresh `VideoDecoder` per call | |
| B41 | `performance/encode-fps` | 5357.56 | remotion 4648.26 | ×1.15 | encode throughput vs Remotion: compare encoder config (bitrate mode, latency mode, hardware preference) and frame hand-off copies in `codec-convert-runner.ts`/`encodeVideoStream` | |
| B42 | `robustness/fuzz_ts_zeroed_spans_demux` | 37.47 | mediabunny 32.65 | ×1.15 | small margin or cold-realm cost: re-measure with phase marks before changing anything; fix only what a mark attributes to the library | |
| B43 | `robustness/edge_headerless_recorder_probe` | 28.41 | remotion 24.89 | ×1.14 | cold-realm probe: chunk count/size on the root probe path (esbuild chunk waterfall, `defaults` registration, lazy MP4 probe window); compare fetch count against MP4Box's single file | |
| B44 | `performance/metamorphic-transcode-idempotent-source-res` | 5255.66 | remotion 4633.02 | ×1.13 | encode throughput vs Remotion: compare encoder config (bitrate mode, latency mode, hardware preference) and frame hand-off copies in `codec-convert-runner.ts`/`encodeVideoStream` | |
| B45 | `transcode/malformed_zero_length_transcode` | 78.16 | remotion 69.54 | ×1.12 | small margin or cold-realm cost: re-measure with phase marks before changing anything; fix only what a mark attributes to the library | |
| B46 | `mux/neg_h264_into_wav_illegal` | 66.87 | mediabunny 59.70 | ×1.12 | robustness negatives (n=1): re-measure first — several were fixed on 2026-09-03 (root runtime, no oracle-side parse); the cache predates those runs | |
| B47 | `performance/seek-ms` | 21.80 | mediabunny 20.07 | ×1.09 | seek: MP4 has no `seekPackets` (whole-file demux); add sample-table random access mirroring `webmSeekFrames`, keep the warm decoder pool | |
| B48 | `probe/micro_audio_short` | 0.19 | mediabunny 0.18 | ×1.08 | small margin or cold-realm cost: re-measure with phase marks before changing anything; fix only what a mark attributes to the library | |
| B49 | `robustness/fuzz_mp4_tail_truncated_demux` | 34.23 | mp4box 31.80 | ×1.08 | malformed MP4 rejection cost: MP4Box rejects in ~8 ms; measure where our reject path spends time (whole-file copy in the adapter, full driver load before the strict probe declines, error translation) | |
| B50 | `probe/fragmented_cmaf` | 1.32 | mediabunny 1.26 | ×1.05 | small margin or cold-realm cost: re-measure with phase marks before changing anything; fix only what a mark attributes to the library | |
| B51 | `robustness/prop_duration_consistent_across_containers` | 58.47 | remotion 56.69 | ×1.03 | small margin or cold-realm cost: re-measure with phase marks before changing anything; fix only what a mark attributes to the library | |
| B52 | `remux/neg_headerless_webm_to_mkv` | 115.30 | mediabunny 111.80 | ×1.03 | robustness negatives (n=1): re-measure first — several were fixed on 2026-09-03 (root runtime, no oracle-side parse); the cache predates those runs | |
| B53 | `mux/neg_vp9_into_adts_illegal` | 34.69 | mediabunny 33.98 | ×1.02 | robustness negatives (n=1): re-measure first — several were fixed on 2026-09-03 (root runtime, no oracle-side parse); the cache predates those runs | |
| B54 | `robustness/fuzz_ogg_opus_header_truncated_probe` | 22.69 | mediabunny 22.26 | ×1.02 | cold-realm probe: chunk count/size on the root probe path (esbuild chunk waterfall, `defaults` registration, lazy MP4 probe window); compare fetch count against MP4Box's single file | |
| B55 | `robustness/edge_mislabeled_container_probe` | 26.04 | mediabunny 25.65 | ×1.02 | cold-realm probe: chunk count/size on the root probe path (esbuild chunk waterfall, `defaults` registration, lazy MP4 probe window); compare fetch count against MP4Box's single file | |

## Out of scope

- Anything in `media-test` beyond the adapter rule above; commits; the memory-window patch; test deletions; release gates; browser-matrix or soak runs.

