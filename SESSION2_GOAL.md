Finish building **aibrush-media** — a SOTA, capability-routed, in-browser media engine — to 100% of its Definition of Done, in /Users/tarekbadr/Home/software/projects/aibrush/aibrush.lib/media. Session 1 reached ~80%; **main is GREEN**. DO NOT STOP until the DoD is green.

FIRST read in full: @BUILD_INSTRUCTIONS_SESSION2.md (remaining-work plan + full mechanics), BUILD_INSTRUCTIONS.md §2 (the DoD), CLAUDE.md, docs/architecture/. Binding. Restate the plan, then build.

HOW TO WORK — MANDATORY: drive this with the **Workflow tool (multi-agent orchestration), opus agents at MAX reasoning effort (ULTRATHINK), as much as possible**. A workflow per phase; fan out parallel opus agents on STRICTLY DISJOINT files (one per driver/feature). Per agent: ULTRATHINK (restate goal; enumerate edge cases — B-frames, VFR, seek, cancel, frame lifetime, backpressure; design note) → FAILING strict-oracle test on ≥5 real downloaded files → SOTA implement → pass → fresh multi-sample benchmark → green. Pattern: understand → fan-out implement → adversarially verify (skeptic agents) → integrate (LEADER runs the harness — single serial oracle — and re-vendors). Keep agents WARM (reassign on finish; never cold-start with backlog left). The cross-cutting #61 DTS seam is ONE coordinated workflow, never parallel edits to the shared contract.

THE GAP IS COVERAGE. Last fresh chromium leaderboard: 214 PASS / 96.4% conformance / **39.8% coverage** vs mediabunny 91.4% — we tie 100% conformance in 9/13 families but win none because **336 scenarios are NA**. Closing them IS the aggregate win. Priority: (1) **#61 DTS packet-seam** — packets()/Muxer.write carry dtsUs ({chunk,dtsUs}; mp4 computes sample.dtsUs but drops it; webm/mp4 muxers + the adapter read it) → flips 6 FAILs; (2) **Gap#5** audio packets() for adts/mp3/ogg; (3) **streaming-output** wiring (convert{fragmented}→fragmentMp4→writeToStreamTarget, 26 NA); (4) **ogg/ts createMuxer** accept foreign packets; (5) **image decode** in-browser + register in defaults.ts + adapter; (6) FAILs: fastStart layout (mp4_progressive_buffer), #62 trim-corrupt (ADR — undetectable at container level), #65 worker, #66 fuzz; (7) **coverage ≥90% branch** (wasm-mp3/aac frame-header tests; v8-ignore browser-only image paths). Then RE-VENDOR → fresh full 558 chromium run → `bun scripts/aggregate.mjs` → webkit + firefox → iterate until WIN.

PRIME DIRECTIVES (non-negotiable): ULTRATHINK at max effort before every step. NEVER STOP until DoD §2 is 100% green; blocked → choose SOTA, record an ADR, continue. EVERY LINE SOTA: strict TS, zero `any`/`!`, typed errors, exhaustive, no dead code; every VideoFrame/AudioData closed once. NO FEATURE WITHOUT TESTS — ≥5 real DOWNLOADED diverse files, a strict oracle that CAN fail (force-software) + a fresh multi-sample benchmark; one-file green is a fail. DOCS ARE LAW (change → doc + ADR same commit). NEVER FAKE — no hardcoded paths, no input→output passthrough, no oracle that can't fail; honest NA, never a fake pass.

MECHANICS (see §4 of that file): bun 1.3.14 (NOT npm/npx); gate = typecheck + biome ci + vitest --coverage + build. Re-vendor: `bun run build && bun run vendor-wasm`, copy dist/*.{js,d.ts,wasm} → ../media-test/media-browser-test/src/engines/aibrush-media/vendor/. Harness: clear results/.browser-cache/chromium, then `bash scripts/run.sh --engine aibrush-media --browser chromium [--feature X]`. The adapter (engines/aibrush-media/adapter.ts) is the harness's own live file (not vendored). NEVER run git. Keep main green.

DONE = BUILD_INSTRUCTIONS §2 every box: all ops + full codec/container set; strict-oracle validation on the downloaded corpus; aggregate WIN vs all 7 engines (fresh, cross-browser); typecheck/lint/format/coverage≥90%/cross-browser green; budgets met; ESM+.d.ts; docs synced; CI green. Begin now: read @BUILD_INSTRUCTIONS_SESSION2.md, create the tasks, launch the first workflow, build until done.
