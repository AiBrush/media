# SESSION 12 — GOAL: finish every feature, then prove #1

## Binding mission

This summarizes, never narrows, [`BUILD_INSTRUCTIONS_SESSION12.md`](BUILD_INSTRUCTIONS_SESSION12.md). Work
continuously until every §2 checkbox and §4 gate is proven green. Never stop at a plan, partial pass, empty
loss list, blocker, or smaller goal. Record blocked paths and continue the next safe item; request input only
when no meaningful safe work remains.

Before every change, ultrathink truth, edge cases, B-frames/VFR, streaming, cancellation, backpressure,
memory, and frame ownership. Use subagents for independent features, research, audits, and verification;
parallelize unrelated work, coordinate shared seams centrally, and review everything before integration.

## Product first

Finish all genuine missing/throwing features before broad harness repetition. The board is 61 N/As—57
evidence/corpus gaps, two intentionally unavailable MP3 encode rows, and two rows awaiting external capability
declarations—plus nine reds;
exact actions are in §1.8.

Every feature needs real implementation, fail-first validation, timing/lifecycle/leak checks, a fresh
multi-sample benchmark, and docs/ADR. Software tails must be reproducible, lazy, same-origin, and
license-compatible. Never fake support, weaken an oracle, special-case assets, or add unapproved licenses.

## Harness and finish condition

Harness is black-box: use only public commands, status/metric/reason output, and exported JSON; never read
scenario/oracle/tolerance/runner/rotation/parser/adapter code.

After product closure: use exact licensed real corpus; supersede every red/rotation; reach zero
functional/bake/coverage gaps; prove no regressions, leaks, or stale cache; pass Chromium/WebKit/Firefox and
`bun run gate`; then obtain qualified same-export warm `n>=5` all-engine evidence with real contested
coverage and zero wall/memory losses. Stop only when the complete Definition of Done is verified.
